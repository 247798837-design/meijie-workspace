<!doctype html>
<html lang="zh-CN">
<!-- 唯一正式面板：梅姐五项管理Obsidian同步版.html｜自2026-07-28起 -->
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>梅姐五项管理｜唯一正式面板</title>
<link rel="icon" href="mei-logo.png">
<style>
:root{--ink:#4a3b2d;--muted:#8b7b6b;--paper:#fcfaf5;--cream:#f5ede1;--line:#eadcc7;--coral:#b87957;--gold:#cda461;--sage:#8b9d87;--lav:#9d8eaa;--shadow:0 16px 40px #80644218}
*{box-sizing:border-box}body{margin:0;background:linear-gradient(135deg,#f4ecdf,#f8fbf7 58%,#eee9f5);color:var(--ink);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif;line-height:1.55}
.shell{max-width:1200px;margin:auto;padding:24px}.hero{background:linear-gradient(90deg,#fbf5ebf2 0%,#fbf5ebd9 42%,#fbf5eb55 100%),url("mei-portrait.png") right center/auto 100% no-repeat;color:var(--ink);border:1px solid #dfc69d;border-radius:24px;padding:22px 26px;box-shadow:var(--shadow);position:relative;overflow:hidden}.hero:after{content:"";position:absolute;right:-70px;top:-80px;width:260px;height:260px;border:1px solid #cda46155;border-radius:50%;box-shadow:0 0 0 24px #cda4610d,0 0 0 48px #cda46108}.brandline{display:grid;grid-template-columns:148px minmax(0,1fr);align-items:center;gap:28px;position:relative;z-index:1}.brandline img{width:148px;height:148px;object-fit:contain;border:0;border-radius:0;background:transparent;padding:0}.hero-copy{max-width:700px}.hero-copy .eyebrow{margin-bottom:8px}.hero-copy h1{margin:0 0 8px}.hero-copy p{margin:0;color:#8b7258;font-size:16px}.eyebrow{letter-spacing:.18em;text-transform:uppercase;font-size:11px;color:#a5783b}.hero h1{margin:5px 0 2px;font-family:Georgia,"Songti SC",serif;font-size:clamp(25px,4vw,40px);line-height:1.15;color:#7f5f35}.hero p{margin:6px 0 18px;color:#8b7258}.stats{display:flex;gap:10px;flex-wrap:wrap;position:relative;z-index:1}.stat{background:#fffaf4b8;border:1px solid #d9bc8c;border-radius:14px;padding:9px 14px;min-width:145px}.stat b{display:block;font-size:21px;color:#8b693b}.review-date-stat{display:flex;align-items:center;gap:12px;flex-wrap:wrap;min-width:min(100%,430px)}.review-date-stat b{display:inline-block}.review-date-stat .btn{padding:9px 14px}.nav{display:flex;gap:8px;overflow:auto;padding:18px 0 8px;position:sticky;top:0;z-index:5;background:linear-gradient(#f7f0e6f5,#f7f0e600)}.nav button{border:1px solid var(--line);background:#fffaf2;color:var(--ink);border-radius:999px;padding:11px 16px;white-space:nowrap;cursor:pointer;transition:.2s}.nav button.active,.nav button:hover{background:#b88b52;color:#fff;border-color:#b88b52;transform:translateY(-1px)}.page{display:none}.page.active{display:block;animation:rise .28s ease}@keyframes rise{from{opacity:0;transform:translateY(5px)}to{opacity:1;transform:none}}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:16px}.card{grid-column:span 6;background:#fffdf9dd;border:1px solid var(--line);border-radius:18px;padding:20px;box-shadow:0 8px 24px #493a2409}.wide{grid-column:1/-1}.third{grid-column:span 4}.title{display:flex;align-items:center;justify-content:space-between;gap:10px;margin:0 0 14px;font-size:18px}.title small{font-size:12px;color:var(--muted);font-weight:400}.sub{font-size:13px;color:var(--muted);margin-top:-8px;margin-bottom:14px}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.kpi{padding:13px;border-radius:14px;background:var(--cream)}.kpi span{display:block;color:var(--muted);font-size:12px}.kpi b{font-size:20px}.field{display:flex;flex-direction:column;gap:6px;margin:10px 0}.field label{font-size:13px;font-weight:700}.field input,.field textarea,.field select{width:100%;border:1px solid var(--line);background:#fff;border-radius:10px;padding:11px 12px;font:inherit;color:var(--ink);outline:none}.field textarea{min-height:120px;resize:vertical}.field input:focus,.field textarea:focus{border-color:var(--sage);box-shadow:0 0 0 3px #668b7820}.row{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.btn{border:0;border-radius:10px;padding:11px 15px;cursor:pointer;font:inherit;font-weight:700;transition:.2s}.btn:hover{transform:translateY(-1px)}.primary{background:var(--ink);color:white}.accent{background:var(--coral);color:white}.soft{background:#f4ecdf;color:var(--ink)}.success{background:var(--sage);color:white}.ghost{background:transparent;border:1px solid var(--line)}.energy{display:flex;gap:8px;flex-wrap:wrap}.energy button{border:1px solid var(--line);background:#fff;border-radius:12px;padding:10px 12px;cursor:pointer}.energy button.selected{background:#f5d9a8;border-color:var(--gold);font-weight:700}.task{display:grid;grid-template-columns:52px 1fr auto;gap:10px;align-items:center;padding:11px 0;border-bottom:1px solid #eee5d8}.task:last-child{border-bottom:0}.tag{font-size:12px;font-weight:800;padding:5px 8px;border-radius:8px;background:#f4ecdf;text-align:center}.task.done .task-text{text-decoration:line-through;color:var(--muted)}.task input[type=text]{border:0;border-bottom:1px solid var(--line);background:transparent;padding:7px;width:100%;font:inherit}.check{width:22px;height:22px;accent-color:var(--sage)}.timeline{display:grid;gap:9px}.slot{display:grid;grid-template-columns:90px 1fr auto;gap:10px;align-items:center;padding:11px;border-radius:11px;background:#f7f3eb}.slot input{border:0;background:transparent;font:inherit;width:100%}.pill{display:inline-flex;align-items:center;gap:5px;padding:5px 9px;border-radius:999px;background:#edf4ef;color:#466a57;font-size:12px}.notice{padding:12px 14px;border-radius:12px;background:#fff4d9;border-left:4px solid var(--gold);font-size:13px}.quote{font-size:20px;font-weight:700;color:var(--lav);padding:14px 0}.trend{display:flex;gap:8px;align-items:end;height:130px;padding:10px 0}.bar{flex:1;min-width:18px;background:linear-gradient(#c58a35,#e8c98d);border-radius:8px 8px 3px 3px;position:relative}.bar span{position:absolute;bottom:-25px;width:100%;text-align:center;font-size:11px;color:var(--muted)}.bar b{position:absolute;top:-20px;width:100%;text-align:center;font-size:11px}.output{white-space:pre-wrap;background:#27323a;color:#e8f0ea;border-radius:12px;padding:15px;max-height:360px;overflow:auto;font:13px/1.6 ui-monospace,SFMono-Regular,Consolas,monospace}.footer{color:var(--muted);font-size:12px;text-align:center;padding:28px 0 10px}@media(max-width:800px){.shell{padding:14px}.card,.third{grid-column:1/-1}.kpis{grid-template-columns:repeat(2,1fr)}.hero{padding:22px}.brandline{grid-template-columns:96px minmax(0,1fr);gap:16px}.brandline img{width:96px;height:96px}.hero-copy h1{font-size:clamp(23px,7vw,32px)}.hero-copy p{font-size:14px}.stat{min-width:120px}.slot{grid-template-columns:72px 1fr}.slot .btn{grid-column:2}}
.goal-years{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.goal-year{padding:18px;border-radius:16px;border:1px solid #e4d1b2;background:linear-gradient(145deg,#fffaf2,#f4eadb)}.goal-year.current{background:linear-gradient(145deg,#9f7445,#c99d63);color:#fff;border-color:#a97b48}.goal-year span{display:block;font-size:12px;opacity:.78}.goal-year b{display:block;font:700 27px/1.2 Georgia,"Songti SC",serif;margin:5px 0}.goal-year p{margin:0;font-size:13px}.goal-flow{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:12px 0}.goal-step{padding:12px;border-radius:12px;background:#f7f0e5;text-align:center;font-size:13px}.goal-step b{display:block;color:#8a6436;font-size:17px}.progress{height:12px;background:#eee4d6;border-radius:999px;overflow:hidden}.progress i{display:block;height:100%;background:linear-gradient(90deg,#b48049,#d3ae71);border-radius:inherit}.goal-table{width:100%;border-collapse:separate;border-spacing:0 8px;min-width:820px}.goal-table th{font-size:12px;color:var(--muted);text-align:left;padding:0 8px}.goal-table td{padding:9px 8px;background:#f9f4ec}.goal-table td:first-child{border-radius:10px 0 0 10px}.goal-table td:last-child{border-radius:0 10px 10px 0}.goal-table input{width:100%;min-width:75px;border:1px solid var(--line);border-radius:8px;padding:9px;background:#fff;color:var(--ink)}.goal-table .note-input{min-width:160px}.goal-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.goal-summary .kpi{background:linear-gradient(145deg,#f7efe3,#fff)}@media(max-width:800px){.goal-years,.goal-flow,.goal-summary{grid-template-columns:1fr}.goal-table{min-width:760px}}.slot-extended{display:grid;grid-template-columns:110px 130px 1fr 90px 90px auto;gap:8px;align-items:center;padding:11px;border-radius:11px;background:#f7f3eb}.slot-extended input,.slot-extended select{border:1px solid var(--line);border-radius:8px;background:#fff;padding:8px;font:inherit;color:var(--ink);min-width:0}.slot-extended small{display:block;color:var(--muted);font-size:11px}.time-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.time-kpis .kpi{background:linear-gradient(145deg,#f7efe3,#fff)}@media(max-width:800px){.slot-extended{grid-template-columns:1fr 1fr}.slot-extended input:nth-of-type(1){grid-column:1/-1}.slot-extended .btn{grid-column:2}.time-kpis{grid-template-columns:repeat(2,1fr)}}.mindset-score{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.mindset-score .field{margin:0}.mindset-check{display:grid;grid-template-columns:repeat(5,1fr);gap:8px}.mindset-check label{padding:10px;border:1px solid var(--line);border-radius:10px;background:#fff;cursor:pointer;font-size:13px}.mindset-check input{accent-color:var(--coral);margin-right:5px}@media(max-width:800px){.mindset-score,.mindset-check{grid-template-columns:1fr 1fr}}.mindset-legacy{display:none}.mindset-buttons .field:has(#mindsetAction),.mindset-buttons .field:has(#mindsetActionResult),.mindset-buttons .field:has(#rescueResult),.mindset-affirm .field:has(#affirmationHelp),.mindset-affirm .field:has(#affirmationFeeling){display:none}.mindset-scorecard{display:block}.mindset-trend{display:none}.mindset-affirm{order:1}.mindset-energy{order:2}.mindset-buttons{order:3}.mindset-scorecard{order:4}.mindset-trend{order:5}.mindset-month{order:6}.month-detail-wrap{overflow:auto;margin-top:12px}.month-detail{width:100%;min-width:760px;border-collapse:collapse;background:#fffaf2;font-size:12px}.month-detail th,.month-detail td{padding:8px;border-bottom:1px solid var(--line);text-align:center}.month-detail th{background:#f7f0e5}.chart-layout{display:block;width:100%;overflow:hidden}.chart-legend{display:none}.chart-scroll{display:block;min-width:0;width:100%;overflow-x:auto}.chart-scroll svg{display:block;width:100%;min-width:0;height:auto;max-height:none}.chart-legend{display:flex;flex-direction:column;justify-content:space-around;gap:4px}.chart-legend span{border-left-width:4px!important;border-left-style:solid!important;padding:4px 6px}.month-detail-wrap{display:none}@media(max-width:700px){.chart-layout{grid-template-columns:105px 1fr}.chart-legend span{font-size:11px}}width:100%;overflow:auto;background:#fffaf2;border:1px solid var(--line);border-radius:12px}.chart-scroll svg{min-width:700px;height:300px}.chart-legend{display:flex;flex-wrap:wrap;gap:8px;margin-top:10px}.chart-legend span{font-size:12px;padding:4px 8px;border-radius:8px;background:#f7f0e5}.month-table-wrap{overflow:auto}.month-table{width:100%;border-collapse:collapse;background:#fffaf2}.month-table th,.month-table td{padding:10px 12px;border-bottom:1px solid var(--line);text-align:left}.month-table th{background:#f7f0e5}.month-table td b{color:var(--gold);font-size:18px}.month-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.month-summary div{padding:18px;border-radius:12px;background:#fffaf2;text-align:center}.month-summary b{display:block;font-size:28px;color:var(--gold)}.month-summary span{font-size:13px;color:var(--muted)}.mindset-table{border:1px solid var(--line);border-radius:12px;overflow:hidden}.mindset-row{display:grid;grid-template-columns:1.2fr .45fr 1fr;gap:10px;align-items:center;padding:9px 12px;border-bottom:1px solid var(--line)}.mindset-row.head{font-weight:700;background:#f7f0e5}.mindset-row span small{display:block;color:var(--muted);font-size:12px;margin-top:3px}.mindset-row input{width:100%;box-sizing:border-box}.mindset-evidence-table .mindset-row{grid-template-columns:minmax(130px,.65fr) minmax(320px,1.75fr) minmax(280px,1.35fr);gap:18px;align-items:start;padding:16px 14px;line-height:1.75}.mindset-evidence-table .mindset-row.head{align-items:center;line-height:1.35}.mindset-evidence-table .mindset-row>div,.mindset-evidence-table .mindset-row>b{min-width:0;word-break:normal;overflow-wrap:break-word}.mindset-evidence-table .pill{white-space:nowrap}@media(max-width:760px){.mindset-evidence-table .mindset-row{grid-template-columns:1fr;gap:8px}.mindset-evidence-table .mindset-row.head{display:none}.mindset-evidence-table .mindset-row>div:before{display:block;margin-bottom:3px;color:var(--muted);font-size:12px;font-weight:700}.mindset-evidence-table .mindset-row>div:nth-child(2):before{content:"日志证据"}.mindset-evidence-table .mindset-row>div:nth-child(3):before{content:"评估结论"}}.score-button{border:0;background:transparent;color:var(--gold);font-size:24px;font-weight:700;cursor:pointer;text-decoration:underline}.mindset-total{padding:12px;text-align:right;background:#fffaf2}.mindset-total b{font-size:24px;color:var(--gold);margin:0 4px}.flywheel-card{border:1px solid #d4af37;background:linear-gradient(135deg,#fffaf2,#f7f5ee)}.flywheel-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.flywheel-card .pill{margin-left:8px}@media(max-width:800px){.flywheel-grid{grid-template-columns:1fr 1fr}}@media(max-width:520px){.flywheel-grid{grid-template-columns:1fr}}.sync-card{border:1px solid #d4af37;background:#fffaf2}.sync-card .pill{margin-left:8px}</style>
<style>@media(max-width:640px){.evening-time-card{grid-template-columns:1fr!important}.evening-time-card>div:last-child{justify-content:flex-start!important}.evening-time-card input{min-height:44px}}</style>
</head>
<body>
<main class="shell">
<header class="hero"><div class="brandline"><img class="brand-logo" src="mei-logo.png" alt="梅姐·天赋成长正式品牌Logo"><div class="hero-copy"><div class="eyebrow">MEI FLYWHEEL · FIVE MANAGEMENT</div><h1>梅姐·五项管理成长驾驶舱</h1><p>陪读妈妈 × 创业者 · 双轨并行执行系统</p></div></div>
  <div class="stats">
    <div class="stat">🎓 高考倒计时<b id="gaokao">--</b></div>
    <div class="stat">🔢 深耕密码<b id="deepDays">--</b></div>
    <div class="stat">📚 陪读天数<b id="陪读天数">--</b></div>
    <div class="stat review-date-stat">📅 <b id="today"></b><span class="pill" id="dailyCacheStatus">面板暂存：检查中</span><span class="pill" id="archiveStatus">Obsidian：待归档</span><button class="btn accent" type="button" onclick="confirmAndExportFullReview()">生成晚间五项复盘文字</button></div>
  </div>
</header>
<nav class="nav" id="nav">
  <button data-page="mindset" class="active">🌿 心态管理</button><button data-page="goals">🎯 目标管理</button><button data-page="customers">🤝 客户管理</button><button data-page="time">⏰ 时间管理</button><button data-page="action">⚡ 行动管理</button><button data-page="learning">📚 学习管理</button>
</nav>

<section id="home" class="page"><div class="grid">
  <div class="card wide sync-card"><h2 class="title">🔗 Obsidian每日闭环 <small>早晨读取昨晚交接，晚上归档最终复盘</small></h2><div class="row"><button class="btn primary" onclick="connectObsidianVault()">🔗 连接 Obsidian Vault</button><button class="btn success" onclick="loadTodayPlanFromYesterdayArchive()">🌅 读取昨晚归档生成今日计划</button><input id="obsidianSyncFile" type="file" accept=".md,.txt,.json" style="display:none" onchange="syncObsidianFile(event)"><button class="btn soft" onclick="confirmAnalysisAndArchive()">💾 归档最终复盘</button><span class="pill" id="syncStatus">尚未连接 Vault</span></div></div>
<div class="card wide"><h2 class="title">🌅 今日五项计划 <small>只读取昨晚已分析、已最终确认的交接</small></h2><div id="todayPlanStatus" class="notice">尚未读取昨晚最终归档。</div><div id="todayFivePlanView" class="goal-summary" style="margin-top:12px"></div></div>
<div class="card wide"><h2 class="title">今日驾驶舱 <small id="homeHint">先选状态，再确认三件事</small></h2><div class="kpis"><div class="kpi"><span>今日心态</span><b id="homeMood">未选择</b></div><div class="kpi"><span>今日主目标</span><b id="homeGoal">未填写</b></div><div class="kpi"><span>行动完成</span><b id="homeDone">0 / 3</b></div><div class="kpi"><span>学习主线</span><b id="homeLearn">未选择</b></div></div></div>
  <div class="card wide flywheel-card"><h2 class="title">💰 今日财富飞轮 <small>写一点 · 拍一点 · 复盘一点</small></h2><div class="flywheel-grid"><div class="field"><label>今天写什么</label><input id="fwWrite" placeholder="一个观点 / 一个故事 / 一张学习卡"></div><div class="field"><label>今天拍什么</label><input id="fwShoot" placeholder="一个30秒问题解决视频"></div><div class="field"><label>今天发到哪里</label><input id="fwPublish" placeholder="视频号 / 小红书 / 公众号 / 朋友圈"></div><div class="field"><label>今天有效连接几人</label><input id="fwConnect" type="number" min="0" placeholder="0"></div><div class="field"><label>今天明确需求几人</label><input id="fwNeed" type="number" min="0" placeholder="0"></div><div class="field"><label>今天成交/到账</label><input id="fwIncome" type="number" min="0" step="0.01" placeholder="0"></div></div><div class="row"><button class="btn accent" onclick="save()">保存今日结果</button><span class="pill">只记结果，不做复杂客户表</span></div><div class="field" style="margin-top:12px"><label>今天复盘一句话</label><textarea id="fwReview" rows="2" placeholder="今天什么动作带来了真实结果？"></textarea></div></div>
  <div class="card"><h2 class="title">🌿 今日心态</h2><div id="mindsetSummary" class="notice">请在心态管理中粘贴今日能量急救包。</div><div class="row" style="margin-top:12px"><button class="btn soft" data-jump="mindset">打开能量急救包</button></div></div>
  <div class="card"><h2 class="title">🎯 今日目标</h2><div class="field"><label>今日唯一主目标</label><input id="dailyGoal" placeholder="今天最重要的结果是什么？"></div><button class="btn primary" onclick="save()">保存今日目标</button></div>
  <div class="card wide"><h2 class="title">⚡ 今日工作清单 <small>从 Obsidian/AI 导入后，只需确认和打勾</small></h2><div class="row" style="margin-bottom:10px"><button class="btn primary" onclick="document.getElementById('taskImportFile').click()">📥 导入Obsidian清单</button><input id="taskImportFile" type="file" accept=".md,.txt" style="display:none" onchange="importTaskMarkdown(event)"><span class="pill">导入后自动生成任务</span></div><div id="tasks"></div><button class="btn accent" onclick="save()">保存执行状态</button></div>
  <div class="card"><h2 class="title">⏰ 今日时间摘要</h2><div id="timeSummary" class="timeline"></div><button class="btn soft" data-jump="time">管理时间块</button></div>
  <div class="card"><h2 class="title">📚 今日学习</h2><div id="learnSummary" class="notice">还没有选择今天的学习主线。</div><button class="btn soft" data-jump="learning" style="margin-top:12px">填写学习卡</button></div>
</div></section>

<section id="mindset" class="page active"><div class="grid">
  <div class="card wide"><h2 class="title">🌅 今日心态计划 <small>来自昨晚最终复盘交接</small></h2><div id="todayMindsetPlanView" class="notice">尚未生成今日心态提醒。</div></div>
  <div class="card wide mindset-energy"><h2 class="title">⚡ 今日能量急救包 <small>日签、塔罗、星座牌和幸运信息整段粘贴</small></h2><div class="field"><label>今日完整能量文案</label><textarea id="energyRaw" placeholder="把小程序生成的今日日签、塔罗牌、星座牌、幸运色/水晶/数字完整复制到这里"></textarea></div><div class="field"><label>今日能量状态（数字，可手动调整）</label><input id="mindsetEnergyScore" type="number" min="0" step="1" placeholder="例如：8；暂不设自动好坏门槛"></div><div class="row"><button class="btn accent" onclick="parseEnergy()">保存并提取摘要</button><span id="energySaved" class="pill">尚未保存</span></div><div id="energyParsed" class="notice" style="margin-top:12px">保存后显示摘要；完整原文始终保留。</div></div>
  <div class="card wide mindset-legacy"><h2 class="title">🧭 今日状态与心理阻力 <small>外部提示和真实状态分开记录</small></h2><div class="mindset-score"><div class="field"><label>小程序外部能量分</label><input id="externalEnergyScore" type="number" min="0" max="100" placeholder="自动提取或手动填写"></div><div class="field"><label>早晨真实状态（1—10）</label><input id="morningMoodScore" type="number" min="1" max="10"></div><div class="field"><label>晚上真实状态（1—10）</label><input id="nightMoodScore" type="number" min="1" max="10"></div></div><div class="field"><label>今天最明显的心理阻力</label><div class="mindset-check"><label><input type="checkbox" value="封闭独处" data-resistance>封闭独处</label><label><input type="checkbox" value="焦虑" data-resistance>焦虑</label><label><input type="checkbox" value="拖延" data-resistance>拖延</label><label><input type="checkbox" value="自我怀疑" data-resistance>自我怀疑</label><label><input type="checkbox" value="害怕成交" data-resistance>害怕成交</label><label><input type="checkbox" value="注意力分散" data-resistance>注意力分散</label><label><input type="checkbox" value="家庭压力" data-resistance>家庭压力</label><label><input type="checkbox" value="暂无明显阻力" data-resistance>暂无明显阻力</label></div></div><div class="field"><label>心理卡点补充</label><textarea id="resistanceNote" placeholder="例如：担心被拒绝，所以一直没有跟进客户"></textarea></div></div>
  <div class="card mindset-buttons"><h2 class="title">🔋 能量按键</h2><div class="energy" id="energyButtons"></div><div class="field"><label>今天选择的调整动作</label><textarea id="mindsetAction" placeholder="例如：主动联系一位客户，完成一次深度沟通"></textarea></div><div class="field"><label>动作结果</label><textarea id="mindsetActionResult" placeholder="执行了吗？状态有什么变化？有没有产生行动结果？"></textarea></div><div class="field"><label>动作是否有帮助</label><select id="rescueResult"><option>未记录</option><option>有帮助</option><option>一般</option><option>没有帮助</option></select></div></div>
  <div class="card mindset-affirm"><h2 class="title">✨ 今日肯定语</h2><div class="quote" id="affirmation"></div><div class="row"><button class="btn soft" onclick="newAffirmation()">换一句</button><button class="btn accent" onclick="confirmAffirmation()">确认这句</button></div></div>
  <div class="card wide mindset-trend"><h2 class="title">📅 本周能量趋势</h2><div id="trend" class="trend"></div><p class="sub">记录早晚状态，观察哪些行动真正帮助你恢复。</p></div>
  <div class="card wide mindset-legacy"><h2 class="title">🌙 晚间心态复盘 <small>连接明天的目标与行动</small></h2><div class="grid"><div class="field third"><label>今天最有效的恢复动作</label><input id="effectiveMindsetAction"></div><div class="field third"><label>明天继续/调整/停止什么</label><input id="tomorrowMindsetAdjust"></div><div class="field third"><label>是否需要缩小明日行动颗粒度</label><select id="mindsetTomorrowMode"><option>不需要</option><option>需要，改成最小行动</option><option>需要，先恢复状态</option></select></div></div></div>
  <div class="card wide mindset-scorecard"><h2 class="title">📊 今日心态评比 <small>每项分数可编辑，晚上按事实复盘</small></h2><p class="sub">每项按0—10分填写并补一句事实证据。评判门槛：待确认；当前只记录真实分数，不自动判定“好／差”或“达标／不达标”。</p><div class="mindset-table"><div class="mindset-row head"><span>心态标准</span><span>今日评分（0—10）</span><span>今天的一句话证据</span></div><div class="mindset-row"><span>认真<small>第一次做好，重视品质</small></span><input id="mindset10_0" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_0" placeholder="今天的事实"></div><div class="mindset-row"><span>快<small>马上行动，当日事当日毕</small></span><input id="mindset10_1" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_1" placeholder="今天的事实"></div><div class="mindset-row"><span>坚守承诺<small>说到做到，守信</small></span><input id="mindset10_2" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_2" placeholder="今天的事实"></div><div class="mindset-row"><span>保证完成任务<small>对结果负责</small></span><input id="mindset10_3" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_3" placeholder="今天的事实"></div><div class="mindset-row"><span>乐观<small>看到事情有益的一面</small></span><input id="mindset10_4" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_4" placeholder="今天的事实"></div><div class="mindset-row"><span>自信<small>敢于接受挑战</small></span><input id="mindset10_5" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_5" placeholder="今天的事实"></div><div class="mindset-row"><span>爱与奉献<small>主动帮助、分享</small></span><input id="mindset10_6" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_6" placeholder="今天的事实"></div><div class="mindset-row"><span>绝不找借口<small>寻找方法和承担责任</small></span><input id="mindset10_7" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_7" placeholder="今天的事实"></div><div class="mindset-row"><span>聚焦目标<small>关注结果与解决方案</small></span><input id="mindset10_8" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_8" placeholder="今天的事实"></div><div class="mindset-row"><span>持续成长<small>复盘并改进</small></span><input id="mindset10_9" type="number" min="0" max="10" placeholder="0-10"><input id="mindset10_note_9" placeholder="今天的事实"></div><div class="mindset-total">今日心态总分：<button id="mindsetTotal" class="score-button" onclick="recordMindsetTotal()">0</button> / 100 <button class="btn soft" onclick="calcMindset10()">计算总分</button></div></div></div>
</div><div class="card wide mindset-month"><h2 class="title">📈 月度心态评分曲线 <select id="chartMonth" onchange="renderMonthSummary()" style="margin-left:12px;padding:6px 10px;border-radius:8px;border:1px solid var(--line)"></select></h2><div class="chart-layout"><div id="monthMindsetLegend" class="chart-legend"></div><div class="chart-scroll"><svg id="monthMindsetChart" viewBox="0 0 1120 480" preserveAspectRatio="none"></svg></div></div><div class="month-detail-wrap"><table class="month-detail"><thead><tr><th>日期</th><th>认真</th><th>快</th><th>承诺</th><th>完成</th><th>乐观</th><th>自信</th><th>奉献</th><th>不找借口</th><th>聚焦</th><th>成长</th></tr></thead><tbody id="monthMindsetRows"><tr><td colspan="11">填写评分后自动生成</td></tr></tbody></table></div><p class="sub">每天填数字并保存，系统自动生成当天日期和一行记录；同一天再次保存会更新当天数据。</p></div>
</section>

<section id="goals" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">🎯 今日重要三件事 <small>昨晚生成，白天排时间并执行；晚间确认结果</small></h2><div id="todayBigThreeView" class="goal-flow"></div></div>
  <div class="card wide"><h2 class="title">🧭 本月、本周、今日目标 <small>由 Obsidian 同步；今日目标由昨晚复盘生成</small></h2><div class="grid">
    <div class="card third"><span class="sub">本月主线</span><b id="monthGoalView">未同步</b></div><div class="card third"><span class="sub">本周主目标</span><b id="weekGoalView">未同步</b></div><div class="card third"><span class="sub">今日主目标</span><b id="goalDailyGoalView">昨晚复盘尚未生成</b></div>
  </div></div>
  <div class="card wide"><h2 class="title">➡️ 明日目标与行动草案 <small>今晚复盘后生成；明天自动成为今日目标与行动</small></h2><div class="notice">这里不勾选、不手填。复盘确认后，Obsidian 的明日目标和行动会自动显示在这里。</div><div id="tomorrowActionBoard"></div></div>
  <div class="card wide"><h2 class="title">🧭 2026—2028 三年目标 <small>从长期方向倒推今天，不把愿望停在纸上</small></h2><div class="goal-years">
    <div class="goal-year current"><span>2026｜闭环验证年</span><b>收入 ¥80,000</b><p>建立“梅姐·天赋成长”内容、客户、成交、交付闭环。</p></div>
    <div class="goal-year"><span>2027｜稳定增长年</span><b>收入 ¥200,000</b><p>形成稳定栏目、案例资产、复购与转介绍系统。</p></div>
    <div class="goal-year"><span>2028｜品牌放大年</span><b>收入 ¥500,000</b><p>放大个人品牌、团队协作与长期事业价值。</p></div>
  </div></div>
  <div class="card wide"><h2 class="title">💰 2026年度经营目标 <small>只按实际到账计算完成率</small></h2>
    <div class="goal-summary"><div class="kpi"><span>年度目标</span><b>¥80,000</b></div><div class="kpi"><span>已确认到账</span><b id="yearActualView">¥18,407.60</b></div><div class="kpi"><span>收入差额</span><b id="yearGapView">¥61,592.40</b></div><div class="kpi"><span>完成率</span><b id="yearRateView">23%</b></div></div>
    <div class="progress" style="margin:14px 0 8px"><i id="yearProgress" style="width:23%"></i></div>
    <div class="goal-flow"><div class="goal-step"><b>4人</b>49,800导师班</div><div class="goal-step"><b>1人</b>14,999高级进阶</div><div class="goal-step"><b>40人</b>有效咨询</div><div class="goal-step"><b>140人</b>有效线索</div><div class="goal-step"><b>11,650</b>目标曝光</div></div>
    <div class="notice">推导依据：历史漏斗“曝光2330 → 私信28 → 咨询8 → 成交1”。计划用于执行，实际结果用于复盘校准；导师班参考佣金按30%计算，到账后仍需手动确认。</div>
  </div>
  <div class="card wide"><h2 class="title">🪜 已确认成交与到账 <small>只核对，不手填</small></h2><div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>客户／记录</th><th>产品</th><th>成交金额</th><th>实际到账</th><th>核对</th></tr></thead><tbody id="confirmedIncomeRows">
    <tr><td>2026年3月</td><td>历史收入记录</td><td>未拆分</td><td>—</td><td>¥1,573</td><td><button class="btn success" type="button" onclick="confirmIncome('2026年3月｜历史收入｜¥1,573')">金额无误</button></td></tr>
    <tr><td>2026年4月</td><td>李立凤</td><td>导师班</td><td>¥43,333</td><td>约¥13,000（30%佣金）</td><td><button class="btn success" type="button" onclick="confirmIncome('李立凤｜导师班｜¥43,333｜约¥13,000')">金额无误</button></td></tr>
    <tr><td>2026年4月</td><td>其他9笔历史成交</td><td>未拆分</td><td>—</td><td>¥1,372</td><td><button class="btn success" type="button" onclick="confirmIncome('2026年4月｜其他9笔历史成交｜¥1,372')">金额无误</button></td></tr>
    <tr><td>2026年5月</td><td>999创富营、365年卡与红包</td><td>历史收入</td><td>—</td><td>¥1,864</td><td><button class="btn success" type="button" onclick="confirmIncome('2026年5月｜历史收入｜¥1,864')">金额无误</button></td></tr>
    <tr><td>2026年6月</td><td>199元产品与999产品佣金</td><td>历史收入</td><td>—</td><td>¥598.60</td><td><button class="btn success" type="button" onclick="confirmIncome('2026年6月｜历史收入｜¥598.60')">金额无误</button></td></tr>
  </tbody></table></div><p class="sub">明细来自 Obsidian 已有记录；尚未拆分到单笔的历史到账只显示汇总，等补齐原始记录后再自动列出。</p></div>
  <div class="card wide"><h2 class="title">📊 本月执行结果 <small>只填写实际数字；计划、完成率与差额自动计算</small></h2><div id="targetTable"></div></div>
</div></section>

<section id="customers" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">🤝 客户管理 <small>管理跟进过程；收入结果仍归目标管理</small></h2><div class="notice">客户管理负责记录“从哪里来、需要什么、跟进到哪一步”；成交与实际到账结果同步显示到目标管理，不重复记账。</div><div id="customerSummary" class="goal-summary" style="margin-top:14px"></div></div>
  <div class="card wide"><h2 class="title">➕ 新增／编辑客户</h2><input id="customerEditId" type="hidden"><div class="grid">
    <div class="field third"><label>客户姓名／昵称</label><input id="customerName" placeholder="必填"></div>
    <div class="field third"><label>来源平台</label><select id="customerSource"><option>微信</option><option>视频号</option><option>小红书</option><option>公众号</option><option>朋友圈</option><option>直播</option><option>转介绍</option><option>线下</option><option>其他</option></select></div>
    <div class="field third"><label>生命数字／编号</label><input id="customerContact" placeholder="例如：3号人"></div>
    <div class="field third"><label>当前阶段</label><select id="customerStage"><option value="S0">S0 待触达</option><option value="S1">S1 刚认识</option><option value="S2">S2 有兴趣</option><option value="S3">S3 中意向</option><option value="S4">S4 高意向</option><option value="S5">S5 已成交</option></select></div>
    <div class="field third"><label>意向产品</label><select id="customerProduct"><option>待判断</option><option>1199产品</option><option>14999产品</option><option>49800产品</option><option>其他产品</option></select></div>
    <div class="field third"><label>预计成交金额</label><input id="customerExpected" type="number" min="0" step="0.01" placeholder="0"></div>
    <div class="field third"><label>加入／最近联系日期</label><input id="customerLastFollow" type="date"></div>
    <div class="field third"><label>下一步行动日期</label><input id="customerNextFollow" type="date"></div>
    <div class="field third"><label>成交日期</label><input id="customerDealDate" type="date"></div>
    <div class="field third"><label>成交金额</label><input id="customerDealAmount" type="number" min="0" step="0.01" placeholder="0"></div>
    <div class="field third"><label>实际到账</label><input id="customerPaidAmount" type="number" min="0" step="0.01" placeholder="0"></div>
    <div class="field third"><label>需求／关键情况</label><input id="customerNeed" placeholder="她目前最需要解决什么？"></div>
  </div><div class="field"><label>备注与下一步行动</label><textarea id="customerNote" rows="3" placeholder="原客户备注、下一步做什么、何时推进到下一阶段"></textarea></div><div class="row"><button class="btn accent" type="button" onclick="saveCustomer()">保存客户</button><button class="btn soft" type="button" onclick="resetCustomerForm()">清空表单</button><span class="pill">数据保存在本机浏览器</span></div></div>
  <div class="card wide"><h2 class="title">📌 今日待跟进 <small>到期和超期客户优先显示</small></h2><div id="customerDueList"></div></div>
  <div class="card wide"><h2 class="title">🗂️ 客户清单</h2><div class="row" style="margin-bottom:12px"><input id="customerSearch" placeholder="搜索姓名、需求、来源或产品" oninput="renderCustomers()" style="max-width:360px"><select id="customerStageFilter" onchange="renderCustomers()" style="max-width:220px"><option value="">全部阶段</option><option value="S4">S4 高意向</option><option value="S3">S3 中意向</option><option value="S2">S2 有兴趣</option><option value="S1">S1 刚认识</option><option value="S0">S0 待触达</option><option value="S5">S5 已成交</option></select><button class="btn soft" type="button" onclick="importPreviousCustomers()">📥 导入原客户档案</button></div><div id="customerTable"></div></div>
</div></section>

<section id="time" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">🧭 今日事项线（四象限） <small>早上先看昨天复盘确定的全天行动地图</small></h2><div class="notice" style="margin-bottom:14px"><b>判断来源：</b>只读取昨天的《五项管理复盘》总结，由小杰根据昨天的实际结果、未完成原因、今日目标和行动优先级确定四象限与时间线；页面不使用固定关键词或默认答案自行判断。</div><div id="timePlanSource" class="row" style="margin-bottom:12px"></div><div id="timeQuadrants" class="goal-flow"></div></div>
  <div class="card wide"><h2 class="title">⏰ 时间管理 <small>白天看事项，晚上集中填写</small></h2><div class="notice"><b>使用方法：</b>早上先看上面的四象限安排；晚上只按任务清单逐项填写实际花费分钟。填入大于0的分钟会自动记为“已完成”，没有做就点“今日未做”。所有内容立即按日期保存，刷新不会丢。</div><div id="timeDailyStatus" class="row" style="margin-top:12px"></div><div style="display:none"><span id="timePlanView"></span><span id="timeCapacityView"></span><span id="timeActualView"></span><span id="timeDoneView"></span><span id="timeRateView"></span></div></div>
  <div class="card wide"><h2 class="title">🌙 今晚填写实际用时 <small>每项填分钟，未做就标记</small></h2><div id="slots" class="timeline"></div></div>
  <div class="card wide"><h2 class="title">🥧 今日12小时分布 <small>左边看饼图，右边看时间管理数据分配</small></h2><div id="timePie"></div></div>
  <div class="card wide"><h2 class="title">📊 本周／本月时间统计 <small>每天确认后自动累计</small></h2><div id="timeSummaryTable"></div></div>
</div></section>

<section id="action" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">✅ 今日行动确认 <small>完成就打勾，确认后才进入今日整体复盘</small></h2><div id="todayActionChecklist"></div></div>
  <div class="card wide"><h2 class="title">📅 本周行动 <small>每天确认后自动汇总</small></h2><div id="weekActionBoard"></div></div>
  <div class="card wide"><h2 class="title">🗓️ 本月行动 <small>看整月各板块完成了多少动作</small></h2><div id="monthActionBoard"></div></div>
</div></section>

<section id="learning" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">🌅 今日学习计划 <small>围绕今日目标，当天学习当天应用</small></h2><div id="todayLearningPlanView" class="notice">尚未生成今日学习安排。</div></div>
 <div class="card wide"><h2 class="title">🏷️ 学习来源确认 <small>会议记录／学习笔记上传后生成学习卡，再勾选对应来源</small></h2><div id="learningSourceChecks" class="mindset-check"></div></div>
  <div class="card wide"><h2 class="title">📚 今日学习 <small>只读取 Obsidian 已上传的学习笔记／学习卡</small></h2><div class="notice">不是为了学习而学习。只有上传到 Obsidian 的笔记才进入这里；确认学完后，才归纳为可应用的学习卡。</div><div id="todayLearningCards"></div></div>
  <div class="card wide"><h2 class="title">🗂️ 本周学习卡 <small>看这周真正学了什么、准备应用什么</small></h2><div id="weekLearningCards"></div></div>
  <div class="card wide"><h2 class="title">🗓️ 本月学习卡 <small>只显示本月归属的学习卡与应用状态</small></h2><div id="monthLearningCards"></div></div>
</div></section>


<section id="review" class="page"><div class="grid">
  <div class="card wide"><h2 class="title">🌙 晚间五项复盘 <small>自动读取白天五个管理板块，不重复填写</small></h2><div class="notice">心态、目标、时间、行动、学习就是今天的计划与执行。晚上这里只按同一天留痕逐项生成复盘。</div><div id="eveningFiveReview" class="grid" style="margin-top:14px"></div><div class="row" style="margin-top:14px"><button class="btn accent" type="button" onclick="generateEveningReviewText()">生成并复制给小杰分析</button><span class="pill" id="eveningReviewStatus">尚未生成</span></div></div>
  <div class="card wide"><h2 class="title">✍️ 晚间复盘补充 <small>用于调整明日工作内容</small></h2><div class="grid"><div class="field third"><label>今日最有效的动作</label><input id="dailyEffectiveAction" placeholder="哪个动作真正推进了结果？"></div><div class="field third"><label>问题根因</label><input id="dailyRootCause" placeholder="未完成或偏差的根本原因"></div><div class="field third"><label>明日保留</label><input id="dailyTomorrowKeep" placeholder="继续做什么"></div><div class="field third"><label>明日调整</label><input id="dailyTomorrowAdjust" placeholder="怎么调整"></div><div class="field third"><label>明日停止</label><input id="dailyTomorrowStop" placeholder="停止什么"></div><div class="field third"><label>未完成原因补充</label><input id="dailyUnfinishedReason" placeholder="没有未完成事项可填“无”"></div></div></div>
  <div class="card wide"><h2 class="title">🔁 今日整体复盘 <small>五项已确认数据自动叠加，不重复填写</small></h2><div class="notice">心态、目标、时间、行动、学习各自确认后，自动汇总到这里，形成今天的全貌。</div><div id="fullReviewBoard"></div></div>
  <div class="card wide"><h2 class="title">🗂️ 按日期查看面板日档 <small>历史判断以 Obsidian 归档为准</small></h2><div class="row"><input id="historyRecordDate" type="date" style="max-width:220px" onchange="renderDailyRecordHistory(this.value)"><span class="pill" id="historyRecordStatus">请选择日期</span></div><div id="historyRecordView" class="output" style="display:block;margin-top:12px;background:#fffaf2;color:var(--ink);max-height:480px">选择日期后查看当天五项管理留痕。</div></div>
  <div class="card wide"><h2 class="title">🤖 小杰分析与最终归档 <small>先分析，后归档</small></h2><div id="reviewAnalysisBoard" class="notice">第一步生成晚间五项复盘并发给小杰；第二步把小杰分析结论粘贴到下方；第三步确认最终总结并写入 Obsidian。</div><div class="field"><label>小杰分析结论</label><textarea id="aiReviewAnalysis" rows="10" placeholder="粘贴小杰对今日结果、差距、根因和明日调整的分析"></textarea></div><div class="row"><button class="btn soft" type="button" onclick="generateEveningReviewText()">① 生成并复制复盘文字</button><button class="btn accent" type="button" onclick="confirmAnalysisAndArchive()">② 确认总结并归档 Obsidian</button><span class="pill" id="finalReviewStatus">等待分析</span></div></div>
  <div id="reviewExport" class="output wide" style="display:none"></div>
</div></section>

<footer class="footer">五项管理：心态 · 目标 · 时间 · 行动 · 学习　｜　今日记录，今晚复盘，明天迭代</footer>
</main>
<script>
const KEY="mei_five_management_v1";const affirmations=["我感受到无条件的爱","我允许自己一步一步来","我有能力把学习变成行动","我今天只需要做好最重要的一件事","我值得被看见，也值得被支持","我的稳定行动会带来长期改变"];
const state=JSON.parse(localStorage.getItem(KEY)||"{}");state.tasks??=[{text:"",done:false},{text:"",done:false},{text:"",done:false}];if(!state.obsidianSeeded){Object.assign(state,{yearGoal:"建立稳定、合规、可复制的梅姐个人品牌内容系统；通过内容获得关注、咨询和长期信任。",monthGoal:"跑通《梅姐1分钟成长课》生产模板，完成视频号复苏并补齐公众号、小红书多平台复用。",weekGoal:"跑通一个母题从IMA素材检索到视频号、公众号、小红书三平台成品的完整闭环。",dailyGoal:"推进母题《越懂事的人，为什么越累》的三平台闭环。",learningTrack:"驿马课程",workBlock:"视频号实名认证未完成；30～60秒干净原声尚未归档；小红书模板尚未跑通。",tasks:[{text:"补齐第1期视频号原声、头像和正式字幕",done:false},{text:"完成公众号深度文章真实故事与完整方法改编",done:false},{text:"建立小红书9图模板并生成首套9图",done:false}],targets:{short:{goal:60,current:0,note:""},xhs:{goal:20,current:0,note:""},mp:{goal:8,current:0,note:""},leads:{goal:90,current:0,note:""},live:{goal:20,current:0,note:""},follow:{goal:200,current:0,note:""},wechat:{goal:90,current:0,note:""},income:{goal:6667,current:0,note:""},study:{goal:7,current:0,note:""}},obsidianSeeded:true});localStorage.setItem(KEY,JSON.stringify(state));}state.slots??=[{time:"09:00–10:00",text:"内容生产",done:false},{time:"14:00–15:00",text:"直播/客户沟通",done:false},{time:"20:30–21:00",text:"学习与复盘",done:false}];state.energy??={};state.energyHistory??=[];state.learningTrack??="驿马课程";state.mindset??={resistances:[]};state.fixedTime??="";state.timeReview??="";state.flywheel??={write:"",shoot:"",publish:"",connect:0,need:0,income:0,review:""};state.goalPlan??={yearTarget:80000,yearBaseActual:18407.6,monthKeyword:"高阶客户转化",monthProject:"视频号复苏＋三平台内容闭环＋高阶客户跟进",weekBlock:"视频号实名认证、稳定内容发布与有效线索承接仍需跑通",weekCustomerFocus:"完成本周重点意向客户的深度沟通，识别1199、14999、49800匹配路径",weekLearningFocus:"驿马课程＋自媒体创业＋AI工作流，各形成一个实际应用",weekBigThree:"1. 完成《越懂事的人，为什么越累》三平台闭环\n2. 深度跟进重点意向客户并记录结果\n3. 完成一张学习卡并落地应用",aiGoalSuggestion:"",aiGoalStatus:"待确认",products:{p1199:{deals:0,income:0},p14999:{deals:0,income:0},p49800:{deals:0,income:0}}};
state.goalMetrics??={exposure:{goal:2330,current:0,note:"历史漏斗月度基线"},leads:{goal:28,current:0,note:"评论、私信、微信等有效线索"},consult:{goal:8,current:0,note:"完成需求判断的有效沟通"},deepTalk:{goal:4,current:0,note:"30分钟以上深度沟通"},highTicket:{goal:1,current:0,note:"14999或49800高阶客户"},video:{goal:14,current:0,note:"14天复苏与稳定栏目"},xhs:{goal:8,current:0,note:"搜索型图文"},mp:{goal:4,current:0,note:"深度母文"},moments:{goal:90,current:0,note:"早认知、午现场、晚复盘/见证"},live:{goal:4,current:0,note:"实名认证和复苏达标后执行"},learning:{goal:8,current:0,note:"驿马、自媒体、AI、榜样深耕"}};
function $(id){return document.getElementById(id)}function save(){read();const scores=Array.from({length:10},(_,i)=>Number($("mindset10_"+i)?.value||0));if(scores.some(v=>v>0)){state.mindset10=scores.map((score,i)=>({score,note:$("mindset10_note_"+i)?.value||""}));state.mindset10History=state.mindset10History||{};state.mindset10History[localDateKey()]=scores;}localStorage.setItem(KEY,JSON.stringify(state));render();toast("已保存")}
function read(){["dailyGoal","fwWrite","fwShoot","fwPublish","fwConnect","fwNeed","fwIncome","fwReview","yearGoal","monthGoal","weekGoal","energyRaw","externalEnergyScore","morningMoodScore","nightMoodScore","resistanceNote","mindsetAction","mindsetActionResult","rescueResult","affirmationHelp","affirmationFeeling","effectiveMindsetAction","tomorrowMindsetAdjust","mindsetTomorrowMode","workTopic","workOutput","workBlock","fixedTime","timeReview","actionLog","contentAction","customerAction","liveAction","learned","learnApply","learnResult","roleModel","roleAction","reviewDone","reviewInsight","tomorrowGrowth","tomorrow0","tomorrow1","tomorrow2"].forEach(id=>{if($(id))state[id]=$(id).value});state.energy=state.energy||{};state.energy.history=state.energy.history||state.energyHistory;state.mindset=state.mindset||{};["externalEnergyScore","morningMoodScore","nightMoodScore","resistanceNote","mindsetAction","mindsetActionResult","affirmationFeeling","effectiveMindsetAction","tomorrowMindsetAdjust","mindsetTomorrowMode"].forEach(id=>{if($(id))state.mindset[id]=$(id).value});state.mindset.resistances=[...document.querySelectorAll("[data-resistance]:checked")].map(x=>x.value)}
function fill(){state.flywheel=state.flywheel||{};["dailyGoal","fwWrite","fwShoot","fwPublish","fwConnect","fwNeed","fwIncome","fwReview","yearGoal","monthGoal","weekGoal","energyRaw","externalEnergyScore","morningMoodScore","nightMoodScore","resistanceNote","mindsetAction","mindsetActionResult","rescueResult","affirmationHelp","affirmationFeeling","effectiveMindsetAction","tomorrowMindsetAdjust","mindsetTomorrowMode","workTopic","workOutput","workBlock","fixedTime","timeReview","actionLog","contentAction","customerAction","liveAction","learned","learnApply","learnResult","roleModel","roleAction","reviewDone","reviewInsight","tomorrowGrowth","tomorrow0","tomorrow1","tomorrow2"].forEach(id=>{if($(id)&&state[id]!=null)$(id).value=state[id]});}
function saveGoalPlan(){read();const gp=state.goalPlan;["monthKeyword","monthProject","weekBlock","weekCustomerFocus","weekLearningFocus","weekBigThree","aiGoalSuggestion","aiGoalStatus"].forEach(id=>{if($(id))gp[id]=$(id).value});if($("goalDailyGoal"))state.dailyGoal=$("goalDailyGoal").value;[["p1199","product1199Deals","product1199Income"],["p14999","product14999Deals","product14999Income"],["p49800","product49800Deals","product49800Income"]].forEach(x=>{gp.products[x[0]].deals=Number($(x[1])?.value)||0;gp.products[x[0]].income=Number($(x[2])?.value)||0});document.querySelectorAll("[data-goal-metric]").forEach(el=>{const id=el.dataset.goalMetric,kind=el.dataset.kind;state.goalMetrics[id][kind]=kind==="note"?el.value:Number(el.value)||0});localStorage.setItem(KEY,JSON.stringify(state));render();toast("完整目标管理已保存")}
function confirmAIGoal(){const text=$("aiGoalSuggestion").value.trim();if(!text){toast("请先填写AI目标建议");return}if($("aiGoalStatus").value!=="已确认，可进入今日目标"){toast("请先把状态改为已确认");return}state.dailyGoal=text;$("goalDailyGoal").value=text;if($("dailyGoal"))$("dailyGoal").value=text;saveGoalPlan()}
function renderGoals(){const gp=state.goalPlan,products=gp.products,total=Number(gp.yearBaseActual||0)+Object.values(products).reduce((s,x)=>s+(Number(x.income)||0),0),gap=Math.max(0,gp.yearTarget-total),rate=gp.yearTarget?Math.min(100,total/gp.yearTarget*100):0;$("yearActualView").textContent="¥"+total.toLocaleString("zh-CN",{minimumFractionDigits:2,maximumFractionDigits:2});$("yearGapView").textContent="¥"+gap.toLocaleString("zh-CN",{minimumFractionDigits:2,maximumFractionDigits:2});$("yearRateView").textContent=rate.toFixed(1)+"%";$("yearProgress").style.width=rate+"%";[["product1199Deals",products.p1199.deals],["product1199Income",products.p1199.income],["product14999Deals",products.p14999.deals],["product14999Income",products.p14999.income],["product49800Deals",products.p49800.deals],["product49800Income",products.p49800.income]].forEach(x=>{if($(x[0]))$(x[0]).value=x[1]||0});["monthKeyword","monthProject","weekBlock","weekCustomerFocus","weekLearningFocus","weekBigThree","aiGoalSuggestion","aiGoalStatus"].forEach(id=>{if($(id))$(id).value=gp[id]||""});if($("goalDailyGoal"))$("goalDailyGoal").value=state.dailyGoal||"";const defs=[["exposure","内容曝光","次"],["leads","有效线索","人"],["consult","有效咨询","人"],["deepTalk","深度沟通","人"],["highTicket","高阶成交","人"],["video","视频号","条"],["xhs","小红书","篇"],["mp","公众号","篇"],["moments","朋友圈","条"],["live","直播","场"],["learning","学习卡","张"]];const rows=defs.map(d=>{const x=state.goalMetrics[d[0]],goal=Number(x.goal)||0,current=Number(x.current)||0,pct=goal?Math.min(100,Math.round(current/goal*100)):0,diff=Math.max(0,goal-current);return `<tr><td><b>${d[1]}</b><small style="color:var(--muted)">（${d[2]}）</small></td><td><input data-goal-metric="${d[0]}" data-kind="goal" type="number" value="${goal}"></td><td><input data-goal-metric="${d[0]}" data-kind="current" type="number" value="${current}"></td><td><span class="pill">${pct}%</span></td><td>${diff}</td><td><input class="note-input" data-goal-metric="${d[0]}" data-kind="note" value="${esc(x.note||"")}"></td></tr>`}).join("");$("targetTable").innerHTML=`<div style="overflow:auto"><table class="goal-table"><thead><tr><th>指标</th><th>计划值</th><th>实际值</th><th>完成率</th><th>差额</th><th>复盘/依据</th></tr></thead><tbody>${rows}</tbody></table></div>`}function render(){renderMonthSummary();fill();state.deepStart="2023-10-15";state.parentStart="2026-03-01";$("gaokao").textContent=countdownFrom("2028-06-07")+" 天";$("deepDays").textContent=daysFrom(state.deepStart)+" 天";$("陪读天数").textContent=daysFrom(state.parentStart)+" 天";const d=new Date();$("today").textContent=d.toLocaleDateString("zh-CN",{year:"numeric",month:"2-digit",day:"2-digit",weekday:"short"});renderTasks();renderSlots();renderEnergy();renderMindset();renderTrend();renderGoals();$("homeMood").textContent=({10:"满血",8:"充足",6:"还行",4:"疲惫",2:"低谷"}[state.energy?.score]||"未选择");$("homeGoal").textContent=state.dailyGoal||"未填写";$("homeLearn").textContent=state.learningTrack||"未选择";$("homeDone").textContent=`${state.tasks.filter(x=>x.done).length} / ${state.tasks.filter(x=>String(x.text||"").trim()).length}`;$("learnSummary").textContent=state.learned?state.learned.slice(0,80):"还没有选择今天的学习主线。";$("mindsetSummary").textContent=state.energy?.theme?`主题：${state.energy.theme}｜能量：${state.energy.score||"未评"}｜${state.energy.reminder||"已保存今日完整文案"}`:"请在心态管理中粘贴今日能量急救包。";}
function countdownFrom(date){const today=new Date(new Date().toDateString());const target=new Date(date+"T00:00:00");return Math.max(0,Math.ceil((target-today)/86400000))}
function daysFrom(date){const today=new Date(new Date().toDateString());const start=new Date(date+"T00:00:00");const n=Math.floor((today-start)/86400000);return Math.max(0,n)}
function importTaskMarkdown(event){const file=event.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const lines=String(reader.result).split(/\r?\n/).filter(x=>/^\s*[-*]\s*\[[ xX]?\]/.test(x)||/^\s*[-*]\s+/.test(x));const tasks=lines.filter(x=>/^\s*[-*]\s*\[[ xX]?\]/.test(x)).map(x=>x.replace(/^\s*[-*]\s*\[[ xX]?\]\s*/,'').trim()).filter(Boolean);if(tasks.length){state.tasks=tasks.map(text=>({text,done:false}));localStorage.setItem(KEY,JSON.stringify(state));render();toast("已导入今日工作清单")}else toast("未识别到任务，请使用Markdown列表或任务框");};reader.readAsText(file,"utf-8");}
let obsidianVaultHandle=null;async function connectObsidianVault(){if(!window.showDirectoryPicker){toast('当前浏览器不支持直接连接Vault，请使用Chrome/Edge');return}try{obsidianVaultHandle=await window.showDirectoryPicker();if($('syncStatus'))$('syncStatus').textContent='Vault已连接';await syncFromObsidianVault();}catch(e){toast('已取消连接')}}async function syncFromObsidianVault(){if(!obsidianVaultHandle)return;try{const fileHandle=await obsidianVaultHandle.getFileHandle('今日工作清单.md');const file=await fileHandle.getFile();const raw=await file.text();const tasks=raw.split(/\r?\n/).filter(x=>/^\s*[-*]\s*\[[ xX]?\]/.test(x)).map(x=>x.replace(/^\s*[-*]\s*\[[ xX]?\]\s*/,'').trim()).filter(Boolean);if(tasks.length){state.tasks=tasks.map(text=>({text,done:false}));localStorage.setItem(KEY,JSON.stringify(state));render();toast('已从Obsidian同步今日清单')}}catch(e){toast('Vault内未找到今日工作清单.md')}}async function writeExecutionToObsidian(){if(!obsidianVaultHandle){toast('请先连接Obsidian Vault');return}read();const d=localDateKey();const h=await obsidianVaultHandle.getFileHandle('执行记录-'+d+'.md',{create:true});const w=await h.createWritable();await w.write(exportExecutionText());await w.close();toast('执行数据已写回Obsidian')}function exportExecutionText(){return '# 今日执行记录｜'+localDateKey()+'\n\n## 今日任务\n'+state.tasks.map(t=>'- ['+(t.done?'x':' ')+'] '+t.text).join('\n')+'\n\n## 心态状态\n- 能量：'+(state.energy?.score||'未记录')+'\n';}
function syncObsidianFile(event){const file=event.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const raw=String(reader.result);let tasks=[];if(file.name.endsWith('.json')){try{const d=JSON.parse(raw);if(d.dailyGoal)state.dailyGoal=d.dailyGoal;tasks=(d.tasks||[]).map(x=>typeof x==='string'?x:x.title||x.text||'');}catch(e){}}else{tasks=raw.split(/\r?\n/).filter(x=>/^\s*[-*]\s*\[[ xX]?\]/.test(x)||/^\s*[-*]\s+/.test(x)).map(x=>x.replace(/^\s*[-*]\s*\[[ xX]?\]\s*/,'').trim()).filter(Boolean);}if(tasks.length){state.tasks=tasks.map(text=>({text,done:false}));}localStorage.setItem(KEY,JSON.stringify(state));render();if($('syncStatus'))$('syncStatus').textContent='已同步 '+new Date().toLocaleTimeString();toast('Obsidian内容已同步');};reader.readAsText(file,'utf-8');}
function renderTasks(){const labels=["P0","P1","P2"];$("tasks").innerHTML=state.tasks.map((t,i)=>`<div class="task ${t.done?"done":""}"><span class="tag">${labels[i]}</span><input class="task-text" value="${esc(t.text)}" placeholder="${["今天必须完成的结果","完成后明显推进的事","有余力再做的事"][i]}" data-task="${i}"><input class="check" type="checkbox" ${t.done?"checked":""} data-check="${i}"></div>`).join("");document.querySelectorAll("[data-task]").forEach(x=>x.oninput=e=>state.tasks[+e.target.dataset.task].text=e.target.value);document.querySelectorAll("[data-check]").forEach(x=>x.onchange=e=>{state.tasks[+e.target.dataset.check].done=e.target.checked;save()})}
function renderSlots(){const opts=[["content","内容生产"],["customer","客户深聊"],["live","直播/发布"],["moments","朋友圈"],["learning","学习"],["review","复盘"],["family","陪读/家庭"]];const optHtml=(v)=>opts.map(o=>`<option value="${o[0]}" ${v===o[0]?"selected":""}>${o[1]}</option>`).join("");$("slots").innerHTML=state.slots.map((s,i)=>`<div class="slot-extended"><div><b>${esc(s.time||"未排时间")}</b><small>时间块${i+1}</small></div><select data-slottarget="${i}">${optHtml(s.target||"content")}</select><input value="${esc(s.text)}" placeholder="要完成的动作" data-slot="${i}"><input type="number" min="0" value="${Number(s.minutes)||0}" placeholder="计划分" data-slotminutes="${i}"><input type="number" min="0" value="${Number(s.actualMinutes)||0}" placeholder="实际分" data-slotactual="${i}"><button class="btn ${s.done?"success":"soft"}" data-slotdone="${i}">${s.done?"已完成":"完成"}</button></div>`).join("");let plan=0,actual=0,done=0;state.slots.forEach(s=>{plan+=Number(s.minutes)||0;actual+=Number(s.actualMinutes)||0;if(s.done)done++});$("timePlanView").textContent=plan+"分钟";$("timeActualView").textContent=actual+"分钟";$("timeDoneView").textContent=done+" / "+state.slots.length;$("timeRateView").textContent=plan?Math.min(100,Math.round(actual/plan*100))+"%":"0%";$("timeGoalMirror").textContent=state.dailyGoal||"未填写";$("timeTasksMirror").innerHTML=state.tasks.map((t,i)=>`${i+1}. ${esc(t.text||"未填写")}（${t.done?"完成":"待做"}）`).join("<br>");document.querySelectorAll("[data-slot]").forEach(x=>x.oninput=e=>state.slots[+e.target.dataset.slot].text=e.target.value);document.querySelectorAll("[data-slottarget]").forEach(x=>x.onchange=e=>state.slots[+e.target.dataset.slottarget].target=e.target.value);document.querySelectorAll("[data-slotminutes]").forEach(x=>x.oninput=e=>state.slots[+e.target.dataset.slotminutes].minutes=Number(e.target.value)||0);document.querySelectorAll("[data-slotactual]").forEach(x=>x.oninput=e=>state.slots[+e.target.dataset.slotactual].actualMinutes=Number(e.target.value)||0);document.querySelectorAll("[data-slotdone]").forEach(x=>x.onclick=e=>{state.slots[+e.target.dataset.slotdone].done=!state.slots[+e.target.dataset.slotdone].done;save()})}
function createSuggestedSlots(){state.slots=[{time:"09:00–10:30",target:"content",text:state.tasks[0]?.text||"完成P0内容动作",minutes:90,actualMinutes:0,done:false},{time:"14:00–15:00",target:"customer",text:state.tasks[1]?.text||"完成P1客户/转化动作",minutes:60,actualMinutes:0,done:false},{time:"20:30–21:00",target:"review",text:state.tasks[2]?.text||"完成P2学习或复盘动作",minutes:30,actualMinutes:0,done:false}];renderSlots();toast("已按今日三件事生成时间块")}
function addSlot(){state.slots.push({time:"",text:"",done:false});renderSlots()}
function renderEnergy(){const vals=[["🔥","满血",10],["💪","充足",8],["😐","还行",6],["😩","疲惫",4],["💔","低谷",2]];$("energyButtons").innerHTML=vals.map(v=>`<button class="${state.energy.score===v[2]?"selected":""}" data-energy="${v[2]}">${v[0]} ${v[1]} ${v[2]}</button>`).join("");document.querySelectorAll("[data-energy]").forEach(x=>x.onclick=e=>{state.energy.score=+e.target.dataset.energy;state.energyHistory.push({date:localDateKey(),score:state.energy.score});save()})}
function parseEnergy(){read();const raw=state.energyRaw||"";const line=(label)=>{const hit=raw.split(/\r?\n/).find(x=>x.includes(label));return hit?hit.split(/[｜|：:]/).slice(1).join("｜").trim():""};const theme=line("今日日签指引");const tarot=line("塔罗能量");const score=(raw.match(/总能量[^0-9]*([0-9]{1,3})/)||[])[1]||"";const lucky=(raw.match(/幸运加持[:：]?([^\n\r]+)/)||[])[1]||"";const color=(lucky.match(/幸运色([^｜|]+)/)||[])[1]?.trim()||"";const crystal=(lucky.match(/幸运水晶([^｜|]+)/)||[])[1]?.trim()||"";const number=(lucky.match(/幸运数字([^｜|]+)/)||[])[1]?.trim()||"";state.energy={...state.energy,raw,score:score||state.energy.score,theme,tarot,color,crystal,number,reminder:tarot?`塔罗提醒：${tarot}`:""};localStorage.setItem(KEY,JSON.stringify(state));$("energyParsed").textContent=`主题：${theme||"未识别"}｜塔罗：${tarot||"未识别"}｜总能量：${score||"未识别"}｜幸运色：${color||"未识别"}｜水晶：${crystal||"未识别"}｜数字：${number||"未识别"}`;render()}
function calcMindset10(){let total=0;const scores=[];for(let i=0;i<10;i++){const v=Number($("mindset10_"+i)?.value||0);const score=Math.max(0,Math.min(10,v));total+=score;scores.push({score,note:$("mindset10_note_"+i)?.value||""});}$("mindsetTotal").textContent=total;state.mindset10=scores;localStorage.setItem(KEY,JSON.stringify(state));}
function recordMindsetTotal(){calcMindset10();const total=Number($("mindsetTotal")?.textContent||0);state.mindsetTotalHistory=state.mindsetTotalHistory||{};state.mindsetTotalHistory[localDateKey()]=total;localStorage.setItem(KEY,JSON.stringify(state));renderMonthSummary();toast("已记录今日总分："+total)}
function renderMindset(){const m=state.mindset||{};const s10=state.mindset10||[];s10.forEach((x,i)=>{if($("mindset10_"+i))$("mindset10_"+i).value=x.score||"";if($("mindset10_note_"+i))$("mindset10_note_"+i).value=x.note||""});if($("mindsetTotal")){let t=s10.reduce((a,x)=>a+Number(x.score||0),0);$("mindsetTotal").textContent=t||0;}["externalEnergyScore","morningMoodScore","nightMoodScore","resistanceNote","mindsetAction","mindsetActionResult","affirmationFeeling","effectiveMindsetAction","tomorrowMindsetAdjust","mindsetTomorrowMode"].forEach(id=>{if($(id)&&m[id]!=null)$(id).value=m[id]});document.querySelectorAll("[data-resistance]").forEach(x=>x.checked=(m.resistances||[]).includes(x.value));if($("externalEnergyScore")&&!$("externalEnergyScore").value&&state.energy?.score)$("externalEnergyScore").value=state.energy.score}function renderTrend(){const h=(state.energyHistory||[]).slice(-7);$("trend").innerHTML=(h.length?h:[{score:0,date:""}]).map(x=>`<div class="bar" style="height:${Math.max(8,(x.score||0)*9)}px"><b>${x.score||"-"}</b><span>${x.date?x.date.slice(5):"--"}</span></div>`).join("")}
function renderMonthSummary(){const hist=state.mindsetTotalHistory||{};Object.keys(state.energyHistory||{}).forEach(k=>{});if(!hist[localDateKey()]&&state.mindset10?.length){const todayTotal=state.mindset10.reduce((a,x)=>a+Number(x.score||0),0);if(todayTotal>0){hist[localDateKey()]=todayTotal;state.mindsetTotalHistory=hist;localStorage.setItem(KEY,JSON.stringify(state));}}const now=new Date();const year=now.getFullYear();const sel=$("chartMonth");if(sel&&!sel.options.length){for(let m=1;m<=12;m++){const o=document.createElement("option");o.value=String(m).padStart(2,"0");o.textContent=year+"年"+m+"月";if(m===now.getMonth()+1)o.selected=true;sel.appendChild(o);}}const month=sel?.value||String(now.getMonth()+1).padStart(2,"0");const svg=$("monthMindsetChart");if(!svg)return;const W=1120,H=480,padL=48,padR=24,padT=48,padB=52;let out=`<text x="${W/2-100}" y="24" font-size="22" fill="#3f3027">${year}年${Number(month)}月心态曲线</text>`;for(let k=0;k<=100;k+=10){const yy=H-padB-(k/100)*(H-padT-padB);out+=`<line x1="${padL}" y1="${yy}" x2="${W-padR}" y2="${yy}" stroke="#8d8177"/><text x="12" y="${yy+4}" font-size="11">${k}</text>`;}const days=new Date(year,Number(month),0).getDate();for(let d=1;d<=days;d++){const xx=padL+(d-1)*(W-padL-padR)/(days-1);out+=`<line x1="${xx}" y1="${padT}" x2="${xx}" y2="${H-padB}" stroke="#b8aaa0" stroke-width="0.7"/><text x="${xx-4}" y="${H-20}" font-size="10">${d}</text>`;}const pts=[];for(let d=1;d<=days;d++){const key=year+"-"+month+"-"+String(d).padStart(2,"0");if(hist[key]!=null){const xx=padL+(d-1)*(W-padL-padR)/(days-1),score=Number(hist[key]||0),yy=H-padB-(score/100)*(H-padT-padB);pts.push({xx,yy,score,d});}}if(pts.length){out+=`<polyline points="${pts.map(p=>p.xx+","+p.yy).join(" ")}" fill="none" stroke="#c47f4f" stroke-width="3"/>`;pts.forEach(p=>out+=`<circle cx="${p.xx}" cy="${p.yy}" r="5" fill="#c47f4f"><title>${p.d}日：${p.score}分</title></circle>`)}svg.innerHTML=out;}
function newAffirmation(){$("affirmation").textContent=affirmations[Math.floor(Math.random()*affirmations.length)]}
function exportMarkdown(){
  read();
  const date=localDateKey(),items=state.timeItems||state.slots||[],completed=items.filter(x=>x.done),unfinished=items.filter(x=>!x.done),m=state.mindset||{};
  const resultLines=completed.length?completed.map((x,i)=>'- '+(i+1)+'. '+(x.text||'未命名事项')+'｜'+(x.category||'未分类')+'｜计划'+(x.plannedMinutes||0)+'分钟｜实际'+(x.actualMinutes||0)+'分钟｜结果：已完成').join('\n'):'- 今日尚未记录已完成事项';
  const pendingLines=unfinished.length?unfinished.map(x=>'- '+(x.text||'未命名事项')+'｜'+(x.skipped?'明确未完成':'待确认')).join('\n'):'- 无';
  const md='# 梅姐飞轮每日记录｜'+date
    +'\n\n## 一、今日核心目标\n\n'+(state.dailyGoal||'未记录')
    +'\n\n## 二、围绕目标完成的关键行动\n\n'+resultLines
    +'\n\n## 三、时效与完成证据\n\n- 今日计划事项：'+items.length+'项\n- 已完成：'+completed.length+'项\n- 未完成／待确认：'+unfinished.length+'项\n- 内容与发布：'+(state.contentAction||'未记录')+'\n- 客户跟进：'+(state.customerAction||'未记录')+'\n- 直播记录：'+(state.liveAction||'未记录')
    +'\n\n### 未完成事项\n'+pendingLines
    +'\n\n## 四、主动承担与团队贡献\n\n- 岗位／工作记录：'+(state.actionLog||'未记录')+'\n- 帮扶、协作或团队贡献：'+(state.teamContribution||'未记录；没有发生请如实写0次')
    +'\n\n## 五、困难、阻力与调整\n\n- 早晨状态：'+(m.morningMoodScore||'未记录')+'\n- 晚上状态：'+(m.nightMoodScore||'未记录')+'\n- 心理阻力：'+((m.resistances||[]).join('、')||'未记录')+'\n- 阻力补充：'+(m.resistanceNote||state.resistanceNote||'未记录')+'\n- 调整动作：'+(m.mindsetAction||state.mindsetAction||'未记录')+'\n- 调整结果：'+(m.mindsetActionResult||state.mindsetActionResult||'未记录')
    +'\n\n## 六、今日学习与成长\n\n- 学习主线：'+(state.learningTrack||'未记录')+'\n- 学习内容：'+(state.learned||'未记录')+'\n- 应用动作：'+(state.learnApply||'未记录')+'\n- 应用结果：'+(state.learnResult||'未记录')
    +'\n\n## 七、今日复盘\n\n- 今日主要成果：'+(state.reviewDone||'未记录')+'\n- 今日洞察：'+(state.reviewInsight||'未记录')+'\n- 有效做法：'+(state.effectiveMindsetAction||'未记录')+'\n- 明日需要调整：'+(state.tomorrowMindsetAdjust||state.tomorrowGrowth||'未记录')
    +'\n\n## 八、明日核心目标与行动\n\n- 明日核心目标：'+(state.tomorrowGrowth||'未记录')+'\n- P0：'+(state.tomorrow0||'未记录')+'\n- P1：'+(state.tomorrow1||'未记录')+'\n- P2：'+(state.tomorrow2||'未记录')
    +'\n\n> 记录原则：只写当天真实发生的事实；说明目标、行动、完成时间、结果与数量。没有发生的事项如实记0，不为评分补写不存在的行为。';
  $('output').textContent=md;state.lastMarkdown=md;localStorage.setItem(KEY,JSON.stringify(state));
}
function copyMarkdown(){if(!state.lastMarkdown)exportMarkdown();navigator.clipboard?.writeText(state.lastMarkdown||"").then(()=>toast("Markdown已复制"))}
function downloadJSON(){read();const blob=new Blob([JSON.stringify({...state,exportedAt:new Date().toISOString()},null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`梅姐飞轮备份_${localDateKey()}.json`;a.click();URL.revokeObjectURL(a.href);toast("JSON备份已导出")}
function esc(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}function toast(t){const n=document.createElement("div");n.textContent=t;n.style="position:fixed;bottom:24px;right:24px;background:#263238;color:#fff;padding:12px 16px;border-radius:10px;z-index:20";document.body.appendChild(n);setTimeout(()=>n.remove(),1600)}
document.querySelectorAll(".nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));$(b.dataset.page).classList.add("active")});document.querySelectorAll("[data-jump]").forEach(b=>b.onclick=()=>document.querySelector(`button[data-page="${b.dataset.jump}"]`).click());document.querySelectorAll("#learningTracks").forEach(c=>{["驿马课程","自媒体创业","AI板块","榜样与深耕"].forEach(x=>{const b=document.createElement("button");b.className="btn "+(state.learningTrack===x?"accent":"soft");b.textContent=x;b.onclick=()=>{state.learningTrack=x;render();save()};c.appendChild(b)})});newAffirmation();render();
async function confirmIncome(record){
  const confirmation='梅姐已确认：'+record;
  localStorage.setItem('mei-income-confirmation',confirmation);
  if(!obsidianVaultHandle){toast('金额已确认；连接 Obsidian 后将写回明细');return}
  try{
    const dir=await obsidianVaultHandle.getDirectoryHandle('40_数据与复盘');
    const handle=await dir.getFileHandle('成交与到账明细_2026.md');
    const file=await handle.getFile();
    const writer=await handle.createWritable();
    await writer.write((await file.text())+'\n- '+confirmation+'\n');
    await writer.close();toast('金额已确认并写回 Obsidian');
  }catch(e){toast('金额已确认；Obsidian 写回待连接后完成')}
}
function renderGoals(){
  const gp=state.goalPlan, products=gp.products;
  const total=Number(gp.yearBaseActual||0)+Object.values(products).reduce((sum,item)=>sum+(Number(item.income)||0),0);
  const gap=Math.max(0,gp.yearTarget-total), rate=gp.yearTarget?Math.min(100,total/gp.yearTarget*100):0;
  $("yearActualView").textContent="¥"+total.toLocaleString("zh-CN",{minimumFractionDigits:2,maximumFractionDigits:2});
  $("yearGapView").textContent="¥"+gap.toLocaleString("zh-CN",{minimumFractionDigits:2,maximumFractionDigits:2});
  $("yearRateView").textContent=rate.toFixed(1)+"%"; $("yearProgress").style.width=rate+"%";
  [["monthGoalView",state.monthGoal],["weekGoalView",state.weekGoal],["goalDailyGoalView",state.dailyGoal]].forEach(([id,value])=>{if($(id))$(id).textContent=value||"知识库待同步"});
  if($("goalDailyGoalTop"))$("goalDailyGoalTop").textContent=state.dailyGoal||"昨晚复盘尚未生成今日目标。";
  const todayGoalDetail=$("todayGoalDetail"),todayActions=typeof currentDayActionPlan==='function'?currentDayActionPlan():[];if(todayGoalDetail)todayGoalDetail.innerHTML=todayActions.length?todayActions.map((item,index)=>`<div class="goal-step"><b>目标${index+1}</b>${esc(item.text)}</div>`).join(''):'<div class="notice">昨晚复盘尚未生成今日行动。</div>';
  const currentDate=localDateKey(),bigThree=(currentDate==='2026-07-26'?todayActions:todayActions.filter(item=>item.category!=='会议')).slice(0,3),bigThreeView=$("todayBigThreeView"),bigThreeChecks=state.todayBigThreeChecks?.[currentDate]||[];if(bigThreeView)bigThreeView.innerHTML=bigThree.length?bigThree.map((item,index)=>`<label class="goal-step" style="display:flex;align-items:center;gap:16px;cursor:pointer;text-align:left"><input type="checkbox" style="width:24px;height:24px;flex:0 0 auto;accent-color:#bd7c58" ${bigThreeChecks[index]?'checked':''} onchange="toggleTodayBigThree(${index},this.checked)"><span><b style="display:block">重要${index+1}</b>${esc(item.text)}</span></label>`).join(''):'<div class="notice">今日重要三件事尚未生成。</div>';
  const defs=[["exposure","内容曝光","次"],["leads","有效线索","人"],["consult","有效咨询","人"],["deepTalk","深度沟通","人"],["highTicket","高阶成交","人"],["video","视频号","条"],["xhs","小红书","篇"],["mp","公众号","篇"],["moments","朋友圈","条"],["live","直播","场"],["learning","学习卡","张"]];
  const rows=defs.map(([id,label,unit])=>{const item=state.goalMetrics[id],goal=Number(item.goal)||0,current=Number(item.current)||0,pct=goal?Math.min(100,Math.round(current/goal*100)):0;return `<tr><td><b>${label}</b><small style="color:var(--muted)">（${unit}）</small></td><td>${goal}</td><td>${current}</td><td><span class="pill">${pct}%</span></td><td>${Math.max(0,goal-current)}</td></tr>`}).join("");
  $("targetTable").innerHTML=`<div class="notice">本月执行结果由后台报表写入 Obsidian 后自动同步；本页只读查看。</div><div style="overflow:auto"><table class="goal-table"><thead><tr><th>指标</th><th>知识库计划</th><th>后台实际</th><th>完成率</th><th>差额</th></tr></thead><tbody>${rows}</tbody></table></div>`;
}
function timeCategory(text){if(/客户|沟通|咨询|跟进/.test(text))return '客户沟通';if(/学习|课程|学习卡/.test(text))return '学习应用';if(/发布|直播/.test(text))return '内容发布／直播';if(/陪读|家庭/.test(text))return '家庭陪读';return '内容生产'}
function ensureTimeItems(){if(!Array.isArray(state.timeItems)||state.timeItems.length!==state.tasks.length){state.timeItems=state.tasks.map((task,index)=>({text:task.text||'未同步任务',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false}))}}
function renderSlots(){ensureTimeItems();const items=state.timeItems, actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0), done=items.filter(item=>item.done).length;$("timeActualView").textContent=actual+'分钟';$("timeDoneView").textContent=done+' / '+items.length;$("timeRateView").textContent=Math.min(100,Math.round(actual/720*100))+'%';$("slots").innerHTML=items.map((item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜建议${item.plannedMinutes}分钟｜实际${item.actualMinutes||0}分钟｜${item.skipped?'未完成':item.done?'已完成':'待执行'}</small></div><div class="row">${item.skipped?'<span class="pill">未完成</span>':`<button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'已完成':item.startedAt?'完成':'开始'}</button><button class="btn ghost" type="button" onclick="markTimeUnfinished(${index})">未完成</button>`}</div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$("timeQuadrants").innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');const categories=['内容生产','内容发布／直播','客户沟通','学习应用','家庭陪读','休息恢复'];$("timePie").innerHTML=categories.map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('')}
function toggleTimeItem(index){ensureTimeItems();const item=state.timeItems[index];if(item.done){item.done=false;item.actualMinutes=0;item.startedAt=null;toast('已取消勾选')}else{item.done=true;item.skipped=false;item.startedAt=null;item.actualMinutes=Number(item.plannedMinutes)||0;toast('已勾选完成')}localStorage.setItem(KEY,JSON.stringify(state));render()}
function markTimeUnfinished(index){ensureTimeItems();const item=state.timeItems[index];item.startedAt=null;item.actualMinutes=0;item.skipped=true;localStorage.setItem(KEY,JSON.stringify(state));render();toast('已记录未完成')}
async function confirmTimeResult(){ensureTimeItems();const rows=state.timeItems.map(item=>`- ${item.text}｜${item.quadrant}｜${item.category}｜实际${item.actualMinutes||0}分钟｜${item.done?'已完成':'未完成'}`).join('\n');const content=`\n## 今日时间结果\n${rows}\n`;
  try{if(!obsidianVaultHandle){if(!window.showDirectoryPicker){toast('请用支持连接 Obsidian 的浏览器');return}obsidianVaultHandle=await window.showDirectoryPicker()}const dataDir=await obsidianVaultHandle.getDirectoryHandle('40_数据与复盘');const logDir=await dataDir.getDirectoryHandle('每日日志');const date=localDateKey().replace(/-/g,'');const handle=await logDir.getFileHandle('每日日志_'+date+'.md',{create:true});const file=await handle.getFile();const writer=await handle.createWritable();await writer.write((await file.text())+content);await writer.close();localStorage.setItem(KEY,JSON.stringify(state));toast('今日时间结果已写回 Obsidian')}catch(e){toast('已保存本页；连接 Obsidian 后可写回')}
}
function timeCategory(text){if(/签到|作业|积分|报督导|报数|组委/.test(text))return '组长运营';if(/帮扶|个案|组员|私聊/.test(text))return '学员帮扶与实战';if(/客户|咨询|邀约|微信|成交/.test(text))return '客户触达与成交';if(/发布|视频号|小红书|公众号|朋友圈|内容/.test(text))return '内容生产与发布';if(/工具|SOP|表格|知识库/.test(text))return '工具与系统';if(/学习|课程|学习卡/.test(text))return '学习应用';return '复盘、机动与临时事项'}
function timeCategories(){return ['组长运营','学员帮扶与实战','客户触达与成交','内容生产与发布','工具与系统','学习应用','复盘、机动与临时事项']}
function ensureTimeItems(){if(!Array.isArray(state.timeItems)||state.timeItems.length!==state.tasks.length){state.timeItems=state.tasks.map((task,index)=>({text:task.text||'未同步任务',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false,skipped:false}))}}
function renderTimeSummary(){const history=Array.isArray(state.timeHistory)?state.timeHistory:[],now=new Date(),weekStart=new Date(now);weekStart.setDate(now.getDate()-((now.getDay()+6)%7));weekStart.setHours(0,0,0,0);const monthKey=now.toISOString().slice(0,7),week=history.filter(x=>new Date(x.date)>=weekStart),month=history.filter(x=>x.date.slice(0,7)===monthKey);const total=records=>records.reduce((sum,r)=>sum+r.items.reduce((s,i)=>s+(Number(i.actualMinutes)||0),0),0);$("timeSummaryTable").innerHTML=`<div class="goal-summary"><div class="kpi"><span>本周实际时间</span><b>${total(week)}分钟</b></div><div class="kpi"><span>本周已确认天数</span><b>${week.length}天</b></div><div class="kpi"><span>本月实际时间</span><b>${total(month)}分钟</b></div><div class="kpi"><span>本月已确认天数</span><b>${month.length}天</b></div></div>`}
function renderSlots(){ensureTimeItems();const items=state.timeItems,actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length;$("timeActualView").textContent=actual+'分钟';$("timeDoneView").textContent=done+' / '+items.length;$("timeRateView").textContent=Math.min(100,Math.round(actual/720*100))+'%';$("slots").innerHTML=items.map((item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜建议${item.plannedMinutes}分钟｜实际${item.actualMinutes||0}分钟｜${item.skipped?'未完成':item.done?'已完成':'待执行'}</small></div><div class="row">${item.skipped?'<span class="pill">未完成</span>':`<button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'已完成':item.startedAt?'完成':'开始'}</button><button class="btn ghost" type="button" onclick="markTimeUnfinished(${index})">未完成</button>`}</div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$("timeQuadrants").innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$("timePie").innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary()}
async function confirmTimeResult(){ensureTimeItems();const date=localDateKey(),entry={date,items:state.timeItems.map(item=>({...item,startedAt:null}))};state.timeHistory=Array.isArray(state.timeHistory)?state.timeHistory:[];state.timeHistory=state.timeHistory.filter(item=>item.date!==date);state.timeHistory.push(entry);const rows=entry.items.map(item=>`- ${item.text}｜${item.quadrant}｜${item.category}｜实际${item.actualMinutes||0}分钟｜${item.done?'已完成':'未完成'}`).join('\n');const content=`\n## 今日时间结果\n${rows}\n`;try{if(!obsidianVaultHandle){if(!window.showDirectoryPicker){toast('请用支持连接 Obsidian 的浏览器');return}obsidianVaultHandle=await window.showDirectoryPicker()}const dataDir=await obsidianVaultHandle.getDirectoryHandle('40_数据与复盘');const logDir=await dataDir.getDirectoryHandle('每日日志');const handle=await logDir.getFileHandle('每日日志_'+date.replace(/-/g,'')+'.md',{create:true});const file=await handle.getFile();const writer=await handle.createWritable();await writer.write((await file.text())+content);await writer.close();localStorage.setItem(KEY,JSON.stringify(state));render();toast('今日时间结果已写回 Obsidian')}catch(e){localStorage.setItem(KEY,JSON.stringify(state));render();toast('时间结果已保存，连接 Obsidian 后可写回')}}
function defaultAwakeningTimeItems(){return[
  {text:'08:15 群内提醒签到与当天上课',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:15},
  {text:'08:35 确认签到，私聊未签到成员',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'09:30 布置当天作业要求',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:20},
  {text:'白天 盯作业、点评、提醒掉队成员',quadrant:'紧急且重要',category:'学员帮扶与实战',plannedMinutes:90},
  {text:'白天 完成个案、客户触达或私聊跟进',quadrant:'重要不紧急',category:'客户触达与成交',plannedMinutes:120},
  {text:'白天 发布成长内容',quadrant:'重要不紧急',category:'内容生产与发布',plannedMinutes:120},
  {text:'白天 沉淀话术、SOP、表格或知识库资料',quadrant:'重要不紧急',category:'工具与系统',plannedMinutes:60},
  {text:'21:00 收组委报数、汇总积分',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'22:00 向督导报送签到、作业、积分与异常',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30}
].map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}))}
function ensureTimeItems(){if(state.timeSourceVersion!==3){state.timeItems=defaultAwakeningTimeItems();state.timeSourceVersion=3;localStorage.setItem(KEY,JSON.stringify(state))}}
function renderSlots(){ensureTimeItems();const items=state.timeItems,actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length;$("timeActualView").textContent=actual+'分钟';$("timeDoneView").textContent=done+' / '+items.length;$("timeRateView").textContent=Math.min(100,Math.round(actual/720*100))+'%';$("slots").innerHTML=items.map((item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜建议${item.plannedMinutes}分钟｜实际${item.actualMinutes||0}分钟｜${item.skipped?'未完成':item.done?'已完成':'待执行'}</small></div><div class="row">${item.skipped?`<button class="btn soft" type="button" onclick="restoreTimeItem(${index})">恢复待执行</button>`:`<button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'已完成':item.startedAt?'完成':'开始'}</button><button class="btn ghost" type="button" onclick="markTimeUnfinished(${index})">未完成</button>`}</div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$("timeQuadrants").innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$("timePie").innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary()}
function restoreTimeItem(index){ensureTimeItems();const item=state.timeItems[index];item.skipped=false;item.done=false;item.startedAt=null;item.actualMinutes=0;localStorage.setItem(KEY,JSON.stringify(state));render();toast('已恢复为待执行')}
function defaultAwakeningTimeItems(){const weekday=[
  {text:'06:00 生命觉醒小课',quadrant:'紧急且重要',category:'学习应用',plannedMinutes:60},
  {text:'08:00 觉醒计划课',quadrant:'紧急且重要',category:'学习应用',plannedMinutes:90},
  {text:'08:35 督促纪律委员完成签到与未签到跟进',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'09:30 督促学习委员布置作业并检查推进',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:20},
  {text:'10:00 辅导员定向会（当天课表有安排时）',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'11:00 分堂会／组员会议（有安排时）',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60,optional:true},
  {text:'辅导员组会（有安排时）',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60,optional:true},
  {text:'白天 督促、检查作业与掉队成员跟进结果',quadrant:'紧急且重要',category:'学员帮扶与实战',plannedMinutes:90},
  {text:'白天 完成个案、客户触达或私聊跟进',quadrant:'重要不紧急',category:'客户触达与成交',plannedMinutes:120},
  {text:'白天 发布成长内容',quadrant:'重要不紧急',category:'内容生产与发布',plannedMinutes:120},
  {text:'白天 沉淀话术、SOP、表格或知识库资料',quadrant:'重要不紧急',category:'工具与系统',plannedMinutes:60},
  {text:'21:00 收组委报数、汇总积分',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'22:00 向督导报送签到、作业、积分与异常',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30}
];return weekday.map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}))}
function ensureTimeItems(){const now=new Date(),today=now.getDay(),date=now.toISOString().slice(0,10);if(state.timeSourceVersion!==5){const weekendItems=state.tasks.map((task,index)=>({text:task.text||'今日待办',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false,skipped:false}));if(date==='2026-07-26')weekendItems.push({text:'19:30 辅导员组会',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60,actualMinutes:0,startedAt:null,done:false,skipped:false});state.timeItems=(today===0||today===6)?weekendItems:defaultAwakeningTimeItems();state.timeSourceVersion=5;localStorage.setItem(KEY,JSON.stringify(state))}}
function defaultAwakeningTimeItems(){return[
  {text:'06:00—07:00 生命觉醒小课',quadrant:'紧急且重要',category:'学习应用',plannedMinutes:60},
  {text:'08:00—09:30 觉醒计划课',quadrant:'紧急且重要',category:'学习应用',plannedMinutes:90},
  {text:'09:30—10:00 督促学习委员、检查作业布置',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'10:00—11:00 辅导员定向会',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'11:00—12:00 分堂会／组员会议',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'13:30—15:00 督促作业、点评与掉队成员跟进',quadrant:'紧急且重要',category:'学员帮扶与实战',plannedMinutes:90},
  {text:'15:00—16:30 个案、客户触达或私聊跟进',quadrant:'重要不紧急',category:'客户触达与成交',plannedMinutes:90},
  {text:'16:30—18:00 内容生产与发布',quadrant:'重要不紧急',category:'内容生产与发布',plannedMinutes:90},
  {text:'19:30—20:30 辅导员组会／幸福人生安排（当天有时）',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60,optional:true},
  {text:'20:30—21:30 收报数、汇总积分',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'21:30—22:00 向督导报送结果与异常',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'22:00—22:30 复盘、知识库更新与次日安排',quadrant:'重要不紧急',category:'工具与系统',plannedMinutes:30}
].map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}))}
function ensureTimeItems(){const now=new Date(),today=now.getDay(),date=now.toISOString().slice(0,10);if(state.timeSourceVersion!==6){const weekendItems=state.tasks.map((task,index)=>({text:task.text||'今日待办',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false,skipped:false}));if(date==='2026-07-26')weekendItems.push({text:'19:30左右 辅导员组会',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60,actualMinutes:0,startedAt:null,done:false,skipped:false});state.timeItems=(today===0||today===6)?weekendItems:defaultAwakeningTimeItems();state.timeSourceVersion=6;localStorage.setItem(KEY,JSON.stringify(state))}}
function timeCategory(text){if(/会|定向|分堂/.test(text))return '会议';if(/签到|作业|积分|报督导|报数|组委/.test(text))return '组长运营';if(/个案/.test(text))return '实战个案';if(/帮扶|组员|私聊|掉队/.test(text))return '学员帮扶';if(/客户|咨询|邀约|微信|成交/.test(text))return '客户触达与成交';if(/发布|视频号|小红书|公众号|朋友圈|内容/.test(text))return '内容生态发布';if(/SOP|知识库|流程|自动化|系统/.test(text))return '系统';if(/工具|模板|话术|表格/.test(text))return '工具';if(/应用|落地|转成/.test(text))return '应用';if(/学习|课程|学习卡/.test(text))return '学习';return '复盘、机动与临时事项'}
function timeCategories(){return ['组长运营','会议','学员帮扶','实战个案','客户触达与成交','内容生态发布','工具','系统','学习','应用','复盘、机动与临时事项']}
function defaultAwakeningTimeItems(){return[
  {text:'06:00—07:00 觉醒早课',quadrant:'紧急且重要',category:'学习',plannedMinutes:60},
  {text:'08:00—09:30 觉醒计划课',quadrant:'紧急且重要',category:'学习',plannedMinutes:90},
  {text:'09:30—10:00 督促学习委员、检查作业布置',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'10:00—11:00 辅导员定向会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60},
  {text:'11:00—12:00 分堂会／组员会议',quadrant:'紧急且重要',category:'会议',plannedMinutes:60},
  {text:'13:30—15:00 督促作业、点评与掉队成员跟进',quadrant:'紧急且重要',category:'学员帮扶',plannedMinutes:90},
  {text:'15:00—16:30 实战个案、客户触达或私聊跟进',quadrant:'重要不紧急',category:'客户触达与成交',plannedMinutes:90},
  {text:'16:30—18:00 内容生态发布',quadrant:'重要不紧急',category:'内容生态发布',plannedMinutes:90},
  {text:'19:30—20:30 辅导员组会／幸福人生安排（当天有时）',quadrant:'紧急且重要',category:'会议',plannedMinutes:60,optional:true},
  {text:'20:30—21:30 收报数、汇总积分',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'21:30—22:00 向督导报送结果与异常',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'22:00—22:30 复盘、知识库更新与次日安排',quadrant:'重要不紧急',category:'系统',plannedMinutes:30}
].map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}))}
function ensureTimeItems(){const now=new Date(),today=now.getDay(),date=now.toISOString().slice(0,10);if(state.timeSourceVersion!==7){const weekendItems=state.tasks.map((task,index)=>({text:task.text||'今日待办',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false,skipped:false}));if(date==='2026-07-26')weekendItems.push({text:'19:30左右 辅导员组会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60,actualMinutes:0,startedAt:null,done:false,skipped:false});state.timeItems=(today===0||today===6)?weekendItems:defaultAwakeningTimeItems();state.timeSourceVersion=7;localStorage.setItem(KEY,JSON.stringify(state))}}
function renderTimeSummary(){const history=Array.isArray(state.timeHistory)?state.timeHistory:[],now=new Date(),weekStart=new Date(now);weekStart.setDate(now.getDate()-((now.getDay()+6)%7));weekStart.setHours(0,0,0,0);const monthKey=now.toISOString().slice(0,7),week=history.filter(x=>new Date(x.date)>=weekStart),month=history.filter(x=>x.date.slice(0,7)===monthKey),total=records=>records.reduce((sum,r)=>sum+r.items.reduce((s,i)=>s+(Number(i.actualMinutes)||0),0),0),allMonth=total(month);const colors=['#b7791f','#d65f5f','#2f855a','#805ad5','#3182ce','#d69e2e','#4a5568','#38a169','#dd6b20','#9f7aea','#718096'];const categoryData=timeCategories().map((category,index)=>{const mins=month.reduce((sum,r)=>sum+r.items.filter(i=>i.category===category).reduce((s,i)=>s+(Number(i.actualMinutes)||0),0),0),rate=allMonth?Math.round(mins/allMonth*100):0;return {category,mins,rate,color:colors[index]}});const gradient=allMonth?`conic-gradient(${categoryData.reduce((s,x)=>{const a=s.end,b=a+x.rate;s.text+=`${x.color} ${a}% ${b}%,`;s.end=b;return s},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';const verticalBars=categoryData.map(x=>`<div style="width:58px;text-align:center;flex:0 0 58px"><b style="font-size:12px">${x.mins}分</b><div style="height:180px;display:flex;align-items:flex-end;justify-content:center;border-bottom:1px solid #cbd5e0;margin:6px 0"><div title="${x.category}：${x.mins}分钟，占${x.rate}%" style="width:32px;height:${x.mins?Math.max(8,Math.round(x.rate*1.7)):5}px;background:${x.mins?x.color:'#cbd5e0'};border-radius:8px 8px 0 0"></div></div><small style="font-size:11px;line-height:1.3;display:block">${x.category}</small><small>${x.rate}%</small></div>`).join('');const weekRows=week.flatMap(r=>r.items.map(i=>`<tr><td>${r.date}</td><td>${esc(i.text)}</td><td>${i.category}</td><td>${i.actualMinutes||0}分钟</td><td>${i.done?'完成':'未完成'}</td></tr>`)).join('')||'<tr><td colspan="5">本周尚未确认时间结果</td></tr>';const monthRows=categoryData.map(x=>`<tr><td>${x.category}</td><td>${x.mins}分钟</td><td>${x.rate}%</td></tr>`).join('');$("timeSummaryTable").innerHTML=`<div class="goal-summary"><div class="kpi"><span>本周实际时间</span><b>${total(week)}分钟</b></div><div class="kpi"><span>本周已确认天数</span><b>${week.length}天</b></div><div class="kpi"><span>本月实际时间</span><b>${allMonth}分钟</b></div><div class="kpi"><span>本月已确认天数</span><b>${month.length}天</b></div></div><h3 style="margin:20px 0 8px">本月时间去向图</h3><div style="display:flex;gap:28px;align-items:center;flex-wrap:wrap;padding:10px 0"><div style="width:156px;height:156px;border-radius:50%;background:${gradient};position:relative"><div style="position:absolute;inset:39px;background:var(--card);border-radius:50%;display:flex;align-items:center;justify-content:center;text-align:center;font-size:13px"><b>${allMonth}<br>分钟</b></div></div><div style="overflow:auto;max-width:100%"><div style="display:flex;gap:8px;align-items:flex-end;min-width:700px;padding:4px">${verticalBars}</div></div></div><h3 style="margin:20px 0 8px">本周工作事项明细</h3><div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>事项</th><th>时间板块</th><th>实际用时</th><th>结果</th></tr></thead><tbody>${weekRows}</tbody></table></div><h3 style="margin:20px 0 8px">本月时间统计</h3><div style="overflow:auto"><table class="goal-table"><thead><tr><th>时间板块</th><th>累计用时</th><th>时间占比</th></tr></thead><tbody>${monthRows}</tbody></table></div>`}
function renderTimeSummary(){const history=Array.isArray(state.timeHistory)?state.timeHistory:[],now=new Date(),weekStart=new Date(now);weekStart.setDate(now.getDate()-((now.getDay()+6)%7));weekStart.setHours(0,0,0,0);const monthKey=now.toISOString().slice(0,7),week=history.filter(x=>new Date(x.date)>=weekStart),month=history.filter(x=>x.date.slice(0,7)===monthKey),total=records=>records.reduce((sum,r)=>sum+r.items.reduce((s,i)=>s+(Number(i.actualMinutes)||0),0),0),weekTotal=total(week),monthTotal=total(month),colors=['#b7791f','#d65f5f','#2f855a','#805ad5','#3182ce','#d69e2e','#4a5568','#38a169','#dd6b20','#9f7aea','#718096'];const summarize=records=>timeCategories().map((category,index)=>{const mins=records.reduce((sum,r)=>sum+r.items.filter(i=>i.category===category).reduce((s,i)=>s+(Number(i.actualMinutes)||0),0),0),all=total(records);return {category,mins,rate:all?Math.round(mins/all*100):0,color:colors[index]}});const weekData=summarize(week),monthData=summarize(month);const pieGradient=weekTotal?`conic-gradient(${weekData.reduce((s,x)=>{const a=s.end,b=a+x.rate;s.text+=`${x.color} ${a}% ${b}%,`;s.end=b;return s},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';const legend=weekData.map(x=>`<div style="display:flex;align-items:center;gap:6px;margin:5px 0"><i style="width:10px;height:10px;border-radius:50%;background:${x.mins?x.color:'#cbd5e0'};display:inline-block"></i><span>${x.category}</span><b style="margin-left:auto">${x.mins}分 ${x.rate}%</b></div>`).join('');const weekRows=week.flatMap(r=>r.items.map(i=>`<tr><td>${r.date}</td><td>${esc(i.text)}</td><td>${i.category}</td><td>${i.actualMinutes||0}分钟</td><td>${i.done?'完成':'未完成'}</td></tr>`)).join('')||'<tr><td colspan="5">本周尚未确认时间结果</td></tr>';const monthRows=monthData.map(x=>`<tr><td>${x.category}</td><td>${x.mins}分钟</td><td>${x.rate}%</td></tr>`).join('');const bars=monthData.map(x=>`<div style="width:58px;text-align:center;flex:0 0 58px"><b style="font-size:12px">${x.mins}分</b><div style="height:180px;display:flex;align-items:flex-end;justify-content:center;border-bottom:1px solid #cbd5e0;margin:6px 0"><div title="${x.category}：${x.mins}分钟，占${x.rate}%" style="width:32px;height:${x.mins?Math.max(8,Math.round(x.rate*1.7)):5}px;background:${x.mins?x.color:'#cbd5e0'};border-radius:8px 8px 0 0"></div></div><small style="font-size:11px;line-height:1.3;display:block">${x.category}</small><small>${x.rate}%</small></div>`).join('');$("timeSummaryTable").innerHTML=`<div class="goal-summary"><div class="kpi"><span>本周实际时间</span><b>${weekTotal}分钟</b></div><div class="kpi"><span>本周已确认天数</span><b>${week.length}天</b></div><div class="kpi"><span>本月实际时间</span><b>${monthTotal}分钟</b></div><div class="kpi"><span>本月已确认天数</span><b>${month.length}天</b></div></div><h3 style="margin:20px 0 8px">本周时间去向（饼图）</h3><div style="display:flex;gap:28px;align-items:center;flex-wrap:wrap;padding:10px 0"><div style="width:156px;height:156px;border-radius:50%;background:${pieGradient};position:relative"><div style="position:absolute;inset:39px;background:var(--card);border-radius:50%;display:flex;align-items:center;justify-content:center;text-align:center;font-size:13px"><b>${weekTotal}<br>分钟</b></div></div><div style="min-width:280px;flex:1">${legend}</div></div><h3 style="margin:20px 0 8px">本周工作事项明细</h3><div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>事项</th><th>时间板块</th><th>实际用时</th><th>结果</th></tr></thead><tbody>${weekRows}</tbody></table></div><h3 style="margin:20px 0 8px">本月时间统计</h3><div style="overflow:auto"><table class="goal-table"><thead><tr><th>时间板块</th><th>累计用时</th><th>时间占比</th></tr></thead><tbody>${monthRows}</tbody></table></div><h3 style="margin:20px 0 8px">本月时间去向（柱状图）</h3><div style="overflow:auto"><div style="display:flex;gap:8px;align-items:flex-end;min-width:700px;padding:4px">${bars}</div></div>`}
function renderSlots(){ensureTimeItems();const items=state.timeItems,planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length,balance=720-planned;$('timePlanView').textContent=planned+'分钟';$('timeCapacityView').textContent=balance>=0?'余'+balance+'分钟':'超'+Math.abs(balance)+'分钟';$('timeActualView').textContent=actual+'分钟';$('timeDoneView').textContent=done+' / '+items.length;$('timeRateView').textContent=Math.min(100,Math.round(actual/720*100))+'%';$('slots').innerHTML=items.map((item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜建议${item.plannedMinutes}分钟｜实际${item.actualMinutes||0}分钟｜${item.skipped?'未完成':item.done?'已完成':'待执行'}</small></div><div class="row">${item.skipped?`<button class="btn soft" type="button" onclick="restoreTimeItem(${index})">恢复待执行</button>`:`<button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'已完成':item.startedAt?'完成':'开始'}</button><button class="btn ghost" type="button" onclick="markTimeUnfinished(${index})">未完成</button>`}</div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$('timeQuadrants').innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$('timePie').innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary();renderActionBoard()}
function renderActionBoard(){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);const tomorrowKey=tomorrow.toISOString().slice(0,10),weekend=tomorrow.getDay()===0||tomorrow.getDay()===6;const plan=weekend?[]:defaultAwakeningTimeItems().filter(item=>!item.optional);const tomorrowEl=$('tomorrowActionBoard');if(tomorrowEl)tomorrowEl.innerHTML=plan.length?`<div class="notice">${tomorrowKey}｜工作日默认行动清单（共${plan.reduce((s,x)=>s+x.plannedMinutes,0)}分钟）</div>${plan.map((item,i)=>`<div class="slot"><span class="tag">${item.category}</span><div><b>${i+1}. ${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.quadrant}｜${item.plannedMinutes}分钟</small></div></div>`).join('')}`:`<div class="notice">${tomorrowKey} 是周末：不自动排觉醒计划事项；只显示 Obsidian 已安排的会议或任务。</div>`;const history=Array.isArray(state.timeHistory)?state.timeHistory:[],weekStart=new Date();weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));weekStart.setHours(0,0,0,0);const week=history.filter(r=>new Date(r.date)>=weekStart),monthKey=new Date().toISOString().slice(0,7),month=history.filter(r=>r.date.slice(0,7)===monthKey);const weekRows=week.flatMap(r=>r.items.map(i=>`<tr><td>${r.date}</td><td>${esc(i.text)}</td><td>${i.category}</td><td>${i.done?'完成':'未完成'}</td></tr>`)).join('')||'<tr><td colspan="4">本周尚未确认行动结果</td></tr>';const weekEl=$('weekActionBoard');if(weekEl)weekEl.innerHTML=`<div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>行动事项</th><th>板块</th><th>结果</th></tr></thead><tbody>${weekRows}</tbody></table></div>`;const monthEl=$('monthActionBoard');if(monthEl)monthEl.innerHTML=`<div class="goal-summary">${timeCategories().map(category=>{const items=month.flatMap(r=>r.items).filter(i=>i.category===category),done=items.filter(i=>i.done).length;return `<div class="kpi"><span>${category}</span><b>${done} / ${items.length}项</b></div>`}).join('')}</div>`}
function confirmTomorrowActions(){const date=new Date();date.setDate(date.getDate()+1);state.tomorrowActionConfirmedFor=date.toISOString().slice(0,10);localStorage.setItem(KEY,JSON.stringify(state));renderActionBoard();toast('明日行动已确认')}
setTimeout(renderActionBoard,0)
function renderSlots(){ensureTimeItems();const items=state.timeItems,planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length,balance=720-planned;$('timePlanView').textContent=planned+'分钟';$('timeCapacityView').textContent=balance>=0?'余'+balance+'分钟':'超'+Math.abs(balance)+'分钟';$('timeActualView').textContent=actual+'分钟';$('timeDoneView').textContent=done+' / '+items.length;$('timeRateView').textContent=Math.min(100,Math.round(actual/720*100))+'%';$('slots').innerHTML=items.map((item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜计划${item.plannedMinutes}分钟｜${item.done?'已完成':'未勾选=未完成'}</small></div><div class="row"><button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'✓ 已完成（点此取消）':'○ 勾选完成'}</button></div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$('timeQuadrants').innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$('timePie').innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary();renderActionBoard()}
function tomorrowPlanItems(){const d=new Date();d.setDate(d.getDate()+1);const key=d.toISOString().slice(0,10);return defaultAwakeningTimeItems().filter(item=>!(key==='2026-07-27'&&/辅导员定向会/.test(item.text)))}
function ensureTimeItems(){const now=new Date(),today=now.getDay(),date=now.toISOString().slice(0,10);if(state.timeSourceVersion!==8){const weekendItems=state.tasks.map((task,index)=>({text:task.text||'今日待办',quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text||''),plannedMinutes:[120,60,30][index]||30,actualMinutes:0,startedAt:null,done:false,skipped:false}));if(date==='2026-07-26')weekendItems.push({text:'19:30左右 辅导员组会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60,actualMinutes:0,startedAt:null,done:false,skipped:false});const weekdayItems=defaultAwakeningTimeItems().filter(item=>!(date==='2026-07-27'&&/辅导员定向会/.test(item.text)));state.timeItems=(today===0||today===6)?weekendItems:weekdayItems;state.timeSourceVersion=8;localStorage.setItem(KEY,JSON.stringify(state))}}
function renderActionBoard(){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);const tomorrowKey=tomorrow.toISOString().slice(0,10),weekend=tomorrow.getDay()===0||tomorrow.getDay()===6,plan=weekend?[]:tomorrowPlanItems();const tomorrowEl=$('tomorrowActionBoard');if(tomorrowEl)tomorrowEl.innerHTML=plan.length?`<div class="notice">${tomorrowKey}｜工作日行动清单（共${plan.reduce((s,x)=>s+x.plannedMinutes,0)}分钟）</div>${plan.map((item,i)=>`<div class="slot"><span class="tag">${item.category}</span><div><b>${i+1}. ${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.quadrant}｜${item.plannedMinutes}分钟</small></div></div>`).join('')}`:`<div class="notice">${tomorrowKey} 是周末：不自动排觉醒计划事项；只显示 Obsidian 已安排的会议或任务。</div>`;const history=Array.isArray(state.timeHistory)?state.timeHistory:[],weekStart=new Date();weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));weekStart.setHours(0,0,0,0);const week=history.filter(r=>new Date(r.date)>=weekStart),monthKey=new Date().toISOString().slice(0,7),month=history.filter(r=>r.date.slice(0,7)===monthKey);const weekRows=week.flatMap(r=>r.items.map(i=>`<tr><td>${r.date}</td><td>${esc(i.text)}</td><td>${i.category}</td><td>${i.done?'完成':'未完成'}</td></tr>`)).join('')||'<tr><td colspan="4">本周尚未确认行动结果</td></tr>';const weekEl=$('weekActionBoard');if(weekEl)weekEl.innerHTML=`<div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>行动事项</th><th>板块</th><th>结果</th></tr></thead><tbody>${weekRows}</tbody></table></div>`;const monthEl=$('monthActionBoard');if(monthEl)monthEl.innerHTML=`<div class="goal-summary">${timeCategories().map(category=>{const items=month.flatMap(r=>r.items).filter(i=>i.category===category),done=items.filter(i=>i.done).length;return `<div class="kpi"><span>${category}</span><b>${done} / ${items.length}项</b></div>`}).join('')}</div>`}
function defaultAwakeningTimeItems(){return[
  {text:'06:00—07:00 觉醒早课',quadrant:'紧急且重要',category:'学习',plannedMinutes:60},
  {text:'08:00—09:30 觉醒计划课',quadrant:'紧急且重要',category:'学习',plannedMinutes:90},
  {text:'09:30—10:00 督促学习委员、检查作业布置',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'13:30—15:00 督促作业、点评与掉队成员跟进',quadrant:'紧急且重要',category:'学员帮扶',plannedMinutes:90},
  {text:'15:00—16:30 实战个案、客户触达或私聊跟进',quadrant:'重要不紧急',category:'客户触达与成交',plannedMinutes:90},
  {text:'16:30—18:00 内容生态发布',quadrant:'重要不紧急',category:'内容生态发布',plannedMinutes:90},
  {text:'20:30—21:30 收报数、汇总积分',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:60},
  {text:'21:30—22:00 向督导报送结果与异常',quadrant:'紧急且重要',category:'组长运营',plannedMinutes:30},
  {text:'22:00—22:30 复盘、知识库更新与次日安排',quadrant:'重要不紧急',category:'系统',plannedMinutes:30}
].map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}))}
function ensureTimeItems(){const now=new Date(),today=now.getDay(),date=now.toISOString().slice(0,10);if(state.timeSourceVersion!==11){const extras=state.tasks.filter(t=>t.text&&t.text.trim()).map((task,index)=>({text:task.text,quadrant:index===0?'紧急且重要':index===1?'重要不紧急':'不紧急不重要',category:timeCategory(task.text),plannedMinutes:0,actualMinutes:0,startedAt:null,done:false,skipped:false,isExtra:true}));let weekendItems=[...extras];if(date==='2026-07-26')weekendItems=[{text:'19:30左右 辅导员组会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60,actualMinutes:0,startedAt:null,done:false,skipped:false,isExtra:true}];state.timeItems=(today===0||today===6)?weekendItems:[...defaultAwakeningTimeItems(),...extras];state.timeSourceVersion=11;localStorage.setItem(KEY,JSON.stringify(state))}}
function renderSlots(){ensureTimeItems();const items=state.timeItems,planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length,balance=720-planned;$('timePlanView').textContent=planned+'分钟';$('timeCapacityView').textContent=balance>=0?'余'+balance+'分钟':'超'+Math.abs(balance)+'分钟';$('timeActualView').textContent=actual+'分钟';$('timeDoneView').textContent=done+' / '+items.length;$('timeRateView').textContent=Math.min(100,Math.round(actual/720*100))+'%';$('slots').innerHTML=items.map((item,index)=>`${item.isExtra&&(index===0||!items[index-1].isExtra)?'<h3 style="margin:18px 0 8px">其他待办（Obsidian同步）</h3><div class="notice">临时会议、分享会和其他事项放这里；没有就不显示。</div>':''}<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜${item.isExtra?'临时待办':'计划'+item.plannedMinutes+'分钟'}｜${item.done?'已完成':'未勾选=未完成'}</small></div><div class="row"><button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'✓ 已完成（点此取消）':'○ 勾选完成'}</button></div></div>`).join('');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$('timeQuadrants').innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$('timePie').innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary();renderActionBoard()}
function renderSlots(){ensureTimeItems();const items=state.timeItems,planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length,balance=720-planned;$('timePlanView').textContent=planned+'分钟';$('timeCapacityView').textContent=balance>=0?'余'+balance+'分钟':'超'+Math.abs(balance)+'分钟';$('timeActualView').textContent=actual+'分钟';$('timeDoneView').textContent=done+' / '+items.length;$('timeRateView').textContent=Math.min(100,Math.round(actual/720*100))+'%';const card=(item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜${item.isExtra?'临时待办':'计划'+item.plannedMinutes+'分钟'}｜${item.done?'已完成':'未勾选=未完成'}</small></div><div class="row"><button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'✓ 已完成（点此取消）':'○ 勾选完成'}</button></div></div>`;const regular=items.map((item,index)=>({item,index})).filter(x=>!x.item.isExtra),extras=items.map((item,index)=>({item,index})).filter(x=>x.item.isExtra);$('slots').innerHTML=regular.map(x=>card(x.item,x.index)).join('')+`<h3 style="margin:18px 0 8px">其他待办（Obsidian自动同步）</h3>`+(extras.length?extras.map(x=>card(x.item,x.index)).join(''):'<div class="notice">暂无其他待办；Obsidian 有临时会议或任务时会自动显示在这里。</div>');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$('timeQuadrants').innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$('timePie').innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary();renderActionBoard()}
if(!Array.isArray(state.learningCards)){state.learningCards=[{id:'2026-07-22-真实需求',date:'2026-07-22',title:'穿透表象，看见真实需求',source:'觉醒计划腾讯会议纪要',learningType:'觉醒计划（腾讯会议纪要）',status:'待转母内容',core:'先倾听原话和情绪，再通过提问、观察与小行动验证真实需求。',application:'选择一位真实沟通对象，完成一次“不急着给答案”的倾听。'}];localStorage.setItem(KEY,JSON.stringify(state))}
{const knownCard=state.learningCards.find(x=>x.id==='2026-07-22-真实需求');if(knownCard){knownCard.source='觉醒计划腾讯会议纪要';knownCard.learningType='觉醒计划（腾讯会议纪要）';localStorage.setItem(KEY,JSON.stringify(state))}}
function renderLearningCards(){const cards=state.learningCards||[],today=localDateKey(),weekStart=new Date();weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));weekStart.setHours(0,0,0,0),monthKey=today.slice(0,7),week=cards.filter(c=>c.date<today&&new Date(c.date)>=weekStart),month=cards.filter(c=>c.date<today&&c.date.slice(0,7)===monthKey),todayCards=cards.filter(c=>c.date===today);const card=c=>`<div class="goal-step"><b>${esc(c.title)}</b><small style="display:block;color:var(--muted)">${c.date}｜${esc(c.learningType||'未分类')}｜${esc(c.source)}｜${esc(c.status)}</small><p style="margin:8px 0 0">核心：${esc(c.core)}</p><p style="margin:6px 0 0"><b>应用：</b>${esc(c.application)}</p></div>`;const todayEl=$('todayLearningCards');if(todayEl)todayEl.innerHTML=todayCards.length?todayCards.map(card).join(''):'<div class="notice">今天没有新增的 Obsidian 学习笔记／学习卡，不需要填写学习任务。</div>';const weekEl=$('weekLearningCards');if(weekEl)weekEl.innerHTML=week.length?week.map(card).join(''):'<div class="notice">本周暂无新增学习卡。</div>';const monthEl=$('monthLearningCards');if(monthEl)monthEl.innerHTML=month.length?`<div class="goal-summary"><div class="kpi"><span>本月学习卡</span><b>${month.length}张</b></div><div class="kpi"><span>待应用／转内容</span><b>${month.filter(c=>/待/.test(c.status)).length}张</b></div></div>${month.map(card).join('')}`:'<div class="notice">本月暂无学习卡。</div>'}
setTimeout(renderLearningCards,0)
function fullReviewText(){const date=localDateKey(),items=state.timeItems||[],done=items.filter(x=>x.done),unfinished=items.filter(x=>!x.done),actual=items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),planned=items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),cards=(state.learningCards||[]).filter(x=>x.date===date),metrics=Object.entries(state.goalMetrics||{}).map(([id,x])=>`${id}：${Number(x.current)||0}/${Number(x.goal)||0}`).join('；');return `# 今日整体复盘｜${date}\n\n## 心态\n- 能量：${state.energy?.score||'未确认'}\n\n## 目标\n- 今日目标：${state.dailyGoal||'未同步'}\n- 指标实际／计划：${metrics||'未确认'}\n\n## 时间\n- 已排：${planned}分钟｜实际：${actual}分钟｜12小时余量：${720-planned}分钟\n- 已完成：${done.length}项｜未完成：${unfinished.length}项\n\n## 行动\n${items.map(x=>`- ${x.done?'[x]':'[ ]'} ${x.text}｜${x.category}`).join('\n')||'- 未同步行动'}\n\n## 学习\n${cards.length?cards.map(c=>`- ${c.title}｜${c.learningType||'未分类'}｜${c.status}`).join('\n'):'- 今日无新增学习卡'}\n\n## 待小杰分析\n- 今日最有效的动作\n- 最大阻塞与偏差\n- 明日保留、调整、停止什么\n`}
function renderReviewBoard(){const text=fullReviewText(),items=state.timeItems||[],done=items.filter(x=>x.done).length,planned=items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),actual=items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),cards=(state.learningCards||[]).filter(x=>x.date===localDateKey());const board=$('fullReviewBoard');if(board)board.innerHTML=`<div class="goal-summary"><div class="kpi"><span>心态</span><b>${state.energy?.score||'未确认'}</b></div><div class="kpi"><span>行动完成</span><b>${done} / ${items.length}项</b></div><div class="kpi"><span>时间</span><b>${actual} / ${planned}分</b></div><div class="kpi"><span>今日学习卡</span><b>${cards.length}张</b></div></div><div style="margin-top:14px;white-space:pre-wrap;line-height:1.7">${esc(text)}</div>`}
function exportFullReview(){const out=$('reviewExport'),text=fullReviewText();out.style.display='none';out.textContent=text;state.lastFullReview=text;localStorage.setItem(KEY,JSON.stringify(state));navigator.clipboard?.writeText(text).then(()=>toast('复盘文字已复制，直接粘贴发给小杰')).catch(()=>toast('复制失败，请刷新后重试'))}
function confirmFullReview(){const date=localDateKey(),content='\n'+fullReviewText();state.fullReviews=state.fullReviews||{};state.fullReviews[date]=content;localStorage.setItem(KEY,JSON.stringify(state));renderReviewBoard();toast('今日整盘已确认；下一步导出文字给小杰分析')}
function confirmAndExportFullReview(){confirmFullReview();exportFullReview()}
function saveReviewInput(key,data){const date=localDateKey();state.reviewInputs=state.reviewInputs||{};state.reviewInputs[date]=state.reviewInputs[date]||{};state.reviewInputs[date][key]={...data,confirmedAt:new Date().toISOString()};localStorage.setItem(KEY,JSON.stringify(state));renderReviewBoard();toast('已确认并进入今日复盘')}
function confirmMindsetToReview(){read();save();saveReviewInput('mindset',{energy:state.energy?.score||'未记录',nightMood:state.mindset?.nightMoodScore||'未记录'})}
function confirmGoalsToReview(){saveGoalPlan();saveReviewInput('goals',{dailyGoal:state.dailyGoal||'未同步',metrics:state.goalMetrics||{}})}
async function confirmTimeToReview(){await confirmTimeResult();const items=state.timeItems||[];saveReviewInput('time',{planned:items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),actual:items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),done:items.filter(x=>x.done).length})}
function confirmActionToReview(){const items=state.timeItems||[];saveReviewInput('action',{done:items.filter(x=>x.done).length,total:items.length})}
function confirmLearningToReview(){const date=localDateKey(),cards=(state.learningCards||[]).filter(x=>x.date===date);saveReviewInput('learning',{cards:cards.length,titles:cards.map(x=>x.title)})}
function confirmTimeResult(){ensureTimeItems();const date=localDateKey(),entry={date,items:state.timeItems.map(item=>({...item,startedAt:null}))};state.timeHistory=Array.isArray(state.timeHistory)?state.timeHistory:[];state.timeHistory=state.timeHistory.filter(item=>item.date!==date);state.timeHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('time',{planned:entry.items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),actual:entry.items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),done:entry.items.filter(x=>x.done).length});render();toast('今日时间已确认，已进入整体复盘')}
function formatTimeClock(value){if(!value)return '—';const d=new Date(Number(value));return String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0')}
function toggleTimeItem(index){ensureTimeItems();const item=state.timeItems[index];if(item.done)return;if(!item.startedAt){item.startedAt=Date.now();item.completedAt=null;toast('已记录开始时间 '+formatTimeClock(item.startedAt))}else{item.completedAt=Date.now();item.actualMinutes=Math.max(1,Math.round((item.completedAt-item.startedAt)/60000));item.done=true;item.skipped=false;toast('已记录结束时间 '+formatTimeClock(item.completedAt)+'，实际'+item.actualMinutes+'分钟')}localStorage.setItem(KEY,JSON.stringify(state));renderSlots()}
function markTimeUnfinished(index){ensureTimeItems();const item=state.timeItems[index];item.startedAt=null;item.completedAt=null;item.actualMinutes=0;item.skipped=true;localStorage.setItem(KEY,JSON.stringify(state));renderSlots();toast('已记录未完成')}
function restoreTimeItem(index){ensureTimeItems();const item=state.timeItems[index];item.skipped=false;item.done=false;item.startedAt=null;item.completedAt=null;item.actualMinutes=0;localStorage.setItem(KEY,JSON.stringify(state));renderSlots();toast('已恢复为待执行，可重新点开始')}
function renderSlots(){ensureTimeItems();const items=state.timeItems,planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length,balance=720-planned;$('timePlanView').textContent=planned+'分钟';$('timeCapacityView').textContent=balance>=0?'余'+balance+'分钟':'超'+Math.abs(balance)+'分钟';$('timeActualView').textContent=actual+'分钟';$('timeDoneView').textContent=done+' / '+items.length;$('timeRateView').textContent=Math.min(100,Math.round(actual/720*100))+'%';const card=(item,index)=>`<div class="slot"><span class="tag">${item.quadrant}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.category}｜${item.isExtra?'临时待办':'计划'+item.plannedMinutes+'分钟'}｜开始 ${formatTimeClock(item.startedAt)}｜结束 ${formatTimeClock(item.completedAt)}｜实际${item.actualMinutes||0}分钟｜${item.skipped?'未完成':item.done?'已完成':item.startedAt?'进行中':'待执行'}</small></div><div class="row">${item.skipped?`<button class="btn soft" type="button" onclick="restoreTimeItem(${index})">恢复待执行</button>`:`<button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleTimeItem(${index})">${item.done?'已完成':item.startedAt?'完成':'开始'}</button><button class="btn ghost" type="button" onclick="markTimeUnfinished(${index})">未完成</button>`}</div></div>`;const regular=items.map((item,index)=>({item,index})).filter(x=>!x.item.isExtra),extras=items.map((item,index)=>({item,index})).filter(x=>x.item.isExtra);$('slots').innerHTML=regular.map(x=>card(x.item,x.index)).join('')+`<h3 style="margin:18px 0 8px">其他待办（Obsidian自动同步）</h3>`+(extras.length?extras.map(x=>card(x.item,x.index)).join(''):'<div class="notice">暂无其他待办；Obsidian 有临时会议或任务时会自动显示在这里。</div>');const quads=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];$('timeQuadrants').innerHTML=quads.map(q=>`<div class="goal-step"><b>${items.filter(item=>item.quadrant===q).length}项</b>${q}</div>`).join('');$('timePie').innerHTML=timeCategories().map(category=>`<div class="goal-step"><b>${items.filter(item=>item.category===category).reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0)}分</b>${category}</div>`).join('');renderTimeSummary()}
function ensureActionItems(){ensureTimeItems();const date=localDateKey();if(state.actionSourceDate!==date||state.actionSourceVersion!==2){state.actionItems=date==='2026-07-26'?[]:state.timeItems.map(item=>({text:item.text,category:item.category,quadrant:item.quadrant,done:false}));state.actionSourceDate=date;state.actionSourceVersion=2;localStorage.setItem(KEY,JSON.stringify(state))}}
function toggleActionItem(index){ensureActionItems();state.actionItems[index].done=!state.actionItems[index].done;localStorage.setItem(KEY,JSON.stringify(state));renderTodayActionChecklist();renderActionBoard()}
function renderTodayActionChecklist(){ensureActionItems();const el=$('todayActionChecklist');if(el)el.innerHTML=state.actionItems.map((item,index)=>`<div class="slot"><span class="tag">${item.category}</span><div><b>${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.done?'已完成':'待完成'}</small></div><button class="btn ${item.done?'success':'soft'}" type="button" onclick="toggleActionItem(${index})">${item.done?'✓ 已完成':'○ 勾选完成'}</button></div>`).join('')}
function confirmActionToReview(){ensureActionItems();const date=localDateKey(),entry={date,items:state.actionItems.map(x=>({...x}))};state.actionHistory=Array.isArray(state.actionHistory)?state.actionHistory:[];state.actionHistory=state.actionHistory.filter(x=>x.date!==date);state.actionHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('action',{done:entry.items.filter(x=>x.done).length,total:entry.items.length});renderActionBoard();toast('今日行动已确认，已进入整体复盘')}
function renderActionBoard(){renderTodayActionChecklist();const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);const tomorrowKey=tomorrow.toISOString().slice(0,10),weekend=tomorrow.getDay()===0||tomorrow.getDay()===6,plan=weekend?[]:tomorrowPlanItems();const tomorrowEl=$('tomorrowActionBoard');if(tomorrowEl)tomorrowEl.innerHTML=plan.length?`<div class="notice">${tomorrowKey}｜工作日行动清单</div>${plan.map((item,i)=>`<div class="slot"><span class="tag">${item.category}</span><div><b>${i+1}. ${esc(item.text)}</b><small style="display:block;color:var(--muted)">${item.quadrant}</small></div></div>`).join('')}`:`<div class="notice">${tomorrowKey} 是周末：只显示 Obsidian 已安排的会议或任务。</div>`;const history=Array.isArray(state.actionHistory)?state.actionHistory:[],weekStart=new Date();weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));weekStart.setHours(0,0,0,0);const week=history.filter(r=>new Date(r.date)>=weekStart),monthKey=new Date().toISOString().slice(0,7),month=history.filter(r=>r.date.slice(0,7)===monthKey),colors=['#b7791f','#d65f5f','#2f855a','#805ad5','#3182ce','#d69e2e','#4a5568','#38a169','#dd6b20','#9f7aea','#718096'];const actionData=records=>timeCategories().map((category,index)=>{const all=records.flatMap(r=>r.items).filter(i=>i.category===category),done=all.filter(i=>i.done).length;return {category,all:all.length,done,color:colors[index]}});const wData=actionData(week),mData=actionData(month),wTotal=wData.reduce((s,x)=>s+x.done,0),gradient=wTotal?`conic-gradient(${wData.reduce((s,x)=>{const a=s.end,b=a+Math.round(x.done/wTotal*100);s.text+=`${x.color} ${a}% ${b}%,`;s.end=b;return s},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';const legend=wData.map(x=>`<div style="display:flex;gap:6px;align-items:center;margin:5px 0"><i style="width:10px;height:10px;border-radius:50%;background:${x.done?x.color:'#cbd5e0'}"></i><span>${x.category}</span><b style="margin-left:auto">${x.done}项</b></div>`).join('');const weekRows=week.flatMap(r=>r.items.map(i=>`<tr><td>${r.date}</td><td>${esc(i.text)}</td><td>${i.category}</td><td>${i.done?'完成':'未完成'}</td></tr>`)).join('')||'<tr><td colspan="4">本周尚未确认行动结果</td></tr>';const weekEl=$('weekActionBoard');if(weekEl)weekEl.innerHTML=`<h3 style="margin:0 0 8px">本周完成动作分布（饼图）</h3><div style="display:flex;gap:28px;align-items:center;flex-wrap:wrap;padding:8px 0 16px"><div style="width:150px;height:150px;border-radius:50%;background:${gradient};position:relative"><div style="position:absolute;inset:38px;background:var(--card);border-radius:50%;display:flex;align-items:center;justify-content:center;text-align:center"><b>${wTotal}<br>项完成</b></div></div><div style="min-width:280px;flex:1">${legend}</div></div><div style="overflow:auto"><table class="goal-table"><thead><tr><th>日期</th><th>行动事项</th><th>板块</th><th>结果</th></tr></thead><tbody>${weekRows}</tbody></table></div>`;const max=Math.max(1,...mData.map(x=>x.done)),bars=mData.map(x=>`<div style="width:58px;text-align:center;flex:0 0 58px"><b style="font-size:12px">${x.done}项</b><div style="height:170px;display:flex;align-items:flex-end;justify-content:center;border-bottom:1px solid #cbd5e0;margin:6px 0"><div style="width:32px;height:${x.done?Math.max(8,Math.round(x.done/max*160)):5}px;background:${x.done?x.color:'#cbd5e0'};border-radius:8px 8px 0 0"></div></div><small style="font-size:11px;line-height:1.3;display:block">${x.category}</small><small>${x.done}/${x.all}</small></div>`).join('');const monthEl=$('monthActionBoard');if(monthEl)monthEl.innerHTML=`<div style="overflow:auto"><table class="goal-table"><thead><tr><th>行动板块</th><th>完成／已记录</th></tr></thead><tbody>${mData.map(x=>`<tr><td>${x.category}</td><td>${x.done} / ${x.all}项</td></tr>`).join('')}</tbody></table></div><h3 style="margin:20px 0 8px">本月完成动作（柱状图）</h3><div style="overflow:auto"><div style="display:flex;gap:8px;align-items:flex-end;min-width:700px;padding:4px">${bars}</div></div>`}
document.querySelector('button[data-page="action"]')?.addEventListener('click',renderActionBoard)
function renderReviewBoard(){const date=localDateKey(),timeItems=state.timeItems||[],mindsetItems=state.mindset10||[],goalItems=Object.values(state.goalMetrics||{}),learningCards=(state.learningCards||[]).filter(x=>x.date===date),confirmed=state.reviewInputs?.[date]||{},actionSnapshot=confirmed.action||{done:0,total:0},counts={mindset:{done:mindsetItems.filter(x=>Number(x.score)>0).length,total:10},goals:{done:goalItems.filter(x=>Number(x.current)>0).length,total:goalItems.length},time:{done:timeItems.filter(x=>x.done).length,total:timeItems.length},action:{done:Number(actionSnapshot.done)||0,total:Number(actionSnapshot.total)||0},learning:{done:learningCards.filter(x=>x.confirmed).length,total:learningCards.length}};const text=fullReviewText(),board=$('fullReviewBoard');if(board)board.innerHTML=`<div class="goal-summary"><div class="kpi"><span>心态</span><b>${counts.mindset.done} / ${counts.mindset.total}项</b></div><div class="kpi"><span>目标</span><b>${counts.goals.done} / ${counts.goals.total}项</b></div><div class="kpi"><span>时间</span><b>${counts.time.done} / ${counts.time.total}项</b></div><div class="kpi"><span>行动</span><b>${counts.action.done} / ${counts.action.total}项</b></div><div class="kpi"><span>学习</span><b>${counts.learning.done} / ${counts.learning.total}项</b></div></div><div class="notice" style="margin-top:14px">上面只读取今天各板块“确认并进入复盘”后保存的数据；尚未确认就显示 0 / 0，不预读明天。</div><div style="margin-top:14px;white-space:pre-wrap;line-height:1.7">${esc(text)}</div>`}
function confirmLearningToReview(){const date=localDateKey(),cards=(state.learningCards||[]).filter(x=>x.date===date);cards.forEach(x=>x.confirmed=true);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('learning',{cards:cards.length,titles:cards.map(x=>x.title)});renderLearningCards()}
function renderMindsetQuick(){const oldCard=document.querySelector('.mindset-scorecard'),oldMonth=document.querySelector('.mindset-month'),weekCard=document.querySelector('.mindset-trend'),quick=$('mindsetQuickCard');if(oldCard)oldCard.style.display='block';if(oldMonth)oldMonth.style.display='block';if(weekCard)weekCard.style.display='block';if(quick)quick.remove();renderMindsetWeekPie();renderMonthSummary()}
function renderMindsetWeekPie(){const card=document.querySelector('.mindset-trend'),el=$('trend');if(!card||!el)return;const title=card.querySelector('h2');if(title)title.innerHTML='🥧 本周心态分布 <small>按本周已记录能量自动汇总</small>';const weekStart=new Date();weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));weekStart.setHours(0,0,0,0);const bins=[{name:'满血',scores:[10],color:'#d65f5f'},{name:'充足',scores:[8],color:'#d69e2e'},{name:'还行',scores:[6],color:'#3182ce'},{name:'疲惫',scores:[4],color:'#805ad5'},{name:'低谷',scores:[2],color:'#718096'}];const records=(state.energyHistory||[]).filter(x=>new Date(x.date)>=weekStart);const data=bins.map(bin=>({...bin,count:records.filter(x=>bin.scores.includes(Number(x.score))).length})),total=data.reduce((s,x)=>s+x.count,0);const gradient=total?`conic-gradient(${data.reduce((a,x)=>{const start=a.end,end=start+x.count/total*100;a.text+=`${x.color} ${start}% ${end}%,`;a.end=end;return a},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';el.style.height='auto';el.innerHTML=`<div style="display:flex;gap:28px;align-items:center;flex-wrap:wrap"><div style="width:150px;height:150px;border-radius:50%;background:${gradient};position:relative"><div style="position:absolute;inset:38px;border-radius:50%;background:var(--card);display:flex;align-items:center;justify-content:center;text-align:center"><b>${total}<br>次记录</b></div></div><div style="min-width:240px;flex:1">${data.map(x=>`<div style="display:flex;gap:8px;align-items:center;margin:6px 0"><i style="width:10px;height:10px;border-radius:50%;background:${x.color}"></i><span>${x.name}</span><b style="margin-left:auto">${x.count}次</b></div>`).join('')}</div></div>`}
function saveMindsetQuick(value){state.mindsetQuickScore=Number(value)||0;localStorage.setItem(KEY,JSON.stringify(state));toast('今日心态评分已记录')}
function confirmAffirmation(){state.mindsetAffirmationConfirmed=true;localStorage.setItem(KEY,JSON.stringify(state));toast('今日肯定语已确认')}
function confirmMindsetToReview(){read();save();const checks=[Boolean(state.energy?.raw),Boolean(state.energy?.score),Boolean(state.mindsetAffirmationConfirmed),Boolean(state.mindsetQuickScore)];saveReviewInput('mindset',{done:checks.filter(Boolean).length,total:4,items:['能量急救包','能量按键','今日肯定语','今日心态评分']})}
const originalRenderReviewBoard=renderReviewBoard;renderReviewBoard=function(){originalRenderReviewBoard();const board=$('fullReviewBoard'),date=localDateKey(),mindset=state.reviewInputs?.[date]?.mindset;if(board){const first=board.querySelector('.goal-summary .kpi b');if(first)first.textContent=`${Number(mindset?.done)||0} / ${Number(mindset?.total)||4}项`}}
const learningSources=['觉醒早课','觉醒计划／腾讯会议纪要','组会／分享会','复盘学习','课程／听课','AI 共学与总结','公众号学习','视频学习','其他学习'];
function renderLearningSourceChecks(){const el=$('learningSourceChecks');if(!el)return;const selected=state.todayLearningSources||[];el.innerHTML=learningSources.map(source=>`<label><input type="checkbox" value="${source}" onchange="toggleLearningSource(this.value,this.checked)" ${selected.includes(source)?'checked':''}>${source}</label>`).join('')}
function toggleLearningSource(source,checked){const selected=new Set(state.todayLearningSources||[]);checked?selected.add(source):selected.delete(source);state.todayLearningSources=[...selected];localStorage.setItem(KEY,JSON.stringify(state))}
function confirmLearningToReview(){const date=localDateKey(),cards=(state.learningCards||[]).filter(x=>x.date===date),sources=state.todayLearningSources||[];cards.forEach(x=>x.confirmed=true);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('learning',{done:cards.length,total:cards.length,sources,cards:cards.length,titles:cards.map(x=>x.title)});renderLearningCards();toast('今日学习卡已确认，已进入整体复盘')}
function fullReviewText(){const date=localDateKey(),items=state.timeItems||[],done=items.filter(x=>x.done),unfinished=items.filter(x=>!x.done),actual=items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),planned=items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),cards=(state.learningCards||[]).filter(x=>x.date===date),metrics=Object.entries(state.goalMetrics||{}).map(([id,x])=>`${id}：${Number(x.current)||0}/${Number(x.goal)||0}`).join('；');return `# 今日整体复盘｜${date}\n\n## 心态\n- 能量：${state.energy?.score||'未确认'}\n\n## 目标\n- 今日目标：${state.dailyGoal||'未同步'}\n- 指标实际／计划：${metrics||'未确认'}\n\n## 时间\n- 已排：${planned}分钟｜实际：${actual}分钟｜12小时余量：${720-planned}分钟\n- 已完成：${done.length}项｜未完成：${unfinished.length}项\n\n## 行动\n${items.map(x=>`- ${x.done?'[x]':'[ ]'} ${x.text}｜${x.category}`).join('\n')||'- 未同步行动'}\n\n## 学习卡\n${cards.length?cards.map((c,i)=>`${i+1}. ${c.title}\n   - 来源：${c.learningType||'未分类'}｜${c.source||'未写明'}\n   - 核心：${c.core||'待归纳'}\n   - 应用动作：${c.application||'待确认'}\n   - 状态：${c.status||'待确认'}`).join('\n'):'- 今日尚未上传学习笔记／会议记录，因此没有学习卡'}\n\n## 待小杰分析\n- 今日最有效的动作\n- 最大阻塞与偏差\n- 明日保留、调整、停止什么\n`}
function confirmTimeResult(){ensureTimeItems();const date=localDateKey(),items=state.timeItems.map(item=>({...item,startedAt:null})),entry={date,items};state.timeHistory=Array.isArray(state.timeHistory)?state.timeHistory:[];state.timeHistory=state.timeHistory.filter(x=>x.date!==date);state.timeHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('time',{done:4,total:4,sections:['今日时间事项','今日事项线','今日四象限','今日12小时分布'],timeItemsDone:items.filter(x=>x.done).length,timeItemsTotal:items.length,planned:items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),actual:items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),items});render();toast('今日时间管理 4 项已确认，已进入整体复盘')}
const renderReviewWithMindset=renderReviewBoard;renderReviewBoard=function(){renderReviewWithMindset();const board=$('fullReviewBoard'),date=localDateKey(),time=state.reviewInputs?.[date]?.time;if(board){const cards=board.querySelectorAll('.goal-summary .kpi b'),timeCard=cards[2];if(timeCard)timeCard.textContent=`${Number(time?.done)||0} / ${Number(time?.total)||0}项｜${Number(time?.actual)||0}分`}}
function currentDayActionPlan(){const date=localDateKey();if(date==='2026-07-26')return[{text:'参加 19:30 左右辅导员组会',category:'会议',quadrant:'紧急且重要'}];return(state.timeItems||[]).map(item=>({text:item.text,category:item.category,quadrant:item.quadrant}))}
function ensureTimeItems(){const date=localDateKey();if(state.timeSourceDate!==date||state.timeSourceVersion!==9){const todayPlan=date==='2026-07-26'?[{text:'19:30 左右辅导员组会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60}]:((new Date().getDay()===0||new Date().getDay()===6)?[]:defaultAwakeningTimeItems());state.timeItems=todayPlan.map(item=>({...item,actualMinutes:0,startedAt:null,done:false,skipped:false}));state.timeSourceDate=date;state.timeSourceVersion=9;localStorage.setItem(KEY,JSON.stringify(state))}}
function ensureActionItems(){ensureTimeItems();const date=localDateKey();if(state.actionSourceDate!==date||state.actionSourceVersion!==4){state.actionItems=currentDayActionPlan().map(item=>({...item,done:false}));state.actionSourceDate=date;state.actionSourceVersion=4;localStorage.setItem(KEY,JSON.stringify(state))}}
function confirmActionToReview(){ensureActionItems();const date=localDateKey(),entry={date,items:state.actionItems.map(x=>({...x}))};state.actionHistory=Array.isArray(state.actionHistory)?state.actionHistory:[];state.actionHistory=state.actionHistory.filter(x=>x.date!==date);state.actionHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('action',{done:entry.items.filter(x=>x.done).length,total:entry.items.length,items:entry.items});renderActionBoard();toast('今日行动已确认，已进入整体复盘')}
function fullReviewText(){const date=localDateKey(),confirmed=state.reviewInputs?.[date]||{},mindset=confirmed.mindset||{},goals=confirmed.goals||{},time=confirmed.time||{},action=confirmed.action||{},timeItems=time.items||[],actionItems=action.items||[],cards=(state.learningCards||[]).filter(x=>x.date===date&&x.confirmed),scores=(state.mindset10||[]).map((x,i)=>Number(x.score)>0?`第${i+1}项：${x.score}分${x.note?'｜'+x.note:''}`:'').filter(Boolean),actualMetrics=Object.entries(state.goalMetrics||{}).filter(([,x])=>Number(x.current)>0).map(([id,x])=>`${id}：${x.current}/${x.goal}`).join('；'),mindsetText=!mindset.confirmedAt?'- 尚未确认今日心态':`- 能量：${state.energy?.score||'未记录'}\n- 早晨状态：${state.mindset?.morningMoodScore||'未记录'}｜晚上状态：${state.mindset?.nightMoodScore||'未记录'}\n- 心理阻力：${(state.mindset?.resistances||[]).join('、')||'未记录'}\n- 心态评分：${scores.join('；')||'未记录'}\n- 调整动作与结果：${state.mindset?.mindsetAction||'未记录'}｜${state.mindset?.mindsetActionResult||'未记录'}` ,goalText=!goals.confirmedAt?'- 尚未确认今日目标':`- 今日重要事项：${(goals.items||[]).map(x=>x.text).join('、')||goals.dailyGoal||'未记录'}\n- 今日已记录指标：${actualMetrics||'暂无新增结果'}` ,timeText=!time.confirmedAt?'- 尚未确认今日时间':`- 时间管理确认：${time.done}/${time.total}项\n- 今日已排：${time.planned||0}分钟｜实际：${time.actual||0}分钟\n${timeItems.map(x=>`- ${x.text}｜${x.category}｜${x.done?'已完成':x.skipped?'未完成':'未记录'}｜实际${x.actualMinutes||0}分钟`).join('\n')}` ,actionText=!action.confirmedAt?'- 尚未确认今日行动':actionItems.map(x=>`- ${x.done?'[x]':'[ ]'} ${x.text}｜${x.category}`).join('\n'),learningText=cards.length?cards.map((c,i)=>`${i+1}. ${c.title}\n   - 来源：${c.learningType||'未分类'}｜${c.source||'未写明'}\n   - 核心：${c.core||'待归纳'}\n   - 应用动作：${c.application||'待确认'}\n   - 状态：${c.status||'待确认'}`).join('\n'):'- 今日尚未确认学习卡';return `# 今日整体复盘｜${date}\n\n## 心态管理\n${mindsetText}\n\n## 目标管理\n${goalText}\n\n## 时间管理\n${timeText}\n\n## 行动管理\n${actionText}\n\n## 学习管理\n${learningText}\n\n## 请小杰分析\n- 今天最有效的动作及原因\n- 最大阻塞、时间偏差与改进建议\n- 明天应保留、调整、停止什么\n- 根据明日目标，把明日行动逐项分入四象限并排入时间事项线\n`}
const renderReviewWithLiveTotals=renderReviewBoard;renderReviewBoard=function(){renderReviewWithLiveTotals();const board=$('fullReviewBoard'),date=localDateKey(),confirmed=state.reviewInputs?.[date]||{},cards=(state.learningCards||[]).filter(x=>x.date===date),kpis=board?.querySelectorAll('.goal-summary .kpi b');if(!kpis)return;const mindset=confirmed.mindset||{},goals=confirmed.goals||{},time=confirmed.time||{},action=confirmed.action||{},actionTotal=currentDayActionPlan().length;kpis[0].textContent=`${Number(mindset.done)||0} / 4项`;kpis[1].textContent=`${Number(goals.done)||0} / 1项`;kpis[2].textContent=`${Number(time.done)||0} / 4项｜${Number(time.actual)||0}分`;kpis[3].textContent=`${Number(action.done)||0} / ${Number(action.total)||actionTotal}项`;kpis[4].textContent=`${cards.filter(x=>x.confirmed).length} / ${cards.length}项`}
function confirmGoalsToReview(){saveGoalPlan();saveReviewInput('goals',{done:state.dailyGoal?1:0,total:1,dailyGoal:state.dailyGoal||'未生成'})}
function toggleTodayBigThree(index,checked){const date=localDateKey();state.todayBigThreeChecks=state.todayBigThreeChecks||{};state.todayBigThreeChecks[date]=state.todayBigThreeChecks[date]||[];state.todayBigThreeChecks[date][index]=Boolean(checked);localStorage.setItem(KEY,JSON.stringify(state));toast(checked?'已勾选完成':'已取消勾选')}
function confirmTodayBigThree(){const date=localDateKey(),plan=currentDayActionPlan(),base=(date==='2026-07-26'?plan:plan.filter(item=>item.category!=='会议')).slice(0,3),checks=state.todayBigThreeChecks?.[date]||[],items=base.map((item,index)=>({...item,done:Boolean(checks[index])}));state.reviewInputs=state.reviewInputs||{};state.reviewInputs[date]=state.reviewInputs[date]||{};state.reviewInputs[date].goals={done:items.filter(x=>x.done).length,total:items.length,items,confirmedAt:new Date().toISOString()};state.todayBigThreeConfirmedFor=date;localStorage.setItem(KEY,JSON.stringify(state));renderReviewBoard();toast('今日重要三件事勾选结果已确认，已进入整体复盘')}
let dashboardAutoSaveTimer;function autoSaveDashboardState(){clearTimeout(dashboardAutoSaveTimer);dashboardAutoSaveTimer=setTimeout(()=>{try{read();localStorage.setItem(KEY,JSON.stringify(state))}catch(e){}},350)}
document.addEventListener('input',autoSaveDashboardState);document.addEventListener('change',autoSaveDashboardState);
const renderSlotsWithNumbers=renderSlots;renderSlots=function(){renderSlotsWithNumbers();renderTodayTimePieChart();renderTimeQuadrantMatrix()};
function renderTimeQuadrantMatrix(){const el=$('timeQuadrants'),items=state.timeItems||[];if(!el)return;const blocks=[['紧急且重要','左上｜立即做','#fce8e4'],['重要不紧急','左下｜安排做','#e8f2ea'],['紧急不重要','右上｜减少／委托','#fff3d8'],['不紧急不重要','右下｜其他事项','#edf0f3']];el.style.display='grid';el.style.gridTemplateColumns='1fr 1fr';el.style.gap='0';el.style.border='2px solid var(--line)';el.style.borderRadius='14px';el.style.overflow='hidden';el.innerHTML=blocks.map(([key,label,bg],index)=>{const cell=items.filter(x=>x.quadrant===key),empty=key==='不紧急不重要'?'其他事项（今天暂不处理）':'今日无此类事项';return `<div style="min-height:130px;padding:14px;background:${bg};border-right:${index%2===0?'1px solid var(--line)':'0'};border-bottom:${index<2?'1px solid var(--line)':'0'}"><b>${label}</b><div style="margin-top:9px">${cell.length?cell.map(x=>`<div style="margin:5px 0;padding:6px 8px;border-radius:8px;background:#ffffffa8"><small style="display:block;color:var(--muted)">${esc(x.category)}${x.isExtra?'｜临时事项':''}</small>${esc(x.text)}</div>`).join(''):`<small style="color:var(--muted)">${empty}</small>`}</div></div>`}).join('')}
function renderTodayTimePieChart(){const el=$('timePie'),items=state.timeItems||[];if(!el)return;const colors=['#b7791f','#d65f5f','#2f855a','#805ad5','#3182ce','#d69e2e','#4a5568','#38a169','#dd6b20','#9f7aea','#718096'],data=timeCategories().map((category,index)=>({category,minutes:items.filter(x=>x.category===category).reduce((sum,x)=>sum+(Number(x.actualMinutes)||0),0),color:colors[index]})),total=data.reduce((sum,x)=>sum+x.minutes,0),planned=items.reduce((sum,x)=>sum+(Number(x.plannedMinutes)||0),0),done=items.filter(x=>x.done).length,gradient=total?`conic-gradient(${data.reduce((a,x)=>{const start=a.end,end=start+x.minutes/total*100;a.text+=`${x.color} ${start}% ${end}%,`;a.end=end;return a},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';el.innerHTML=`<div style="display:grid;grid-template-columns:220px minmax(300px,1fr);gap:28px;align-items:center"><div style="display:flex;justify-content:center"><div style="width:180px;height:180px;border-radius:50%;background:${gradient};position:relative"><div style="position:absolute;inset:44px;border-radius:50%;background:var(--card);display:flex;align-items:center;justify-content:center;text-align:center"><b>${total}<br><small>实际分钟</small></b></div></div></div><div class="goal-summary" style="grid-template-columns:repeat(3,1fr)"><div class="kpi"><span>可分配时间</span><b>12小时</b></div><div class="kpi"><span>今日已排</span><b>${planned}分</b></div><div class="kpi"><span>容量判断</span><b>${720-planned>=0?'余'+(720-planned):'超'+(planned-720)}分</b></div><div class="kpi"><span>已记录实际</span><b>${total}分</b></div><div class="kpi"><span>已完成事项</span><b>${done} / ${items.length}</b></div><div class="kpi"><span>时间执行率</span><b>${Math.min(100,Math.round(total/720*100))}%</b></div></div></div>`}
function renderReviewBoard(){const date=localDateKey(),confirmed=state.reviewInputs?.[date]||{},mindset=confirmed.mindset||{},goals=confirmed.goals||{},time=confirmed.time||{},action=confirmed.action||{},cards=(state.learningCards||[]).filter(x=>x.date===date&&x.confirmed),summary=[['心态管理',mindset],['目标管理',goals],['时间管理',time],['行动管理',action],['学习管理',{done:cards.length,total:cards.length}]],board=$('fullReviewBoard');if(!board)return;board.innerHTML=`<div class="goal-summary">${summary.map(([name,data])=>`<div class="kpi"><span>${name}</span><b>${Number(data.done)||0} / ${Number(data.total)||0}项</b></div>`).join('')}</div><div class="notice" style="margin-top:14px">上面只看今日已确认的完成情况；点“导出复盘文字”后，五项具体内容会合成为一份整体复盘并复制给小杰分析。</div>`}
function renderTodayTimePieChart(){const el=$('timePie'),items=state.timeItems||[];if(!el)return;const colors=['#b7791f','#d65f5f','#2f855a','#805ad5','#3182ce','#d69e2e','#4a5568','#38a169','#dd6b20','#9f7aea','#718096'],data=timeCategories().map((category,index)=>({category,minutes:items.filter(x=>x.category===category).reduce((sum,x)=>sum+(Number(x.actualMinutes)||0),0),color:colors[index]})),total=data.reduce((sum,x)=>sum+x.minutes,0),planned=items.reduce((sum,x)=>sum+(Number(x.plannedMinutes)||0),0),done=items.filter(x=>x.done).length,gradient=total?`conic-gradient(${data.reduce((a,x)=>{const start=a.end,end=start+x.minutes/total*100;a.text+=`${x.color} ${start}% ${end}%,`;a.end=end;return a},{text:'',end:0}).text.slice(0,-1)})`:'#edf2f7';el.innerHTML=`<div style="display:grid;grid-template-columns:220px minmax(300px,1fr);gap:28px;align-items:center"><div style="display:flex;justify-content:center"><div style="width:180px;height:180px;border-radius:50%;background:${gradient};position:relative"><div style="position:absolute;inset:44px;border-radius:50%;background:var(--card);display:flex;align-items:center;justify-content:center;text-align:center"><b>${total}<br><small>实际分钟</small></b></div></div></div><div class="goal-summary" style="grid-template-columns:repeat(3,1fr)"><div class="kpi"><span>可分配时间</span><b>12小时</b></div><div class="kpi"><span>今日已排</span><b>${planned}分</b></div><div class="kpi"><span>容量判断</span><b>${720-planned>=0?'余'+(720-planned):'超'+(planned-720)}分</b></div><div class="kpi"><span>已记录实际</span><b>${total}分</b></div><div class="kpi"><span>已完成事项</span><b>${done} / ${items.length}</b></div><div class="kpi"><span>时间执行率</span><b>${Math.min(100,Math.round(total/720*100))}%</b></div></div></div>`}
const reviewMindsetLabels=['认真','快','坚守承诺','保证完成任务','乐观','自信','爱与奉献','绝不找借口','聚焦目标','持续成长'];
function confirmMindsetToReview(){read();calcMindset10();const scores=(state.mindset10||[]).map((x,i)=>({name:reviewMindsetLabels[i],score:Number(x.score)||0,note:x.note||''})),total=scores.reduce((sum,x)=>sum+x.score,0);saveReviewInput('mindset',{done:scores.filter(x=>x.score>0).length,total:10,totalScore:total,scores,energy:state.energy?.score||0,morning:state.mindset?.morningMoodScore||'',night:state.mindset?.nightMoodScore||'',resistances:state.mindset?.resistances||[],action:state.mindset?.mindsetAction||'',actionResult:state.mindset?.mindsetActionResult||''})}
function confirmTodayBigThree(){const date=localDateKey(),plan=currentDayActionPlan(),items=(date==='2026-07-26'?plan:plan.filter(item=>item.category!=='会议')).slice(0,3);saveReviewInput('goals',{done:items.length,total:items.length,items,confirmedAt:new Date().toISOString()});state.todayBigThreeConfirmedFor=date;localStorage.setItem(KEY,JSON.stringify(state))}
function fullReviewText(){const date=localDateKey(),c=state.reviewInputs?.[date]||{},m=c.mindset||{},g=c.goals||{},t=c.time||{},a=c.action||{},cards=(state.learningCards||[]).filter(x=>x.date===date&&x.confirmed),mindset=!m.confirmedAt?'- 尚未确认':`- 能量：${m.energy||'未记录'}\n- 早晨状态：${m.morning||'未记录'}｜晚上状态：${m.night||'未记录'}\n- 心理阻力：${(m.resistances||[]).join('、')||'未记录'}\n- 十项总分：${m.totalScore||0}/100\n${(m.scores||[]).map(x=>`  - ${x.name}：${x.score}分${x.note?'｜'+x.note:''}`).join('\n')}\n- 调整动作与结果：${m.action||'未记录'}｜${m.actionResult||'未记录'}`,goals=!g.confirmedAt?'- 尚未确认':(g.items||[]).map((x,i)=>`- 第${i+1}项：${x.text}`).join('\n'),time=!t.confirmedAt?'- 尚未确认':`- 第1项：今日时间事项\n- 第2项：今日事项线\n- 第3项：今日四象限\n- 第4项：今日12小时分布\n- 已排：${t.planned||0}分钟｜实际：${t.actual||0}分钟\n${(t.items||[]).map(x=>`  - ${x.text}｜${x.category}｜${x.done?'已完成':x.skipped?'未完成':'未记录'}｜实际${x.actualMinutes||0}分钟`).join('\n')}`,action=!a.confirmedAt?'- 尚未确认':(a.items||[]).map((x,i)=>`- 第${i+1}项：${x.done?'[x]':'[ ]'} ${x.text}｜${x.category}`).join('\n'),learning=cards.length?cards.map((x,i)=>`- 第${i+1}项：${x.title}\n  来源：${x.learningType||x.source||'未分类'}\n  核心：${x.core||'待归纳'}\n  应用动作：${x.application||'待确认'}`).join('\n'):'- 尚未确认学习卡';return `# 今日整体复盘｜${date}\n\n## 心态管理（${m.done||0}/10项）\n${mindset}\n\n## 目标管理（${g.done||0}/${g.total||0}项）\n${goals}\n\n## 时间管理（${t.done||0}/4项）\n${time}\n\n## 行动管理（${a.done||0}/${a.total||0}项）\n${action}\n\n## 学习管理（${cards.length}/${cards.length}项）\n${learning}\n\n## 请小杰按结果主导分析\n- 今日实际结果与目标差距\n- 根因、保留、调整、停止\n- 迭代明日结果目标、行动、四象限与时间事项线\n`}
function ensureCanonicalLearningCard(){
  state.learningCards=Array.isArray(state.learningCards)?state.learningCards:[];
  const cards=[
    {
      id:'2026-07-22-真实需求',
      date:'2026-07-22',
      title:'穿透表象，看见真实需求',
      source:'觉醒计划腾讯会议纪要',
      learningType:'觉醒计划（腾讯会议纪要）',
      status:'待应用确认',
      core:'先倾听对方的原话和情绪，再通过提问、观察与小行动验证真实需求，不急着替对方下结论。',
      application:'选择一位真实沟通对象，完成一次“不急着给答案”的倾听，记录三句原话、一个情绪信号和一个待验证需求。',
      confirmed:false
    },
    {
      id:'2026-07-24-事业时间与陪伴思维',
      date:'2026-07-24',
      title:'把学习变成行动：分清事业时间与学习时间',
      source:'觉醒计划今日会议｜陈垚老师分享',
      learningType:'觉醒计划（课程学习）',
      status:'待应用确认',
      core:'听课和整理资料属于学习时间，真正推动结果的是发布、触达、私聊和解决问题等有效动作；经营关系要从“卖课思维”转向长期陪伴。',
      application:'当天记录有效事业动作：朋友圈4条、私聊触达至少5人、发布1条口播；复盘实际完成数量，不再用听课时长代替行动结果。',
      file:'D:/梅姐飞轮知识库/1梅姐飞轮/20260724-学习卡.md',
      confirmed:false
    },
    {
      id:'2026-07-27-共同托举',
      date:'2026-07-27',
      title:'团队协作：从单打独斗到共同托举',
      source:'觉醒计划全员复盘会议纪要',
      learningType:'觉醒计划（腾讯会议纪要）',
      status:'待应用确认',
      core:'真正的团队要围绕共同目标开展真实沟通、共同分工与相互托举；对掉队伙伴先了解事实和意愿，再决定帮扶与分工，并用过程留痕持续校准。',
      application:'未来7天，与1位活跃伙伴和1位沉默伙伴各完成一次15分钟倾听；邀请至少2位伙伴共创社群运营方案，并记录沟通和分工结果。',
      file:'D:/梅姐飞轮知识库/2026-07-27_学习卡_团队协作从单打独斗到共同托举.md',
      confirmed:false
    },
    {
      id:'2026-07-27-不淘汰后的帮扶机制',
      date:'2026-07-27',
      title:'不淘汰之后：用意愿、分工和帮扶激活团队',
      source:'觉醒计划组长碰头会-元宝纪要',
      learningType:'觉醒计划（晚间组委碰头会）',
      status:'待应用确认',
      core:'“不抛弃、不放弃”不是放任不管。先确认成员真实意愿和需求，再通过差异化分工、低门槛行动、跨组帮扶与阶段复盘，让观望者逐步参与，同时把责任、规则和利益边界说清楚。',
      application:'为1位进度较慢伙伴完成一次需求与意愿沟通；共同确定一个可在24小时内完成的小任务，记录结果，并在下一次组会复盘是否需要调整分工或增加帮扶。',
      file:'D:/梅姐飞轮知识库/2026-07-27_学习卡_不淘汰之后用意愿分工和帮扶激活团队.md',
      confirmed:false
    }
  ];
  cards.forEach(card=>{
    const existing=state.learningCards.find(item=>item.id===card.id);
    if(existing) Object.assign(existing,card,{confirmed:Boolean(existing.confirmed)});
    else state.learningCards.push(card);
  });
  state.learningCardSourceMigration='learning-library-four-cards-v2';
  localStorage.setItem(KEY,JSON.stringify(state));
}
function fullReviewText(){const date=localDateKey(),c=state.reviewInputs?.[date]||{},m=c.mindset||{},g=c.goals||{},t=c.time||{},a=c.action||{},cards=(state.learningCards||[]).filter(x=>x.date===date&&x.confirmed),scoreLines=(m.scores||[]).map(x=>`  - ${x.name}：${x.score}分${x.note?'｜'+x.note:''}`).join('\n')||'  - 未逐项记录',mindset=!m.confirmedAt?'- 未确认：不把旧页面或缓存分数当作今日事实。':`- 能量：${m.energy||'未记录'}\n- 早晨状态：${m.morning||'未记录'}｜晚上状态：${m.night||'未记录'}\n- 心理阻力：${(m.resistances||[]).join('、')||'未记录'}\n- 十项总分：${m.totalScore||0}/100\n${scoreLines}\n- 调整动作与结果：${m.action||'未记录'}｜${m.actionResult||'未记录'}`,goals=!g.confirmedAt?'- 未确认：不读取明日或旧版目标。':(g.items||[]).map((x,i)=>`- 第${i+1}项：${x.text}`).join('\n')||'- 未记录今日重要事项',time=!t.confirmedAt?'- 未确认：计划时长不等于实际时长。':`- 第1项：今日时间事项\n- 第2项：今日事项线\n- 第3项：今日四象限\n- 第4项：今日12小时分布\n- 已排：${t.planned||0}分钟｜实际：${t.actual||0}分钟\n${(t.items||[]).map(x=>`  - ${x.text}｜${x.category||'未分类'}｜${x.done?'已完成':x.skipped?'未完成':'未记录'}｜实际${x.actualMinutes||0}分钟`).join('\n')||'  - 未记录具体时间事项'}`,action=!a.confirmedAt?'- 未确认今日行动。':(a.items||[]).map((x,i)=>`- 第${i+1}项：${x.done?'[x]':'[ ]'} ${x.text}｜${x.category||'未分类'}`).join('\n')||'- 未记录具体行动',learning=cards.length?cards.map((x,i)=>`- 第${i+1}项：${x.title}\n  - 来源：${x.learningType||'未分类'}｜${x.source||'未写明'}\n  - 核心：${x.core||'待归纳'}\n  - 应用动作：${x.application||'待确认'}\n  - 状态：${x.status||'待确认'}`).join('\n'):'- 当日没有已确认学习卡（待应用确认不计入完成）。',source=date==='2026-07-26'?'- 当日主记录：Obsidian《2026-07-26_今日工作清单》\n- 规则资料：行动日志图片版结构提炼、五项管理面板改版方案\n- 注意：7月27日学习卡不倒灌到7月26日。':date==='2026-07-27'?'- 当日工作清单：Obsidian《2026-07-27 工作清单》\n- 学习卡：团队协作：从单打独斗到共同托举（待应用确认）\n- 会议纪要：辅导员组委会会议纪要。':'- 来源：当日已确认快照与同日期 Obsidian 记录。';return `# 今日整体复盘｜${date}\n\n## 心态管理（${m.done||0}/10项）\n${mindset}\n\n## 目标管理（${g.done||0}/${g.total||0}项）\n${goals}\n\n## 时间管理（${t.done||0}/4项）\n${time}\n\n## 行动管理（${a.done||0}/${a.total||0}项）\n${action}\n\n## 学习管理（${cards.length}/${cards.length}项）\n${learning}\n\n## 数据来源与状态\n${source}\n\n## 请小杰按结果主导分析\n- 今日实际结果与目标差距\n- 根因、保留、调整、停止\n- 迭代明日结果目标、行动、四象限与时间事项线\n`}
/* 最终确认快照：每个板块把可分析的具体内容一次性保存进复盘。 */
function reviewCopy(value){return JSON.parse(JSON.stringify(value??null))}
function confirmMindsetToReview(){read();calcMindset10();const scores=(state.mindset10||[]).map((x,i)=>({name:reviewMindsetLabels[i],score:Number(x.score)||0,note:x.note||''}));saveReviewInput('mindset',{done:scores.filter(x=>x.score>0).length,total:10,totalScore:scores.reduce((sum,x)=>sum+x.score,0),scores,energy:state.energy?.score||'',energyTheme:state.energy?.theme||'',energyReminder:state.energy?.reminder||'',morning:state.mindset?.morningMoodScore||'',night:state.mindset?.nightMoodScore||'',resistances:reviewCopy(state.mindset?.resistances)||[],action:state.mindset?.mindsetAction||'',actionResult:state.mindset?.mindsetActionResult||'',affirmation:state.mindset?.affirmation||''})}
function confirmTodayBigThree(){const date=localDateKey(),plan=currentDayActionPlan(),items=(date==='2026-07-26'?plan:plan.filter(item=>item.category!=='会议')).slice(0,3),metrics=Object.entries(state.goalMetrics||{}).map(([key,x])=>({key,goal:Number(x.goal)||0,actual:Number(x.current)||0,note:x.note||''}));saveReviewInput('goals',{done:items.length,total:items.length,items:reviewCopy(items),dailyGoal:state.dailyGoal||'',yearGoal:state.goalPlan?.yearTarget||'',monthFocus:state.goalPlan?.monthKeyword||'',weekFocus:state.goalPlan?.weekBigThree||'',metrics});state.todayBigThreeConfirmedFor=date;localStorage.setItem(KEY,JSON.stringify(state));toast('今日重要事项、目标层级和指标已完整保存进复盘')}
function confirmTimeResult(){ensureTimeItems();const date=localDateKey(),items=state.timeItems.map(item=>({...item,startedAt:null})),planned=items.reduce((s,x)=>s+(Number(x.plannedMinutes)||0),0),actual=items.reduce((s,x)=>s+(Number(x.actualMinutes)||0),0),entry={date,items:reviewCopy(items)};state.timeHistory=Array.isArray(state.timeHistory)?state.timeHistory:[];state.timeHistory=state.timeHistory.filter(x=>x.date!==date);state.timeHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('time',{done:4,total:4,sections:['今日时间事项','今日事项线','今日四象限','今日12小时分布'],timeItemsDone:items.filter(x=>x.done).length,timeItemsTotal:items.length,planned,actual,remaining:720-planned,executionRate:Math.min(100,Math.round(actual/720*100)),items:reviewCopy(items)});render();toast('时间事项、四象限、12小时分布和实际用时已完整保存进复盘')}
function confirmActionToReview(){ensureActionItems();const date=localDateKey(),items=state.actionItems.map(x=>({...x})),entry={date,items:reviewCopy(items)};state.actionHistory=Array.isArray(state.actionHistory)?state.actionHistory:[];state.actionHistory=state.actionHistory.filter(x=>x.date!==date);state.actionHistory.push(entry);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('action',{done:items.filter(x=>x.done).length,total:items.length,items:reviewCopy(items)});renderActionBoard();toast('每一项行动的完成状态、板块和四象限已完整保存进复盘')}
function confirmLearningToReview(){const date=localDateKey(),cards=(state.learningCards||[]).filter(x=>x.date===date);cards.forEach(x=>x.confirmed=true);localStorage.setItem(KEY,JSON.stringify(state));saveReviewInput('learning',{done:cards.length,total:cards.length,sources:reviewCopy(state.todayLearningSources)||[],cards:reviewCopy(cards)});renderLearningCards();toast('学习卡的来源、核心、应用动作和状态已完整保存进复盘')}
function fullReviewText(){const date=localDateKey(),c=state.reviewInputs?.[date]||{},m=c.mindset||{},g=c.goals||{},t=c.time||{},a=c.action||{},l=c.learning||{},scores=(m.scores||[]).map(x=>`  - ${x.name}：${x.score}分${x.note?'｜'+x.note:''}`).join('\n')||'  - 未逐项记录',mindset=m.confirmedAt?`- 能量：${m.energy||'未记录'}${m.energyTheme?'｜主题：'+m.energyTheme:''}\n- 早晨状态：${m.morning||'未记录'}｜晚上状态：${m.night||'未记录'}\n- 心理阻力：${(m.resistances||[]).join('、')||'未记录'}\n- 十项总分：${m.totalScore||0}/100\n${scores}\n- 调整动作与结果：${m.action||'未记录'}｜${m.actionResult||'未记录'}`:'- 尚未确认今日心态',goals=g.confirmedAt?`- 年度方向：${g.yearGoal||'未记录'}\n- 本月主线：${g.monthFocus||'未记录'}\n- 本周重点：${g.weekFocus||'未记录'}\n- 今日重要事项：\n${(g.items||[]).map((x,i)=>`  ${i+1}. ${x.text}｜${x.category||'未分类'}｜${x.quadrant||'未分象限'}`).join('\n')||'  未记录'}\n- 今日指标：${(g.metrics||[]).map(x=>`${x.key} ${x.actual}/${x.goal}`).join('；')||'未记录'}`:'- 尚未确认今日目标',time=t.confirmedAt?`- 已确认模块：${(t.sections||[]).join('、')}\n- 已排：${t.planned||0}分钟｜实际：${t.actual||0}分钟｜剩余容量：${t.remaining??'未记录'}分钟｜执行率：${t.executionRate??'未记录'}%\n- 时间事项：\n${(t.items||[]).map(x=>`  - ${x.text}｜${x.category||'未分类'}｜${x.quadrant||'未分象限'}｜计划${x.plannedMinutes||0}分｜实际${x.actualMinutes||0}分｜${x.done?'已完成':x.skipped?'未完成':'未记录'}`).join('\n')||'  未记录'}`:'- 尚未确认今日时间',action=a.confirmedAt?`- 今日行动：\n${(a.items||[]).map(x=>`  - ${x.done?'[x]':'[ ]'} ${x.text}｜${x.category||'未分类'}｜${x.quadrant||'未分象限'}`).join('\n')||'  未记录'}`:'- 尚未确认今日行动',learning=l.confirmedAt?(l.cards||[]).map((x,i)=>`- 第${i+1}项：${x.title}\n  - 来源：${x.learningType||'未分类'}｜${x.source||'未写明'}\n  - 核心：${x.core||'待归纳'}\n  - 应用动作：${x.application||'待确认'}\n  - 状态：${x.status||'待确认'}`).join('\n')||'- 已确认：今天没有学习卡':'- 尚未确认今日学习';return `# 今日整体复盘｜${date}\n\n## 心态管理（${m.done||0}/10项）\n${mindset}\n\n## 目标管理（${g.done||0}/${g.total||0}项）\n${goals}\n\n## 时间管理（${t.done||0}/4项）\n${time}\n\n## 行动管理（${a.done||0}/${a.total||0}项）\n${action}\n\n## 学习管理（${l.done||0}/${l.total||0}项）\n${learning}\n\n## 请小杰按结果主导分析\n- 今日实际结果与目标差距\n- 根因、保留、调整、停止\n- 迭代明日结果目标、行动、四象限与时间事项线\n`}

/* 心态管理按本地日历日独立保存。迁移顺序：先归档旧全局值，再载入/创建今日值。 */
function localDateKey(date=new Date()){
  const y=date.getFullYear(),m=String(date.getMonth()+1).padStart(2,'0'),d=String(date.getDate()).padStart(2,'0');
  return y+'-'+m+'-'+d;
}
function formatMindsetDate(date=new Date()){
  return date.getFullYear()+'年'+(date.getMonth()+1)+'月'+date.getDate()+'日';
}
function copyMindsetValue(value){return JSON.parse(JSON.stringify(value??null))}
function hasMindsetContent(snapshot){
  return Boolean(snapshot?.energyRaw||snapshot?.energy?.raw||snapshot?.energy?.score||snapshot?.affirmation||
    snapshot?.mindsetAffirmationConfirmed||snapshot?.mindsetEnergyScore||
    snapshot?.mindset10?.some(x=>Number(x?.score)>0||x?.note)||
    Object.values(snapshot?.mindset||{}).some(value=>Array.isArray(value)?value.length:Boolean(value)));
}
function captureMindsetSnapshot(date){
  const scoreInput=$('mindsetEnergyScore');
  if(scoreInput&&scoreInput.value!=='') state.energy={...(state.energy||{}),score:Number(scoreInput.value)};
  return {
    date,
    energyRaw:state.energyRaw||state.energy?.raw||'',
    energy:copyMindsetValue(state.energy||{}),
    mindset:copyMindsetValue(state.mindset||{resistances:[]}),
    mindset10:copyMindsetValue(state.mindset10||[]),
    affirmation:state.mindset?.affirmation||'',
    mindsetAffirmationConfirmed:Boolean(state.mindsetAffirmationConfirmed),
    mindsetEnergyScore:scoreInput?.value!==''?Number(scoreInput.value):(state.energy?.score??''),
    reviewSnapshot:copyMindsetValue(state.reviewInputs?.[date]?.mindset||null),
    savedAt:new Date().toISOString()
  };
}
function persistMindsetDaily(){
  const date=localDateKey();
  state.mindsetDaily=state.mindsetDaily||{};
  state.mindsetDaily[date]=captureMindsetSnapshot(date);
  state.mindsetActiveDate=date;
  localStorage.setItem(KEY,JSON.stringify(state));
  updateMindsetArchiveStatus();
}
function previousMindsetRecordDate(today){
  const candidates=[
    ...Object.keys(state.reviewInputs||{}),
    ...Object.keys(state.fullReviews||{}),
    ...Object.keys(state.mindsetDaily||{})
  ].filter(date=>date<today).sort();
  if(candidates.length)return candidates[candidates.length-1];
  const yesterday=new Date();yesterday.setDate(yesterday.getDate()-1);return localDateKey(yesterday);
}
function initializeMindsetDaily(){
  const today=localDateKey();
  state.mindsetDaily=state.mindsetDaily||{};
  const legacyDate=state.mindsetActiveDate&&state.mindsetActiveDate!==today
    ? state.mindsetActiveDate
    : previousMindsetRecordDate(today);
  if(!state.mindsetDaily[legacyDate]){
    const legacy=captureMindsetSnapshot(legacyDate);
    if(hasMindsetContent(legacy)||legacy.reviewSnapshot){
      legacy.archivedAt=new Date().toISOString();
      legacy.archiveReason='首次启用按日期心态记录前，保全原页面已有值';
      state.mindsetDaily[legacyDate]=legacy;
    }
  }
  const todayRecord=state.mindsetDaily[today];
  if(todayRecord){
    state.energyRaw=todayRecord.energyRaw||'';
    state.energy=copyMindsetValue(todayRecord.energy||{});
    state.mindset=copyMindsetValue(todayRecord.mindset||{resistances:[]});
    state.mindset10=copyMindsetValue(todayRecord.mindset10||[]);
    state.mindsetAffirmationConfirmed=Boolean(todayRecord.mindsetAffirmationConfirmed);
  }else{
    state.energyRaw='';
    state.energy={};
    state.mindset={resistances:[]};
    state.mindset10=[];
    state.mindsetAffirmationConfirmed=false;
  }
  state.mindsetActiveDate=today;
  localStorage.setItem(KEY,JSON.stringify(state));
}
function updateMindsetArchiveStatus(){
  const today=localDateKey(),dates=Object.keys(state.mindsetDaily||{}).filter(date=>date!==today).sort();
  if($('mindsetCurrentDate'))$('mindsetCurrentDate').textContent=formatMindsetDate();
  if($('mindsetArchiveStatus'))$('mindsetArchiveStatus').textContent=dates.length
    ? '历史心态已保存在浏览器本地档案：mei_five_management_v1 → mindsetDaily → '+dates.join('、')
    : '尚无历史心态记录；今天保存后会按日期独立归档。';
}
const parseEnergyBeforeDailyArchive=parseEnergy;
parseEnergy=function(){
  const manual=$('mindsetEnergyScore')?.value;
  parseEnergyBeforeDailyArchive();
  if(manual!=='')state.energy={...(state.energy||{}),score:Number(manual)};
  else if($('mindsetEnergyScore')&&state.energy?.score!=='')$('mindsetEnergyScore').value=state.energy.score??'';
  persistMindsetDaily();
};
const calcMindset10BeforeDailyArchive=calcMindset10;
calcMindset10=function(){calcMindset10BeforeDailyArchive();persistMindsetDaily()};
const recordMindsetTotalBeforeDailyArchive=recordMindsetTotal;
recordMindsetTotal=function(){recordMindsetTotalBeforeDailyArchive();persistMindsetDaily()};
function confirmAffirmation(){
  state.mindset=state.mindset||{};
  state.mindset.affirmation=$('affirmation')?.textContent||'';
  state.mindsetAffirmationConfirmed=true;
  persistMindsetDaily();
  toast('今日肯定语已确认并保存');
}
const confirmMindsetToReviewBeforeDailyArchive=confirmMindsetToReview;
confirmMindsetToReview=function(){
  read();calcMindset10BeforeDailyArchive();
  const manual=$('mindsetEnergyScore')?.value;
  if(manual!=='')state.energy={...(state.energy||{}),score:Number(manual)};
  persistMindsetDaily();
  confirmMindsetToReviewBeforeDailyArchive();
  persistMindsetDaily();
};
function renderMindsetDailyFields(){
  updateMindsetArchiveStatus();
  if($('mindsetEnergyScore'))$('mindsetEnergyScore').value=state.energy?.score??'';
  if($('energyRaw'))$('energyRaw').value=state.energyRaw||state.energy?.raw||'';
  if($('affirmation')&&state.mindset?.affirmation)$('affirmation').textContent=state.mindset.affirmation;
}

/* 时间管理按日期独立保存。旧版本号不再覆盖梅姐当天已经操作过的记录。 */
function timeCopy(value){return JSON.parse(JSON.stringify(value??null))}
function defaultTimeItemsForDate(date){
  if(date==='2026-07-26')return [{text:'19:30 左右辅导员组会',quadrant:'紧急且重要',category:'会议',plannedMinutes:60}];
  const day=new Date(date+'T12:00:00').getDay();
  return day===0||day===6?[]:defaultAwakeningTimeItems();
}
function timeCompletion(items=state.timeItems||[]){
  const hasItems=items.length>0;
  const quadrantsReady=hasItems&&items.every(item=>Boolean(item.quadrant&&item.category));
  const resultsReady=hasItems&&items.every(item=>item.done||item.skipped);
  const actualReady=hasItems&&items.filter(item=>item.done).every(item=>Number(item.actualMinutes)>0);
  const checks=[
    {name:'今日时间事项',done:hasItems},
    {name:'今日事项线与四象限',done:quadrantsReady},
    {name:'每项执行结果',done:resultsReady},
    {name:'实际用时与12小时分布',done:actualReady}
  ];
  return {checks,done:checks.filter(item=>item.done).length,total:checks.length};
}
function persistTimeDaily(){
  const date=localDateKey(),items=timeCopy(state.timeItems||[]);
  state.timeDaily=state.timeDaily||{};
  state.timeDaily[date]={date,items,savedAt:new Date().toISOString()};
  state.timeActiveDate=date;
  localStorage.setItem(KEY,JSON.stringify(state));
  renderTimeDailyStatus();
}
function initializeTimeDaily(){
  const date=localDateKey();
  state.timeDaily=state.timeDaily||{};
  const saved=state.timeDaily[date];
  if(saved?.items){state.timeItems=timeCopy(saved.items)}else{
    const sameDayLegacy=state.timeSourceDate===date&&Array.isArray(state.timeItems);
    state.timeItems=timeCopy(sameDayLegacy?state.timeItems:defaultTimeItemsForDate(date)).map(item=>({...item,actualMinutes:Number(item.actualMinutes)||0,startedAt:item.startedAt||null,completedAt:item.completedAt||null,done:Boolean(item.done),skipped:Boolean(item.skipped)}));
    persistTimeDaily();
  }
  state.timeSourceDate=date;state.timeActiveDate=date;
  localStorage.setItem(KEY,JSON.stringify(state));
}
ensureTimeItems=function(){const date=localDateKey();if(state.timeActiveDate!==date||!Array.isArray(state.timeItems))initializeTimeDaily()};
function renderTimeDailyStatus(){
  const el=$('timeDailyStatus');if(!el)return;const progress=timeCompletion();
  el.innerHTML=`<span class="pill">${localDateKey()} 已自动保存</span>`+progress.checks.map(item=>`<span class="pill">${item.done?'✓':'○'} ${item.name}</span>`).join('')+`<span class="pill">完整度 ${progress.done}/${progress.total}</span>`;
}
toggleTimeItem=function(index){
  ensureTimeItems();const item=state.timeItems[index];if(!item||item.done)return;
  if(!item.startedAt){item.startedAt=Date.now();item.completedAt=null;item.skipped=false;toast('已记录开始时间 '+formatTimeClock(item.startedAt))}else{item.completedAt=Date.now();item.actualMinutes=Math.max(1,Math.round((item.completedAt-item.startedAt)/60000));item.done=true;item.skipped=false;toast('已完成并保存，实际 '+item.actualMinutes+' 分钟')}
  persistTimeDaily();renderSlots();
};
markTimeUnfinished=function(index){ensureTimeItems();const item=state.timeItems[index];if(!item)return;item.startedAt=null;item.completedAt=null;item.actualMinutes=0;item.done=false;item.skipped=true;persistTimeDaily();renderSlots();toast('已记录未完成并保存')};
restoreTimeItem=function(index){ensureTimeItems();const item=state.timeItems[index];if(!item)return;item.skipped=false;item.done=false;item.startedAt=null;item.completedAt=null;item.actualMinutes=0;persistTimeDaily();renderSlots();toast('已恢复为待执行并保存')};
function setTimeActualMinutes(index,value){ensureTimeItems();const item=state.timeItems[index];if(!item)return;item.actualMinutes=Math.max(0,Number(value)||0);persistTimeDaily();renderSlots();toast('实际用时已更新并保存')}
const renderSlotsBeforeDailyArchive=renderSlots;
renderSlots=function(){
  ensureTimeItems();renderSlotsBeforeDailyArchive();
  document.querySelectorAll('#slots .slot').forEach((card,index)=>{const item=state.timeItems[index],row=card.querySelector('.row');if(!item||!row)return;const editor=document.createElement('label');editor.className='pill';editor.innerHTML=`实际 <input type="number" min="0" value="${Number(item.actualMinutes)||0}" style="width:68px;border:0;background:transparent" onchange="setTimeActualMinutes(${index},this.value)"> 分`;row.prepend(editor)});
  renderTimeDailyStatus();
};
confirmTimeResult=function(){
  ensureTimeItems();const date=localDateKey(),items=timeCopy(state.timeItems).map(item=>({...item,startedAt:null}));
  const planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),progress=timeCompletion(items);
  const entry={date,items:timeCopy(items),completeness:progress.done,savedAt:new Date().toISOString()};
  state.timeHistory=Array.isArray(state.timeHistory)?state.timeHistory:[];state.timeHistory=state.timeHistory.filter(item=>item.date!==date);state.timeHistory.push(entry);persistTimeDaily();
  saveReviewInput('time',{done:progress.done,total:progress.total,sections:progress.checks.filter(item=>item.done).map(item=>item.name),missingSections:progress.checks.filter(item=>!item.done).map(item=>item.name),timeItemsDone:items.filter(item=>item.done).length,timeItemsTotal:items.length,planned,actual,remaining:720-actual,executionRate:planned?Math.min(100,Math.round(actual/planned*100)):0,items:timeCopy(items)});
  render();toast(progress.done===progress.total?'今日时间管理已完整保存进复盘':`时间记录已保存，当前完整度 ${progress.done}/${progress.total}`);
};


/* 统一每日留痕：面板按日期暂存，Obsidian 是唯一长期主记录。 */
const DAILY_RECORD_VERSION=1;
const DAILY_REVIEW_FIELD_IDS=['dailyEffectiveAction','dailyRootCause','dailyTomorrowKeep','dailyTomorrowAdjust','dailyTomorrowStop','dailyUnfinishedReason'];
function dailyCopy(value){return value==null?value:JSON.parse(JSON.stringify(value))}
function dailyClock(iso){if(!iso)return '';try{return new Date(iso).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit'})}catch(e){return ''}}
function dailyDatedSources(date){
  const review=dailyCopy(state.reviewInputs?.[date]||{}),timeEntry=(state.timeHistory||[]).find(x=>x.date===date),actionEntry=(state.actionHistory||[]).find(x=>x.date===date);
  const mindset=dailyCopy(state.mindsetDaily?.[date]||review.mindset||null),time=dailyCopy(state.timeDaily?.[date]||timeEntry||review.time||null),action=dailyCopy(actionEntry||review.action||null);
  const learningCards=dailyCopy((state.learningCards||[]).filter(x=>x.date===date));
  return {date,version:DAILY_RECORD_VERSION,mindset,goals:dailyCopy(review.goals||null),time,action,learning:{...dailyCopy(review.learning||{}),cards:learningCards},confirmations:review};
}
function captureUnifiedDailyRecord(){
  read();const date=localDateKey(),old=state.dailyRecords?.[date]||{},review=dailyCopy(state.reviewInputs?.[date]||{});
  const extras=Object.fromEntries(DAILY_REVIEW_FIELD_IDS.map(id=>[id,$(id)?.value||old.extras?.[id]||'']));
  const record={...dailyDatedSources(date),...old,date,version:DAILY_RECORD_VERSION,updatedAt:new Date().toISOString(),dirtyAfterArchive:Boolean(old.archivedAt),extras,
    mindset:dailyCopy(state.mindsetDaily?.[date]||captureMindsetSnapshot?.(date)||state.mindset||null),
    goals:dailyCopy(review.goals||{dailyGoal:state.dailyGoal||'',items:state.tasks||[],metrics:state.goalMetrics||{}}),
    time:dailyCopy(state.timeDaily?.[date]||{date,items:state.timeItems||[]}),
    action:dailyCopy(review.action||{items:state.actionItems||[],done:(state.actionItems||[]).filter(x=>x.done).length,total:(state.actionItems||[]).length}),
    learning:{...dailyCopy(review.learning||{}),cards:dailyCopy((state.learningCards||[]).filter(x=>x.date===date))},
    confirmations:review,
    tomorrow:{goal:state.tomorrowGrowth||'',items:[state.tomorrow0||'',state.tomorrow1||'',state.tomorrow2||''].filter(Boolean)}
  };
  return record;
}
function persistUnifiedDailyRecord(silent){
  state.dailyRecords=state.dailyRecords||{};const record=captureUnifiedDailyRecord();state.dailyRecords[record.date]=record;localStorage.setItem(KEY,JSON.stringify(state));renderDailyArchiveStatus();if(!silent)toast('今日内容已按日期暂存');
  return record;
}
function migrateUnifiedDailyRecords(){
  state.dailyRecords=state.dailyRecords||{};const dates=new Set([...Object.keys(state.mindsetDaily||{}),...Object.keys(state.timeDaily||{}),...Object.keys(state.reviewInputs||{}),(state.timeHistory||[]).map(x=>x.date),(state.actionHistory||[]).map(x=>x.date),(state.learningCards||[]).map(x=>x.date)].flat().filter(Boolean));
  dates.forEach(date=>{if(!state.dailyRecords[date])state.dailyRecords[date]=dailyDatedSources(date)});
  localStorage.setItem(KEY,JSON.stringify(state));
}
function dailyItems(record,kind){const value=record?.[kind];return Array.isArray(value?.items)?value.items:Array.isArray(value)?value:[]}
function dailyRecordMarkdown(record){
  const date=record.date,m=record.mindset||{},g=record.goals||{},timeItems=dailyItems(record,'time'),actionItems=dailyItems(record,'action'),cards=record.learning?.cards||[],ex=record.extras||{};
  const goalItems=(g.items||[]).length?g.items:(record.confirmations?.goals?.items||[]);
  const planned=timeItems.reduce((sum,x)=>sum+(Number(x.plannedMinutes)||0),0),actual=timeItems.reduce((sum,x)=>sum+(Number(x.actualMinutes)||0),0);
  const scores=m.scores||m.mindset10||[],resistances=m.resistances||m.mindset?.resistances||[];
  const status=name=>record.confirmations?.[name]?.confirmedAt?'已确认':record[name]?'草稿/待确认':'未记录',parts=[];
  parts.push('# 每日复盘｜'+date,'','> 五项管理面板生成；本文件保存心态、目标、时间、行动、学习复盘。最后更新：'+new Date(record.updatedAt||Date.now()).toLocaleString('zh-CN'),'');
  parts.push('## 今日目标及三件重要事项（'+status('goals')+'）','- 今日目标：'+(g.dailyGoal||record.confirmations?.goals?.dailyGoal||'未记录'));
  parts.push(goalItems.length?goalItems.slice(0,3).map((x,i)=>(i+1)+'. ['+(x.done?'x':' ')+'] '+(x.text||x.title||'未命名事项')).join('\n'):'- 未记录三件重要事项','');
  parts.push('## 实际行动与完成结果（'+status('action')+'）',actionItems.length?actionItems.map(x=>'- ['+(x.done?'x':' ')+'] '+(x.text||'未命名行动')+'｜'+(x.category||'未分类')+(x.done?'':'｜未完成原因：'+(ex.dailyUnfinishedReason||x.reason||'待补充'))).join('\n'):'- 未记录行动','');
  parts.push('## 时间计划、实际与偏差（'+status('time')+'）','- 计划：'+planned+'分钟','- 实际：'+actual+'分钟','- 偏差：'+(actual-planned)+'分钟',timeItems.length?timeItems.map(x=>'- '+(x.text||'未命名事项')+'｜计划'+(x.plannedMinutes||0)+'分｜实际'+(x.actualMinutes||0)+'分｜'+(x.done?'已完成':x.skipped?'未完成':'待确认')).join('\n'):'- 未记录时间事项','');
  parts.push('## 心态状态、阻力与调整（'+status('mindset')+'）','- 能量：'+(m.energy?.score||m.energyScore||m.externalEnergyScore||'未记录'),'- 早晨状态：'+(m.morningMoodScore||m.mindset?.morningMoodScore||'未记录'),'- 晚上状态：'+(m.nightMoodScore||m.mindset?.nightMoodScore||'未记录'),'- 心理阻力：'+(resistances.join('、')||'未记录'),'- 十项评分：'+(scores.length?scores.map((x,i)=>(i+1)+':'+(x.score??x)).join('；'):'未记录'),'- 调整动作：'+(m.mindsetAction||m.mindset?.mindsetAction||'未记录'),'- 调整结果：'+(m.mindsetActionResult||m.mindset?.mindsetActionResult||'未记录'),'');
  parts.push('## 学习内容、应用动作与结果（'+status('learning')+'）',cards.length?cards.map((x,i)=>(i+1)+'. '+(x.title||'未命名学习卡')+'\n   - 核心：'+(x.core||'待归纳')+'\n   - 应用动作：'+(x.application||'待确认')+'\n   - 应用结果：'+(x.result||x.status||'待确认')).join('\n'):'- 未记录学习卡','');
  parts.push('## 今日复盘','- 今日最有效的动作：'+(ex.dailyEffectiveAction||'待复盘'),'- 问题根因：'+(ex.dailyRootCause||'待复盘'),'- 未完成原因：'+(ex.dailyUnfinishedReason||'待复盘'),'');
  parts.push('## 明日调整','- 保留：'+(ex.dailyTomorrowKeep||'待复盘'),'- 调整：'+(ex.dailyTomorrowAdjust||'待复盘'),'- 停止：'+(ex.dailyTomorrowStop||'待复盘'),'- 明日三件事：',(record.tomorrow?.items||[]).length?record.tomorrow.items.slice(0,3).map((x,i)=>(i+1)+'. '+x).join('\n'):'1. 待确认\n2. 待确认\n3. 待确认','');
  return parts.join('\n');
}
function renderDailyArchiveStatus(){
  const date=localDateKey(),record=state.dailyRecords?.[date],cache=$('dailyCacheStatus'),archive=$('archiveStatus');
  if(cache)cache.textContent=record?'面板已暂存 '+dailyClock(record.updatedAt):'面板尚未暂存';
  if(archive)archive.textContent=!record?.archivedAt?'Obsidian：待归档':record.dirtyAfterArchive?'Obsidian：归档后有新修改':'已归档 '+dailyClock(record.archivedAt);
}
function renderDailyRecordHistory(date){
  const record=state.dailyRecords?.[date],view=$('historyRecordView'),status=$('historyRecordStatus');if(!view||!status)return;
  if(!record){status.textContent='该日期无面板日档';view.textContent='没有找到 '+date+' 的面板暂存记录。请以 Obsidian 每日复盘为准。';return}
  status.textContent=record.archivedAt?'已归档到 Obsidian':'仅面板暂存';view.textContent=dailyRecordMarkdown(record);
}
async function resolveDailyLogDirectory(){
  if(!obsidianVaultHandle)throw new Error('尚未连接 Obsidian Vault');let root=obsidianVaultHandle;
  try{await root.getDirectoryHandle('40_数据与复盘')}catch(e){root=await root.getDirectoryHandle('梅姐长期记忆')}
  const data=await root.getDirectoryHandle('40_数据与复盘',{create:true});return await data.getDirectoryHandle('每日日志',{create:true});
}
async function resolveDailyReviewDirectory(){
  if(!obsidianVaultHandle)throw new Error('尚未连接 Obsidian Vault');let root=obsidianVaultHandle;
  try{await root.getDirectoryHandle('40_数据与复盘')}catch(e){root=await root.getDirectoryHandle('梅姐长期记忆')}
  const data=await root.getDirectoryHandle('40_数据与复盘',{create:true});return await data.getDirectoryHandle('每日复盘',{create:true});
}
async function archiveDailyRecordToObsidian(){
  const record=persistUnifiedDailyRecord(true),date=record.date,compact=date.replace(/-/g,''),blockStart='<!-- MEI_DASHBOARD_DAILY_START -->',blockEnd='<!-- MEI_DASHBOARD_DAILY_END -->';
  if(!obsidianVaultHandle){if(!window.showDirectoryPicker)throw new Error('当前浏览器不支持目录连接');obsidianVaultHandle=await window.showDirectoryPicker()}
  const dir=await resolveDailyReviewDirectory(),handle=await dir.getFileHandle('每日复盘_'+compact+'.md',{create:true}),file=await handle.getFile(),old=await file.text(),block=blockStart+'\n'+dailyRecordMarkdown(record)+'\n'+blockEnd;
  let next;if(old.includes(blockStart)&&old.includes(blockEnd)){const start=old.indexOf(blockStart),end=old.indexOf(blockEnd,start)+blockEnd.length;next=old.slice(0,start)+block+old.slice(end)}else next=(old.trim()?old.trim()+'\n\n':'')+block+'\n';
  const writer=await handle.createWritable();await writer.write(next);await writer.close();
  record.archivedAt=new Date().toISOString();record.dirtyAfterArchive=false;record.archivePath='40_数据与复盘/每日复盘/每日复盘_'+compact+'.md';state.dailyRecords[date]=record;state.dailyArchiveStatus=state.dailyArchiveStatus||{};state.dailyArchiveStatus[date]={status:'archived',archivedAt:record.archivedAt,path:record.archivePath};localStorage.setItem(KEY,JSON.stringify(state));renderDailyArchiveStatus();
  if($('syncStatus'))$('syncStatus').textContent='已归档 '+dailyClock(record.archivedAt);return record;
}
connectObsidianVault=async function(){if(!window.showDirectoryPicker){toast('当前浏览器不支持直接连接 Vault');return}try{obsidianVaultHandle=await window.showDirectoryPicker();if($('syncStatus'))$('syncStatus').textContent='Vault 已连接，今晚可归档';toast('Obsidian Vault 已连接')}catch(e){toast('已取消连接')}}

/* 目标—行动共用同一日计划：目标定义结果，行动记录执行。 */
function goalActionSourceItems(){
  const date=localDateKey(),reportItems=state.reportGeneratedActions?.[date]||[],confirmed=state.reviewInputs?.[date]?.goals?.items||[],tasks=(state.tasks||[]).filter(x=>String(x.text||'').trim());
  const yesterday=new Date(date+'T00:00:00');yesterday.setDate(yesterday.getDate()-1);const yesterdayKey=yesterday.getFullYear()+'-'+String(yesterday.getMonth()+1).padStart(2,'0')+'-'+String(yesterday.getDate()).padStart(2,'0'),previous=state.dailyRecords?.[yesterdayKey],tomorrowItems=previous?.archivedAt?(previous.tomorrow?.items||[]):[];
  const normalize=(items,source)=>items.map(x=>({text:String(typeof x==='string'?x:x.text||x.title||'').trim(),category:x.category||'重点工作',quadrant:x.quadrant||'重要不紧急',result:x.result||'',reason:x.reason||'',source})).filter(x=>x.text);
  if(reportItems.length)return normalize(reportItems,'今日工作报告');
  if(tomorrowItems.length)return normalize(tomorrowItems,'昨日已归档复盘');
  if(tasks.length)return normalize(tasks,'Obsidian今日工作清单');
  if(confirmed.length)return normalize(confirmed,'今日已确认目标');
  return [];
}
function ensureGoalActionAlignment(){
  const date=localDateKey(),source=goalActionSourceItems();state.goalActionDaily=state.goalActionDaily||{};const old=state.goalActionDaily[date]||{goals:[],actions:[]};
  const goals=source.map((item,index)=>{const prior=old.goals[index]||{};return {...prior,id:prior.id||date+'-G'+(index+1),text:item.text,category:item.category,quadrant:item.quadrant,source:item.source,order:index+1}});
  const actions=goals.map((goal,index)=>{const item=source[index]||{},prior=old.actions.find(x=>x.goalId===goal.id)||old.actions[index]||{};return {...prior,id:prior.id||date+'-A'+(index+1),goalId:goal.id,text:prior.text||item.actionText||goal.text,category:goal.category,quadrant:goal.quadrant,source:prior.source||item.source||goal.source,done:Boolean(prior.done),result:item.result||prior.result||'',reason:item.reason||prior.reason||''}});
  state.goalActionDaily[date]={date,goals,actions,updatedAt:new Date().toISOString()};state.actionItems=actions;state.actionSourceDate=date;localStorage.setItem(KEY,JSON.stringify(state));return state.goalActionDaily[date];
}
currentDayActionPlan=function(){return ensureGoalActionAlignment().actions.map(x=>({...x}))};
ensureActionItems=function(){ensureGoalActionAlignment()};
function renderGoalActionAlignment(){
  const plan=ensureGoalActionAlignment(),goalEl=$('todayBigThreeView'),actionEl=$('todayActionChecklist');
  if(goalEl)goalEl.innerHTML=plan.goals.length?plan.goals.map((goal,index)=>{const action=plan.actions.find(x=>x.goalId===goal.id)||{};return '<div class="goal-step" style="text-align:left"><span class="tag">目标'+(index+1)+'</span><b style="display:block;margin:8px 0">'+esc(goal.text)+'</b><small>来源：'+esc(goal.source||'工作报告')+'｜对应行动：'+esc(action.text||'尚未生成')+'｜'+(action.done?'已完成':'待执行')+'</small></div>'}).join(''):'<div class="notice">今日目标尚未从昨日复盘或 Obsidian 工作报告生成。</div>';
  if(actionEl)actionEl.innerHTML=plan.actions.length?'<div class="notice" style="margin-bottom:12px">行动由昨日已归档复盘或今日工作报告自动生成；这里只查看、执行和勾选，不手写行动。</div>'+plan.actions.map((action,index)=>{const goal=plan.goals.find(x=>x.id===action.goalId)||{};return '<div class="slot"><span class="tag">目标'+(index+1)+'</span><div><b>'+esc(action.text||'尚未生成行动')+'</b><small style="display:block;color:var(--muted)">对应结果：'+esc(goal.text||'未命名目标')+'｜来源：'+esc(action.source||'工作报告')+(action.result?'｜报告结果：'+esc(action.result):'')+(action.reason?'｜报告原因：'+esc(action.reason):'')+'</small></div><button class="btn '+(action.done?'success':'soft')+'" type="button" onclick="toggleAlignedAction('+index+')">'+(action.done?'✓ 已完成':'○ 勾选完成')+'</button></div>'}).join(''):'<div class="notice">尚未从昨日复盘或今日工作报告生成行动，不自动编造，也无需手写。</div>';
}
function toggleAlignedAction(index){const plan=ensureGoalActionAlignment(),item=plan.actions[index];if(!item)return;item.done=!item.done;if(item.done)item.reason='';plan.updatedAt=new Date().toISOString();state.actionItems=plan.actions;localStorage.setItem(KEY,JSON.stringify(state));renderGoalActionAlignment();renderDailyArchiveStatus()}
function setAlignedActionField(index,key,value){const plan=ensureGoalActionAlignment(),item=plan.actions[index];if(!item||!['text','result','reason'].includes(key))return;item[key]=value;plan.updatedAt=new Date().toISOString();state.actionItems=plan.actions;localStorage.setItem(KEY,JSON.stringify(state));persistUnifiedDailyRecord(true)}
renderTodayActionChecklist=function(){renderGoalActionAlignment()};
confirmTodayBigThree=function(){const date=localDateKey(),plan=ensureGoalActionAlignment(),items=plan.goals.map(goal=>{const action=plan.actions.find(x=>x.goalId===goal.id)||{};return {...goal,done:Boolean(action.done),actionId:action.id,actionText:action.text,result:action.result||'',reason:action.reason||''}}),metrics=Object.entries(state.goalMetrics||{}).map(([key,x])=>({key,goal:Number(x.goal)||0,actual:Number(x.current)||0,note:x.note||''}));saveReviewInput('goals',{done:items.filter(x=>x.done).length,total:items.length,items:reviewCopy(items),dailyGoal:state.dailyGoal||'',yearGoal:state.goalPlan?.yearTarget||'',monthFocus:state.goalPlan?.monthKeyword||'',weekFocus:state.goalPlan?.weekBigThree||'',metrics});toast('今日目标已按对应行动结果保存进复盘')};
confirmActionToReview=function(){const date=localDateKey(),plan=ensureGoalActionAlignment(),items=reviewCopy(plan.actions),entry={date,items};state.actionHistory=Array.isArray(state.actionHistory)?state.actionHistory.filter(x=>x.date!==date):[];state.actionHistory.push(entry);saveReviewInput('action',{done:items.filter(x=>x.done).length,total:items.length,items});confirmTodayBigThree();localStorage.setItem(KEY,JSON.stringify(state));renderActionBoard();renderGoalActionAlignment();toast('行动结果已回写对应目标复盘')};
const renderBeforeGoalActionAlignment=render;render=function(){renderBeforeGoalActionAlignment();renderGoalActionAlignment()};


/* 晚间闭环：五板块自动复盘 → 导出分析 → 确认总结 → Obsidian 归档。 */
function eveningStatus(confirmed,data){return confirmed?.confirmedAt?'已确认':data?'草稿/待确认':'未记录'}
function eveningReviewModel(record){
  const date=record.date,m=record.mindset||{},g=record.goals||{},timeItems=dailyItems(record,'time'),actionItems=dailyItems(record,'action'),cards=record.learning?.cards||[],c=record.confirmations||{};
  const goals=(g.items||c.goals?.items||[]),planned=timeItems.reduce((sum,x)=>sum+(Number(x.plannedMinutes)||0),0),actual=timeItems.reduce((sum,x)=>sum+(Number(x.actualMinutes)||0),0),resistances=m.resistances||m.mindset?.resistances||[];
  return [
    {key:'mindset',name:'心态管理',status:eveningStatus(c.mindset,m),text:'能量：'+(m.energy?.score||m.energyScore||m.externalEnergyScore||'未记录')+'；早晨：'+(m.morningMoodScore||m.mindset?.morningMoodScore||'未记录')+'；晚上：'+(m.nightMoodScore||m.mindset?.nightMoodScore||'未记录')+'；阻力：'+(resistances.join('、')||'未记录')+'；调整动作：'+(m.mindsetAction||m.mindset?.mindsetAction||'未记录')+'；结果：'+(m.mindsetActionResult||m.mindset?.mindsetActionResult||'未记录')},
    {key:'goals',name:'目标管理',status:eveningStatus(c.goals,g),text:goals.length?goals.map((x,i)=>(i+1)+'. '+(x.text||x.title||'未命名目标')+'｜'+(x.done?'完成':'未完成')+(x.result?'｜结果：'+x.result:'')+(x.reason?'｜原因：'+x.reason:'')).join('\n'):'今日目标未记录'},
    {key:'time',name:'时间管理',status:eveningStatus(c.time,record.time),text:'计划 '+planned+' 分钟；实际 '+actual+' 分钟；偏差 '+(actual-planned)+' 分钟。\n'+(timeItems.length?timeItems.map(x=>'- '+(x.text||'未命名事项')+'｜'+(x.done?'完成':x.skipped?'未完成':'待确认')+'｜实际'+(x.actualMinutes||0)+'分').join('\n'):'时间事项未记录')},
    {key:'action',name:'行动管理',status:eveningStatus(c.action,record.action),text:actionItems.length?actionItems.map((x,i)=>(i+1)+'. 目标'+(i+1)+' → '+(x.text||'未命名行动')+'｜'+(x.done?'完成':'未完成')+(x.result?'｜结果：'+x.result:'')+(x.reason?'｜原因：'+x.reason:'')).join('\n'):'尚未由报告生成行动'},
    {key:'learning',name:'学习管理',status:eveningStatus(c.learning,record.learning),text:cards.length?cards.map((x,i)=>(i+1)+'. '+(x.title||'未命名学习卡')+'｜核心：'+(x.core||'待归纳')+'｜应用：'+(x.application||'待确认')+'｜结果：'+(x.result||x.status||'待确认')).join('\n'):'今日学习未记录'}
  ];
}
function renderEveningFiveReview(){
  const el=$('eveningFiveReview');if(!el)return;const record=persistUnifiedDailyRecord(true),model=eveningReviewModel(record);
  el.innerHTML=model.map(item=>'<div class="card third" style="box-shadow:none"><h3 style="margin:0 0 8px">'+item.name+' <span class="pill">'+item.status+'</span></h3><div style="white-space:pre-wrap;font-size:13px">'+esc(item.text)+'</div></div>').join('');
  const saved=state.eveningReviews?.[record.date],status=$('eveningReviewStatus');if(status)status.textContent=saved?'已生成 '+dailyClock(saved.generatedAt):'尚未生成';
  const analysis=$('aiReviewAnalysis');if(analysis&&!analysis.value)analysis.value=record.analysis||'';
  const finalStatus=$('finalReviewStatus');if(finalStatus)finalStatus.textContent=record.archivedAt?'已完成总结归档':record.analysis?'分析已填写，待归档':'等待分析';
}
function eveningReviewText(record){
  const model=eveningReviewModel(record),extras=record.extras||{},parts=['# 晚间五项复盘｜'+record.date,''];
  model.forEach((item,i)=>parts.push('## '+(i+1)+'、'+item.name+'（'+item.status+'）',item.text,''));
  parts.push('## 今日调整线索','- 今日最有效的动作：'+(extras.dailyEffectiveAction||'待分析'),'- 问题根因：'+(extras.dailyRootCause||'待分析'),'- 明日保留：'+(extras.dailyTomorrowKeep||'待分析'),'- 明日调整：'+(extras.dailyTomorrowAdjust||'待分析'),'- 明日停止：'+(extras.dailyTomorrowStop||'待分析'),'');
  parts.push('## 请小杰分析','1. 今日目标、行动和实际结果是否对应？','2. 最大差距及根因是什么？','3. 哪些动作应保留、调整、停止？','4. 生成明日目标、对应行动及时间安排。','');
  return parts.join('\n');
}
function generateEveningReviewText(){
  const record=persistUnifiedDailyRecord(true),text=eveningReviewText(record),date=record.date;state.eveningReviews=state.eveningReviews||{};state.eveningReviews[date]={date,text,generatedAt:new Date().toISOString(),status:'等待小杰分析'};record.eveningReview=state.eveningReviews[date];state.dailyRecords[date]=record;state.lastFullReview=text;localStorage.setItem(KEY,JSON.stringify(state));
  const out=$('reviewExport');if(out){out.style.display='block';out.textContent=text}navigator.clipboard?.writeText(text).then(()=>toast('晚间五项复盘已复制，请发给小杰分析')).catch(()=>toast('复盘已生成，请手动复制'));
  if($('reviewAnalysisBoard'))$('reviewAnalysisBoard').textContent='复盘文字已生成。请发给小杰分析，再把分析结论粘贴到下方。';renderEveningFiveReview();return text;
}
confirmAndExportFullReview=function(){return generateEveningReviewText()};
exportFullReview=function(){return generateEveningReviewText()};
const dailyRecordMarkdownBeforeFinalAnalysis=dailyRecordMarkdown;
function nextLocalDateKey(date){const d=new Date(date+'T00:00:00');d.setDate(d.getDate()+1);return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
function analysisSection(text,label){const source=String(text||''),escaped=label.replace(/[.*+?^${}()|[\]\\]/g,'\\const dailyRecordMarkdownBeforeFinalAnalysis=dailyRecordMarkdown;');const match=source.match(new RegExp('(?:^|\\n)#{0,3}\\s*'+escaped+'\\s*[:：]?\\s*\\n?([\\s\\S]*?)(?=\\n#{1,3}\\s|\\n(?:明日心态提醒|明日目标|每个目标对应行动|时间安排|学习安排)\\s*[:：]|$)','i'));return match?match[1].trim():''}
function analysisList(text,label){return analysisSection(text,label).split(/\n/).map(x=>x.replace(/^\s*(?:[-*]|\d+[.、)]|G\d+[:：]?)\s*/,'').trim()).filter(Boolean)}
function buildTomorrowPlanHandoff(record){const nextDate=nextLocalDateKey(record.date),analysis=record.analysis||'',compact=nextDate.replace(/-/g,''),c=record.confirmations||{};const analysisGoals=analysisList(analysis,'明日目标').slice(0,3),savedGoals=(record.tomorrow?.items||[]).filter(Boolean).slice(0,3);const goals=(analysisGoals.length?analysisGoals:savedGoals).map((text,index)=>({goalId:compact+'-G'+(index+1),text}));const actionLines=analysisList(analysis,'每个目标对应行动'),confirmedActions=c.action?.items||dailyItems(record,'action')||[];const actions=goals.map((goal,index)=>{const source=actionLines[index]||confirmedActions[index]?.text||goal.text;return {goalId:goal.goalId,actionId:compact+'-A'+(index+1),text:String(source).replace(/^(?:goalId|actionId)\s*[:：]\s*\S+\s*/i,'').trim()}});const timeLines=analysisList(analysis,'时间安排'),confirmedTime=c.time?.items||dailyItems(record,'time')||[];const schedule=actions.map((action,index)=>{const source=timeLines[index]||'',minutes=Number(source.match(/(\d+)\s*分钟?/)?.[1])||Number(confirmedTime[index]?.plannedMinutes)||[120,60,30][index]||30;const quadrant=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'].find(x=>source.includes(x))||confirmedTime[index]?.quadrant||'重要不紧急';return {actionId:action.actionId,plannedMinutes:minutes,quadrant}});const mindset=analysisSection(analysis,'明日心态提醒')||record.mindset?.tomorrowMindsetAdjust||record.mindset?.mindset?.tomorrowMindsetAdjust||record.extras?.dailyTomorrowAdjust||'保持觉察，先稳定状态再行动';const learning=analysisSection(analysis,'学习安排')||record.extras?.dailyTomorrowKeep||state.roleAction||state.learnApply||'按明日目标安排学习并当天应用';return {date:nextDate,mindset,goals,actions,schedule,learning,confirmedAt:record.finalizedAt}}
function tomorrowPlanHandoffMarkdown(record){if(!record.analysis||!record.finalizedAt)return '';const handoff=record.tomorrowPlanHandoff||buildTomorrowPlanHandoff(record),parts=['## 明日计划交接',''];parts.push('- 日期：'+handoff.date,'- 确认时间：'+handoff.confirmedAt,'','### 明日心态提醒','- '+handoff.mindset,'','### 明日目标');parts.push(handoff.goals.length?handoff.goals.map(x=>'- goalId: `'+x.goalId+'`｜'+x.text).join('\n'):'- 无已确认目标','','### 每个目标对应行动');parts.push(handoff.actions.length?handoff.actions.map(x=>'- goalId: `'+x.goalId+'`｜actionId: `'+x.actionId+'`｜'+x.text).join('\n'):'- 无已确认行动','','### 时间安排');parts.push(handoff.schedule.length?handoff.schedule.map(x=>'- actionId: `'+x.actionId+'`｜计划分钟: '+x.plannedMinutes+'｜四象限: '+x.quadrant).join('\n'):'- 无已确认时间安排','','### 学习安排','- '+handoff.learning,'');return parts.join('\n')}
dailyRecordMarkdown=function(record){
  let text=dailyRecordMarkdownBeforeFinalAnalysis(record);
  if(record.eveningReview?.text)text+='\n## 晚间五项复盘原文\n\n'+record.eveningReview.text+'\n';
  if(record.analysis)text+='\n## 小杰分析与最终复盘总结\n\n'+record.analysis+'\n';
  const handoff=tomorrowPlanHandoffMarkdown(record);if(handoff)text+='\n'+handoff+'\n';
  return text;
};
async function confirmAnalysisAndArchive(){
  const analysis=$('aiReviewAnalysis')?.value.trim()||'';if(!analysis){toast('请先粘贴小杰分析结论，再归档最终复盘');return}
  let record=persistUnifiedDailyRecord(true),date=record.date;if(!state.eveningReviews?.[date])generateEveningReviewText();record=state.dailyRecords[date];record.analysis=analysis;record.eveningReview=state.eveningReviews[date];record.eveningReview.status='已完成分析';record.finalizedAt=new Date().toISOString();record.tomorrowPlanHandoff=buildTomorrowPlanHandoff(record);state.dailyRecords[date]=record;state.reviewSummaries=state.reviewSummaries||{};state.reviewSummaries[date]={analysis,finalizedAt:record.finalizedAt,tomorrowPlanHandoff:reviewCopy(record.tomorrowPlanHandoff)};localStorage.setItem(KEY,JSON.stringify(state));
  try{await archiveDailyRecordToObsidian();confirmTomorrowActions();if($('finalReviewStatus'))$('finalReviewStatus').textContent='已完成总结归档';toast('小杰分析与晚间复盘已归档到 Obsidian')}catch(e){if($('finalReviewStatus'))$('finalReviewStatus').textContent='分析已保存，Obsidian待归档';toast('分析已保存在面板；Obsidian归档未完成：'+e.message)}renderEveningFiveReview();
}
writeExecutionToObsidian=function(){return confirmAnalysisAndArchive()};
const renderBeforeEveningReview=render;render=function(){renderBeforeEveningReview();renderEveningFiveReview()};


/* 早晨闭环：只读取昨日最终确认的“明日计划交接”，生成今日五项计划。 */
function previousLocalDateKey(date=localDateKey()){const d=new Date(date+'T00:00:00');d.setDate(d.getDate()-1);return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
function handoffSectionLines(lines,title){const at=lines.findIndex(x=>x.trim()==='### '+title);if(at<0)return[];const out=[];for(let i=at+1;i<lines.length;i++){if(/^###?s/.test(lines[i].trim()))break;if(lines[i].trim())out.push(lines[i].trim())}return out}
function handoffCode(line,label){const token=label+': \x60',start=line.indexOf(token);if(start<0)return'';const from=start+token.length,end=line.indexOf('\x60',from);return end<0?'':line.slice(from,end).trim()}
function parseTomorrowPlanHandoffMarkdown(raw){
  const marker='## 明日计划交接',start=raw.lastIndexOf(marker);if(start<0)throw new Error('昨晚日志没有已确认的“明日计划交接”');
  const block=raw.slice(start),lines=block.split(String.fromCharCode(10)).map(x=>x.split(String.fromCharCode(13)).join('')),date=(lines.find(x=>x.startsWith('- 日期：'))||'').slice(5).trim(),confirmedAt=(lines.find(x=>x.startsWith('- 确认时间：'))||'').slice(7).trim();
  if(!date||!confirmedAt)throw new Error('明日计划交接尚未最终确认');
  const bullet=line=>line.startsWith('- ')?line.slice(2).trim():line.trim(),mindset=bullet(handoffSectionLines(lines,'明日心态提醒')[0]||''),learning=bullet(handoffSectionLines(lines,'学习安排')[0]||'');
  const goals=handoffSectionLines(lines,'明日目标').map(line=>{const goalId=handoffCode(line,'goalId'),parts=line.split('｜');return goalId&&parts[1]?{goalId,text:parts.slice(1).join('｜').trim()}:null}).filter(Boolean).slice(0,3);
  const actions=handoffSectionLines(lines,'每个目标对应行动').map(line=>{const goalId=handoffCode(line,'goalId'),actionId=handoffCode(line,'actionId'),parts=line.split('｜');return goalId&&actionId&&parts[2]?{goalId,actionId,text:parts.slice(2).join('｜').trim()}:null}).filter(Boolean);
  const schedule=handoffSectionLines(lines,'时间安排').map(line=>{const actionId=handoffCode(line,'actionId'),parts=line.split('｜'),minutes=Number((parts[1]||'').split(':').pop().trim()),quadrant=(parts[2]||'').split(':').slice(1).join(':').trim();return actionId&&minutes?{actionId,plannedMinutes:minutes,quadrant:quadrant||'重要不紧急'}:null}).filter(Boolean);
  return {date,confirmedAt,mindset,goals,actions,schedule,learning};
}
function validateMorningHandoff(handoff){
  const today=localDateKey();if(handoff.date!==today)throw new Error('昨晚交接日期是 '+handoff.date+'，不是今天 '+today);
  const goalIds=new Set(handoff.goals.map(x=>x.goalId)),actionIds=new Set(handoff.actions.map(x=>x.actionId));
  if(!handoff.goals.length)throw new Error('昨晚没有确认明日目标');
  if(handoff.actions.some(x=>!goalIds.has(x.goalId)))throw new Error('行动与目标关联不完整');
  if(handoff.schedule.some(x=>!actionIds.has(x.actionId)))throw new Error('时间安排与行动关联不完整');
  return true;
}
function applyMorningHandoff(handoff,source){
  validateMorningHandoff(handoff);const date=handoff.date,existing=state.todayPlanDaily?.[date];if(existing?.confirmedAt===handoff.confirmedAt){renderTodayFivePlan();toast('今日五项计划已经生成');return existing}
  const goals=handoff.goals.map((x,index)=>({id:x.goalId,text:x.text,category:'重点工作',quadrant:handoff.schedule.find(t=>handoff.actions.find(a=>a.goalId===x.goalId)?.actionId===t.actionId)?.quadrant||'重要不紧急',source:'昨晚最终复盘',order:index+1}));
  const actions=handoff.actions.map((x,index)=>({id:x.actionId,goalId:x.goalId,text:x.text,category:'重点工作',quadrant:handoff.schedule.find(t=>t.actionId===x.actionId)?.quadrant||'重要不紧急',source:'昨晚最终复盘',done:false,result:'',reason:''}));
  const timeItems=handoff.schedule.map(item=>{const action=actions.find(x=>x.id===item.actionId);return {actionId:item.actionId,goalId:action?.goalId,text:action?.text||'未命名行动',category:'重点工作',quadrant:item.quadrant,plannedMinutes:item.plannedMinutes,actualMinutes:0,startedAt:null,completedAt:null,done:false,skipped:false}});
  state.todayPlanDaily=state.todayPlanDaily||{};state.todayPlanDaily[date]={...handoff,source,loadedAt:new Date().toISOString(),status:'已生成',version:1};state.todayMindsetPlan={date,text:handoff.mindset,source};state.todayLearningPlan={date,text:handoff.learning,source};state.dailyGoal=goals[0]?.text||'';state.tasks=goals.map(x=>({text:x.text,done:false,goalId:x.id}));while(state.tasks.length<3)state.tasks.push({text:'',done:false});
  state.goalActionDaily=state.goalActionDaily||{};state.goalActionDaily[date]={date,goals,actions,updatedAt:new Date().toISOString(),source};state.actionItems=actions;state.actionSourceDate=date;
  state.timeItems=timeItems;state.timeDaily=state.timeDaily||{};state.timeDaily[date]={date,items:dailyCopy(timeItems),savedAt:new Date().toISOString(),source};state.timeActiveDate=date;state.timeSourceDate=date;state.todayLearningSources=[handoff.learning].filter(Boolean);
  localStorage.setItem(KEY,JSON.stringify(state));render();renderTodayFivePlan();persistUnifiedDailyRecord(true);toast('已从昨晚最终归档生成今日五项计划');return state.todayPlanDaily[date];
}
async function loadTodayPlanFromYesterdayArchive(){
  try{if(!obsidianVaultHandle){if(!window.showDirectoryPicker)throw new Error('当前浏览器不支持目录连接');obsidianVaultHandle=await window.showDirectoryPicker()}const dir=await resolveDailyReviewDirectory(),yesterday=previousLocalDateKey(),handle=await dir.getFileHandle('每日复盘_'+yesterday.replace(/-/g,'')+'.md'),file=await handle.getFile(),raw=await file.text(),handoff=parseTomorrowPlanHandoffMarkdown(raw);if($('syncStatus'))$('syncStatus').textContent='Vault 已连接｜已读取复盘 '+yesterday;return applyMorningHandoff(handoff,'Obsidian 每日复盘_'+yesterday.replace(/-/g,''))}catch(e){if($('todayPlanStatus'))$('todayPlanStatus').textContent='今日计划未生成：'+e.message;toast('今日计划未生成：'+e.message);return null}
}
function renderTodayFivePlan(){
  const date=localDateKey(),plan=state.todayPlanDaily?.[date],status=$('todayPlanStatus'),view=$('todayFivePlanView');if(status)status.textContent=plan?'已从昨晚最终归档生成｜确认时间 '+new Date(plan.confirmedAt).toLocaleString('zh-CN'):'尚未读取昨晚最终归档。';
  if(view)view.innerHTML=plan?[
    ['心态',plan.mindset||'未安排'],['目标',plan.goals.map(x=>x.text).join('；')||'未安排'],['时间',plan.schedule.map(x=>x.plannedMinutes+'分｜'+x.quadrant).join('；')||'未安排'],['行动',plan.actions.map(x=>x.text).join('；')||'未安排'],['学习',plan.learning||'未安排']
  ].map(x=>'<div class="kpi"><span>'+x[0]+'</span><b style="font-size:14px">'+esc(x[1])+'</b></div>').join(''):'';
  if($('todayMindsetPlanView'))$('todayMindsetPlanView').textContent=plan?.mindset||'尚未生成今日心态提醒。';if($('todayLearningPlanView'))$('todayLearningPlanView').textContent=plan?.learning||'尚未生成今日学习安排。';
}
const connectObsidianVaultBeforeMorning=connectObsidianVault;
connectObsidianVault=async function(){if(!window.showDirectoryPicker){toast('当前浏览器不支持直接连接 Vault');return}try{obsidianVaultHandle=await window.showDirectoryPicker();if($('syncStatus'))$('syncStatus').textContent='Vault 已连接';toast('Obsidian Vault 已连接');if(!state.todayPlanDaily?.[localDateKey()])await loadTodayPlanFromYesterdayArchive()}catch(e){toast('已取消连接')}}
function initMorningDailyPlan(){const yesterday=previousLocalDateKey(),cached=state.dailyRecords?.[yesterday]?.tomorrowPlanHandoff,record=state.dailyRecords?.[yesterday];if(!state.todayPlanDaily?.[localDateKey()]&&cached&&record?.finalizedAt){try{applyMorningHandoff(cached,'昨晚已确认的面板缓存')}catch(e){}}renderTodayFivePlan()}

function initUnifiedDailyArchive(){
  migrateUnifiedDailyRecords();DAILY_REVIEW_FIELD_IDS.forEach(id=>{const el=$(id);if(el)el.value=state.dailyRecords?.[localDateKey()]?.extras?.[id]||''});
  const picker=$('historyRecordDate');if(picker){picker.value=localDateKey();renderDailyRecordHistory(picker.value)}
  persistUnifiedDailyRecord(true);renderDailyArchiveStatus();renderEveningFiveReview();
  let timer;const schedule=()=>{clearTimeout(timer);timer=setTimeout(()=>persistUnifiedDailyRecord(true),450)};document.addEventListener('input',schedule);document.addEventListener('change',schedule);
}

/* 晚间集中填写模式：白天看四象限，晚上只按任务填写实际分钟。 */
timeCompletion=function(items=state.timeItems||[]){
  const hasItems=items.length>0;
  const quadrantsReady=hasItems&&items.every(item=>Boolean(item.quadrant&&item.category));
  const resultsReady=hasItems&&items.every(item=>item.done||item.skipped);
  const actualReady=resultsReady&&items.filter(item=>item.done).every(item=>Number(item.actualMinutes)>0);
  const checks=[
    {name:'今日事项已带入',done:hasItems},
    {name:'四象限已分类',done:quadrantsReady},
    {name:'今晚结果已逐项填写',done:resultsReady},
    {name:'实际用时已完整',done:actualReady}
  ];
  return {checks,done:checks.filter(item=>item.done).length,total:checks.length};
};
function setEveningTimeMinutes(index,value){
  ensureTimeItems();const item=state.timeItems[index];if(!item)return;
  const minutes=Math.max(0,Number(value)||0);item.actualMinutes=minutes;item.startedAt=null;item.completedAt=null;
  if(minutes>0){item.done=true;item.skipped=false}else if(!item.skipped){item.done=false}
  persistTimeDaily();renderSlots();persistUnifiedDailyRecord?.(true);
  toast(minutes>0?'实际用时已保存并记为完成':'实际用时已清空');
}
function setEveningTimeSkipped(index){
  ensureTimeItems();const item=state.timeItems[index];if(!item)return;
  item.actualMinutes=0;item.done=false;item.skipped=true;item.startedAt=null;item.completedAt=null;
  persistTimeDaily();renderSlots();persistUnifiedDailyRecord?.(true);toast('已记录今日未做');
}
function clearEveningTimeResult(index){
  ensureTimeItems();const item=state.timeItems[index];if(!item)return;
  item.actualMinutes=0;item.done=false;item.skipped=false;item.startedAt=null;item.completedAt=null;
  persistTimeDaily();renderSlots();persistUnifiedDailyRecord?.(true);toast('已恢复为今晚待填写');
}
renderTimeDailyStatus=function(){
  const el=$('timeDailyStatus');if(!el)return;const items=state.timeItems||[],progress=timeCompletion(items),recorded=items.filter(item=>item.done||item.skipped).length,actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0);
  el.innerHTML=`<span class="pill">${localDateKey()} 自动保存</span><span class="pill">今晚已填 ${recorded}/${items.length}项</span><span class="pill">实际 ${actual}分钟</span><span class="pill">完整度 ${progress.done}/${progress.total}</span>`;
};
renderSlots=function(){
  ensureTimeItems();const items=state.timeItems||[];
  const card=(item,index)=>{
    const status=item.done?'已完成':item.skipped?'今日未做':'今晚待填写',statusColor=item.done?'#2f855a':item.skipped?'#9b2c2c':'#8a6d45';
    return `<div class="evening-time-card" style="padding:16px;border:1px solid var(--line);border-radius:12px;background:var(--card);display:grid;grid-template-columns:minmax(220px,1fr) minmax(150px,220px);gap:16px;align-items:center;margin-top:12px">
      <div><b>${index+1}. ${esc(item.text)}</b><small style="display:block;color:var(--muted);margin-top:6px">${esc(item.category||'未分类')}｜计划 ${Number(item.plannedMinutes)||0} 分钟</small><small style="display:block;color:${statusColor};margin-top:4px">${status}</small></div>
      <div style="display:flex;gap:8px;align-items:center;justify-content:flex-end;flex-wrap:wrap"><label style="display:flex;align-items:center;gap:6px;min-height:44px;padding:0 12px;border:1px solid var(--line);border-radius:10px;background:#fff"><span>实际</span><input type="number" min="0" inputmode="numeric" value="${Number(item.actualMinutes)||''}" placeholder="分钟" aria-label="${esc(item.text)}实际分钟" style="width:72px;border:0;font-size:16px;background:transparent" onchange="setEveningTimeMinutes(${index},this.value)"><span>分</span></label>${item.skipped||item.done?`<button class="btn ghost" type="button" onclick="clearEveningTimeResult(${index})">重新填写</button>`:`<button class="btn ghost" type="button" onclick="setEveningTimeSkipped(${index})">今日未做</button>`}</div>
    </div>`;
  };
  $('slots').innerHTML=items.length?`<div class="notice"><b>今晚只填两件事：</b>做了就填实际分钟；没做就点“今日未做”。不需要再次判断四象限。</div>${items.map((item,index)=>card(item,index)).join('')}`:'<div class="notice">今天没有从昨日复盘生成的任务，暂无用时可填写。</div>';  const planned=items.reduce((sum,item)=>sum+(Number(item.plannedMinutes)||0),0),actual=items.reduce((sum,item)=>sum+(Number(item.actualMinutes)||0),0),done=items.filter(item=>item.done).length;
  if($('timePlanView'))$('timePlanView').textContent=planned+'分钟';if($('timeCapacityView'))$('timeCapacityView').textContent=(720-actual>=0?'余':'超')+Math.abs(720-actual)+'分钟';if($('timeActualView'))$('timeActualView').textContent=actual+'分钟';if($('timeDoneView'))$('timeDoneView').textContent=done+' / '+items.length;if($('timeRateView'))$('timeRateView').textContent=(planned?Math.round(actual/planned*100):0)+'%';
  renderTimeQuadrantMatrix();renderTodayTimePieChart();renderTimeSummary();renderTimeDailyStatus();
};

/* 今日四象限的唯一来源：昨天五项管理复盘总结报告。 */
const REVIEW_QUADRANTS=['紧急且重要','重要不紧急','紧急不重要','不紧急不重要'];
function previousDateKey(date=localDateKey()){const value=new Date(date+'T12:00:00');value.setDate(value.getDate()-1);return localDateKey(value)}
function reviewPlanForDate(date=localDateKey()){
  const plan=window.MeiDailyReviewPlans?.[date];if(!plan||!Array.isArray(plan.items))return null;
  return {...plan,items:plan.items.map(item=>({...item,quadrant:REVIEW_QUADRANTS.includes(item.quadrant)?item.quadrant:'待小杰判断',actualMinutes:0,startedAt:null,completedAt:null,done:false,skipped:false}))};
}
defaultTimeItemsForDate=function(date){return timeCopy(reviewPlanForDate(date)?.items||[])};
initializeTimeDaily=function(){
  const date=localDateKey(),plan=reviewPlanForDate(date);state.timeDaily=state.timeDaily||{};const saved=state.timeDaily[date],hasRecorded=Boolean(saved?.items?.some(item=>item.done||item.skipped||Number(item.actualMinutes)>0));
  if(saved?.source==='previous-review'&&saved?.items){state.timeItems=timeCopy(saved.items)}
  else if(plan){state.timeItems=timeCopy(plan.items);state.timeDaily[date]={date,items:timeCopy(state.timeItems),source:'previous-review',sourceDate:plan.sourceDate||previousDateKey(date),reportPath:plan.reportPath||'',savedAt:new Date().toISOString()}}
  else if(saved?.items&&hasRecorded){state.timeItems=timeCopy(saved.items);state.timeDaily[date]={...saved,source:'legacy-record'}}
  else{state.timeItems=[];state.timeDaily[date]={date,items:[],source:'missing-previous-review',sourceDate:previousDateKey(date),savedAt:new Date().toISOString()}}
  state.timeSourceDate=date;state.timeActiveDate=date;localStorage.setItem(KEY,JSON.stringify(state));
};
ensureTimeItems=function(){const date=localDateKey();if(state.timeActiveDate!==date||!Array.isArray(state.timeItems))initializeTimeDaily()};
timeCompletion=function(items=state.timeItems||[]){
  const source=state.timeDaily?.[localDateKey()]?.source,hasItems=items.length>0,quadrantsReady=source==='previous-review'&&hasItems&&items.every(item=>REVIEW_QUADRANTS.includes(item.quadrant)),resultsReady=hasItems&&items.every(item=>item.done||item.skipped),actualReady=resultsReady&&items.filter(item=>item.done).every(item=>Number(item.actualMinutes)>0);
  const checks=[{name:'昨日复盘已生成今日事项',done:source==='previous-review'&&hasItems},{name:'四象限由昨日复盘确定',done:quadrantsReady},{name:'今晚结果已逐项填写',done:resultsReady},{name:'实际用时已完整',done:actualReady}];return {checks,done:checks.filter(item=>item.done).length,total:checks.length};
};
function renderTimePlanSource(){
  const el=$('timePlanSource');if(!el)return;const date=localDateKey(),record=state.timeDaily?.[date]||{},previous=record.sourceDate||previousDateKey(date);
  if(record.source==='previous-review')el.innerHTML=`<span class="pill">✓ 来源：${previous} 五项管理复盘</span>${record.reportPath?`<span class="pill">${esc(record.reportPath)}</span>`:''}`;
  else if(record.source==='legacy-record')el.innerHTML='<div class="notice"><b>来源提醒：</b>这批事项保留了已填写的实际时间，但四象限不是由昨天复盘报告生成，不能作为正式判断依据。</div>';
  else el.innerHTML=`<div class="notice"><b>尚未生成：</b>没有读取到 ${previous} 的复盘总结计划。请先让小杰根据昨天五项管理复盘生成今天的行动、四象限和时间事项线。</div>`;
}
const renderTimeDailyStatusFromReview=renderTimeDailyStatus;
renderTimeDailyStatus=function(){renderTimeDailyStatusFromReview();renderTimePlanSource()};

setTimeout(()=>{initializeMindsetDaily();initializeTimeDaily();ensureCanonicalLearningCard();render();renderMindsetQuick();renderMindsetDailyFields();renderLearningSourceChecks();renderSlots();renderLearningCards();renderReviewBoard();initUnifiedDailyArchive();initMorningDailyPlan()},0)
/* 客户档案：严格沿用原面板 S0-S5 漏斗管理。 */
const CUSTOMER_STAGE_CONFIG={S0:{label:'待触达',icon:'⏳',color:'#94a3b8'},S1:{label:'刚认识',icon:'👋',color:'#60a5fa'},S2:{label:'有兴趣',icon:'👀',color:'#fbbf24'},S3:{label:'中意向',icon:'🎯',color:'#f472b6'},S4:{label:'高意向',icon:'🔥',color:'#a78bfa'},S5:{label:'已成交',icon:'💰',color:'#34d399'}},CUSTOMER_STAGE_ORDER=['S5','S4','S3','S2','S1','S0'],customerStageMap={'新线索':'S0','已加微信':'S1','已沟通':'S1','明确需求':'S2','深度咨询':'S3','已报价':'S4','已成交':'S5','已交付':'S5','复购／转介绍':'S5','暂不跟进':'S0',cold:'S1',warm:'S2',hot:'S3',boil:'S4'};
state.customerRecords=Array.isArray(state.customerRecords)?state.customerRecords.map(x=>({...x,stage:CUSTOMER_STAGE_CONFIG[x.stage]?x.stage:(customerStageMap[x.stage]||'S1')})):[];
function customerMoney(v){return '¥'+Number(v||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2})}function customerValue(id){return $(id)?.value?.trim?.()||''}function customerStageText(s){const c=CUSTOMER_STAGE_CONFIG[s]||CUSTOMER_STAGE_CONFIG.S1;return c.icon+' '+s+' '+c.label}
function saveCustomer(){const name=customerValue('customerName');if(!name){toast('请先填写客户姓名／昵称');return}const editId=customerValue('customerEditId'),now=new Date().toISOString(),stage=customerValue('customerStage')||'S0',item={id:editId||('c_'+Date.now()),name,source:customerValue('customerSource'),number:customerValue('customerContact'),contact:customerValue('customerContact'),stage,product:customerValue('customerProduct'),expected:Number(customerValue('customerExpected'))||0,addDate:customerValue('customerLastFollow'),lastFollow:customerValue('customerLastFollow'),nextFollow:customerValue('customerNextFollow'),nextAction:customerValue('customerNote'),dealDate:customerValue('customerDealDate'),amount:Number(customerValue('customerDealAmount'))||0,dealAmount:Number(customerValue('customerDealAmount'))||0,paidAmount:Number(customerValue('customerPaidAmount'))||0,need:customerValue('customerNeed'),note:customerValue('customerNote'),updatedAt:now};const i=state.customerRecords.findIndex(x=>String(x.id)===String(editId));item.createdAt=i>=0?(state.customerRecords[i].createdAt||now):now;if(stage==='S5'&&!item.dealDate)item.dealDate=localDateKey();if(i>=0)state.customerRecords[i]=item;else state.customerRecords.unshift(item);localStorage.setItem(KEY,JSON.stringify(state));resetCustomerForm();renderCustomers();renderCustomerIncomeRows();toast(i>=0?'客户档案已更新':'客户已加入S0-S5漏斗')}
function resetCustomerForm(){['customerEditId','customerName','customerContact','customerExpected','customerLastFollow','customerNextFollow','customerDealDate','customerDealAmount','customerPaidAmount','customerNeed','customerNote'].forEach(id=>{if($(id))$(id).value=''});if($('customerSource'))$('customerSource').value='微信';if($('customerStage'))$('customerStage').value='S0';if($('customerProduct'))$('customerProduct').value='待判断'}
function editCustomer(id){const x=state.customerRecords.find(v=>String(v.id)===String(id));if(!x)return;const v={customerEditId:x.id,customerName:x.name,customerSource:x.source||'微信',customerContact:x.number||x.contact,customerStage:x.stage,customerProduct:x.product||'待判断',customerExpected:x.expected,customerLastFollow:x.addDate||x.lastFollow,customerNextFollow:x.nextFollow,customerDealDate:x.dealDate,customerDealAmount:x.amount||x.dealAmount,customerPaidAmount:x.paidAmount,customerNeed:x.need,customerNote:x.nextAction||x.note};Object.entries(v).forEach(([id,val])=>{if($(id))$(id).value=val??''});$('customers')?.scrollIntoView({behavior:'smooth',block:'start'})}
function deleteCustomer(id){const x=state.customerRecords.find(v=>String(v.id)===String(id));if(!x||!confirm('确认删除客户“'+x.name+'”吗？'))return;state.customerRecords=state.customerRecords.filter(v=>String(v.id)!==String(id));localStorage.setItem(KEY,JSON.stringify(state));renderCustomers();renderCustomerIncomeRows()}
function importPreviousCustomers(){let src=[];try{const a=JSON.parse(localStorage.getItem('mj_customers')||'[]');if(Array.isArray(a))src.push(...a)}catch(e){}try{const a=JSON.parse(localStorage.getItem('mx_sys_v3')||'{}').customers||[];src.push(...a)}catch(e){}try{const a=JSON.parse(localStorage.getItem('meijie_customers')||'[]');if(Array.isArray(a))src.push(...a)}catch(e){}const names=new Set(state.customerRecords.map(x=>String(x.name||'').trim())),before=state.customerRecords.length;src.forEach((c,i)=>{const name=String(c.name||'').trim();if(!name||names.has(name))return;const stage=CUSTOMER_STAGE_CONFIG[c.stage]?c.stage:(customerStageMap[c.stage]||customerStageMap[c.temp]||'S1'),amount=Number(c.amount)||0;state.customerRecords.push({id:'old_'+Date.now()+'_'+i,name,number:c.number||c.num||'',contact:c.number||c.num||'',stage,source:'历史档案',product:c.product||'待判断',amount,dealAmount:amount,paidAmount:stage==='S5'?amount:0,note:c.note||'',nextAction:c.nextAction||c.action||'',addDate:c.addDate||c.date||'',lastFollow:c.addDate||c.date||'',createdAt:new Date().toISOString()});names.add(name)});localStorage.setItem(KEY,JSON.stringify(state));renderCustomers();renderCustomerIncomeRows();toast('已导入原客户档案 '+(state.customerRecords.length-before)+' 人，重复姓名已跳过')}
function renderCustomers(){const all=state.customerRecords||[],today=localDateKey(),due=all.filter(x=>x.stage!=='S5'&&x.nextFollow&&x.nextFollow<=today),cnt=Object.fromEntries(CUSTOMER_STAGE_ORDER.map(s=>[s,all.filter(x=>x.stage===s).length])),s5=all.filter(x=>x.stage==='S5'),rev=s5.reduce((n,x)=>n+(Number(x.amount||x.dealAmount)||0),0),conv=all.length?(s5.length/all.length*100).toFixed(1):'0.0';if($('customerSummary'))$('customerSummary').innerHTML='<div class="kpi"><span>客户总数</span><b>'+all.length+'人</b></div><div class="kpi"><span>S3+S4重点跟进</span><b>'+(cnt.S3+cnt.S4)+'人</b></div><div class="kpi"><span>S5已成交</span><b>'+cnt.S5+'人</b></div><div class="kpi"><span>总成交额</span><b>'+customerMoney(rev)+'</b></div><div class="kpi"><span>S0→S5转化率</span><b>'+conv+'%</b></div><div style="grid-column:1/-1;display:flex;gap:8px;align-items:center;flex-wrap:wrap">'+CUSTOMER_STAGE_ORDER.map(s=>'<div style="flex:1;min-width:92px;text-align:center"><div style="background:'+CUSTOMER_STAGE_CONFIG[s].color+';color:white;border-radius:8px;padding:8px;font-weight:700">'+cnt[s]+'</div><small>'+s+' '+CUSTOMER_STAGE_CONFIG[s].label+'</small></div>').join('')+'</div>';if($('customerDueList'))$('customerDueList').innerHTML=due.length?due.sort((a,b)=>a.nextFollow.localeCompare(b.nextFollow)).map(x=>'<div class="slot"><span class="tag">'+(x.nextFollow<today?'已超期':'今天')+'</span><div><b>'+esc(x.name)+'</b><small style="display:block;color:var(--muted)">'+customerStageText(x.stage)+'｜'+esc(x.nextAction||x.need||'下一步行动待补')+'</small></div><button class="btn soft" type="button" onclick="editCustomer(&quot;'+x.id+'&quot;)">去跟进</button></div>').join(''):'<div class="notice">今天没有到期或超期客户。</div>';const q=customerValue('customerSearch').toLowerCase(),f=customerValue('customerStageFilter'),rows=all.filter(x=>(!f||x.stage===f)&&(!q||[x.name,x.number,x.product,x.need,x.note,x.nextAction].join(' ').toLowerCase().includes(q))).sort((a,b)=>CUSTOMER_STAGE_ORDER.indexOf(a.stage)-CUSTOMER_STAGE_ORDER.indexOf(b.stage));if($('customerTable'))$('customerTable').innerHTML='<div style="overflow:auto"><table class="goal-table"><thead><tr><th>客户／生命数字</th><th>S级分类</th><th>产品／金额</th><th>备注</th><th>下一步行动</th><th>操作</th></tr></thead><tbody>'+(rows.map(x=>'<tr><td><b>'+esc(x.name)+'</b><small style="display:block;color:var(--muted)">'+esc(x.number||x.contact||'编号待补')+'</small></td><td><span class="pill">'+customerStageText(x.stage)+'</span></td><td>'+esc(x.product||'待判断')+'<small style="display:block;color:var(--muted)">'+customerMoney(x.amount||x.dealAmount||x.expected)+'</small></td><td>'+esc(x.need||x.note||'待补充')+'</td><td>'+(x.nextFollow||'日期待安排')+'<small style="display:block;color:var(--muted)">'+esc(x.nextAction||'行动待补充')+'</small></td><td><div class="row"><button class="btn soft" type="button" onclick="editCustomer(&quot;'+x.id+'&quot;)">编辑</button><button class="btn ghost" type="button" onclick="deleteCustomer(&quot;'+x.id+'&quot;)">删除</button></div></td></tr>').join('')||'<tr><td colspan="6">还没有匹配的客户记录</td></tr>')+'</tbody></table></div>'}
function renderCustomerIncomeRows(){const b=$('confirmedIncomeRows');if(!b)return;b.querySelectorAll('[data-customer-income]').forEach(r=>r.remove());(state.customerRecords||[]).filter(x=>x.stage==='S5'&&Number(x.paidAmount||x.amount)>0).forEach(x=>{const r=document.createElement('tr');r.dataset.customerIncome='1';r.innerHTML='<td>'+(x.dealDate||x.addDate||'待补日期')+'</td><td>'+esc(x.name)+'</td><td>'+esc(x.product||'待判断')+'</td><td>'+customerMoney(x.amount||x.dealAmount)+'</td><td>'+customerMoney(x.paidAmount||x.amount)+'</td><td><span class="pill">S5客户档案</span></td>';b.appendChild(r)})}
renderCustomers();renderCustomerIncomeRows();

</script>
<script src="daily-review-plan.js"></script>
<script src="mindset-assessment-core.js"></script>
<script src="mindset-evidence.js"></script></body>
</html>




















